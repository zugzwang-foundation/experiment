# SCAFFOLD.13-A — Postgres cutover to Supabase Pro (substrate-setup PR)

**Status:** Closed 2026-05-17
**Branch:** `feat/scaffold-13-a` (3 plan commits + 1 .env.example commit + 1 log commit at log-write time; final PR# update commit pending)
**PR:** #<TBD — fills on `gh pr create`>
**Predecessor:** tracker-sweep-v9 (`docs/logs/tracker-sweep-v9.md`, `main` at `82bee48`)
**Unblocks:** SCAFFOLD.13-B (credential rotation per Handoff section below, then Doppler + Upstash/R2/Resend env audit); HARDEN.* connection-pool tuning; HARDEN.* PITR add-on + backup-runbook + restore-drill; SCAFFOLD.3-FOLLOWUP-1 (Better Auth Content-Type bug per §0 finding 5 in plan)

## What landed (§5.9 field 1)

- PROD Supabase Pro project (`zugzwang-experiment-prod`, ap-south-1 Mumbai, Session pooler @ 5432, daily backups active) **verified pre-existing** (provisioned + 6 migrations applied 2026-05-15 per A4 q0 finding; predates this session). PITR add-on (~$100/mo, coupled with on-demand snapshots) deferred to HARDEN.
- 6 drizzle migrations confirmed applied to PROD via A4 q0 cursor verification: `__drizzle_migrations.last_id=6, last_hash=0e9e617f...242edf4e`. A5 db-migration-reviewer verified `shasum -a 256 drizzle/migrations/0005_auth_schema_corrections.sql` produces **exact match** for the plan's stated hash — strongest file-level evidence cursor 0005 genuinely tracks the canonical migration file. A3 `pnpm drizzle-kit migrate` was an idempotent no-op (journal cursor already at 0005).
- `DATABASE_URL` wired to PROD via Vercel web dashboard (operator-applied 2026-05-17 mid-session, **out of plan procedural sequence** per §0 finding 4). Scope = Production + Preview (single grouped entry per Vercel UI grouping); Sensitive flag on; auto-redeployed to build `3QvGjQ32M` from commit `82bee48`. Stored value unreadable post-set; operational verification via A10 first-smoke 415 is authoritative.
- A10 PARTIAL first-smoke landed in browser at `experiment-zugzwang-worlds-projects.vercel.app`: `/sign-in` renders cleanly (HTTP 200); Email-OTP POST to `/api/auth/email-otp/send-verification-otp` returned HTTP 415 with Better Auth Content-Type validation error. The 415 is **end-to-end runtime evidence** `DATABASE_URL` is correctly wired (request reached Better Auth past the auth-handler boundary; would have been 500 with Postgres/connection error if wiring failed). Surfaced downstream bug: SCAFFOLD.3 sign-in form posts urlencoded; route expects JSON — separate tracker entry SCAFFOLD.3-FOLLOWUP-1.
- A11 rollback anchor **satisfied incidentally** per §0 finding 6: original A11 procedure (on-demand snapshot via Supabase dashboard) infeasible because on-demand snapshots require PITR add-on (deferred). The **2026-05-16 23:30:19 UTC scheduled daily backup** (Pro tier default, included; predates A6/A7 cutover) captures the post-migration / pre-traffic state — de facto rollback anchor. No operator action in Supabase needed.
- `.env.example` drift fix landed (per A9 + §2.1 housekeeping): line 3 stale "Vercel env vars + Doppler" comment updated to "Vercel env vars (Sensitive flag on; per SCAFFOLD.13-A); Doppler integration is deferred to SCAFFOLD.13-B". SCAFFOLD.4 block was already clean — no additional drift found.
- Plan + log committed; interim project (`niihrpqgzxpczyignxnn`, ap-northeast-1 Tokyo, **schemaless** per §0 finding 2) deletion scheduled +24h soft window per §10 Q9 amendment (no data to drain — grace is for unrelated regression discovery).
- Mid-session security incident: **caught + rotated** (PROD + INTERIM `DATABASE_URL` credentials leaked into Claude Code chat at A4 q0 protocol; both passwords rotated mid-session; new URLs re-captured via `read -rs` discipline). Also **caught + scrubbed in-session**: residual literal credential in `.claude/settings.local.json:129` permission-allowlist entry — gitignored so never reached git history; scrubbed before plan amendment commit; verified `grep -n "Surprise1-Said0" .claude/settings.local.json` empty.

## Decisions made (§5.9 field 2)

- **§0 amendment log: three successive amendment states same day (2026-05-17).** Initial (post-A4 q0): 3 candidate findings. First counter-amendment: finding 3 RETRACTED (Sensitive-var CLI artifact). Second counter-amendment: finding 3 RESOLVED via A10 415 ground-truth + findings 4-6 added.
- **Q1-Q10 resolutions** (per plan §10). Notably resolved: Q5 (interim region = `ap-northeast-1` Tokyo); Q8 (cron bridge retirement decoupled from PITR per §0 finding 6 — bridge was orphaned anyway).
- **Reviewer-call invocation summary.** Both reviewer calls invoked via general-purpose Agent with role briefing baked in per CLAUDE.md §5.11:
  - A5 db-migration-reviewer (`.claude/agents/db-migration-reviewer.md`): **PASS for proceeding to A12.** Verified PROD schema correctness vs canonical migration files via SHA match + journal consistency; reframed scope as retroactive state review (not procedural gate); 4 SURPRISE items (2 pre-existing FK-index gaps deferred to HARDEN per operator decision; 1 verification-posture note; 1 procedural-divergence note already captured in §0). Stray opening-line "Sentry wiring is HARDEN.* scope" preamble at top of reviewer output — flag for `.claude/agents/db-migration-reviewer.md` hardening per maintenance items below (no harm; auditor instructions could add explicit "output format: report only, no preamble" guard).
  - A8 security-auditor (`.claude/agents/security-auditor.md`): **CONDITIONAL → resolved.** Two HIGH findings: HIGH #1 (`.claude/settings.local.json:129` rotated INTERIM URL persistence) scrubbed in-session before plan commit; HIGH #2 (`/tmp/scaffold14-env.txt` live SCAFFOLD.14 vendor credentials) operator-deferred to SCAFFOLD.13-B kickoff per Handoff section below. 1 MEDIUM (transcript persistence, augmented into SURPRISE 4). 2 LOW (`.env.local` mode 644 → deferred to 13-B; vercel env ls audit-trail backstop → executed, output captured as §14 appendix).
- **A11 procedure infeasible** (per §0 finding 6): on-demand snapshots require PITR add-on (HARDEN-deferred). Substituted incidentally by 2026-05-16 23:30 UTC scheduled daily backup. Plan-review template miss (future templates should verify dashboard procedures available on chosen plan tier before prescribing).
- **A6/A7 procedural divergence** (per §0 finding 4): operator-applied via Vercel dashboard mid-session, before A5 reviewer call + A11 anchor. State landed correct (A10 415 evidence); procedure broke. Captured for future-discipline.
- **A12 vitest baseline-figure update.** Plan §11 originally stated "matches SCAFFOLD.3 baseline (58 pass, 5 todo)"; current run (2026-05-17): 133 passed + 5 todo (138 total) across 26 test files, exit 0. +75 since SCAFFOLD.3 era; no regressions; all 5 todo carry forward. Plan §11 amended per A12 SURPRISE finding.
- **SECURITY-1 tracker entry NOT minted.** Per tracker-sweep cadence rule agreed 2026-05-17, the credential-hygiene work was folded into SCAFFOLD.13-B's load-bearing first action (per Handoff section below) rather than minting a parallel tracker entry. SCAFFOLD.13-B's stratum description amended at its own kickoff to include the rotation work as its first step.

## Surprises caught + fixed in-session (§5.10 — 8 entries)

1. **PROD pre-existed (§0 finding 1).** Project + migrations applied 2026-05-15 (before session). A2 became URL-capture; A3 became no-op verification. State correct.
2. **Interim schemaless (§0 finding 2).** Interim Tokyo project never received `drizzle-kit migrate`; plan §6 parity-vs-interim framework structurally invalid; reframed to PROD-only schema-correctness vs canonical migration files.
3. **Sensitive-var CLI artifact (§0 finding 3 retraction).** `vercel env pull` returned `DATABASE_URL=""` — mistaken initially for "Vercel env is empty"; root cause: Vercel marks `DATABASE_URL` Sensitive (CLI returns empty for Sensitive vars by deliberate platform-security design). Retracted same day; later resolved via A10 first-smoke 415 evidence.
4. **Credential-leak incident (caught + rotated, with residual historical-record exposure).** PROD + INTERIM `DATABASE_URL` credentials leaked into Claude Code chat during A4 q0 protocol; Claude Code refused execution + recommended immediate rotation; both passwords rotated mid-session; new URLs re-captured via `read -rs` discipline. A8 security-auditor scope expanded to audit incident. **Augmented per A8 audit (MEDIUM finding 3):** post-rotation Claude Code transcripts (`~/.claude/projects/.../*.jsonl` + `~/.claude/history.jsonl`) retain historical PROD + INTERIM URLs with **burned** passwords; new post-rotation passwords confirmed absent (auditor verified `read -rs` discipline worked). Persistence as historical record is expected; mitigated by rotation. Out-of-repo (operator-side ~/.claude/); not eligible for in-PR scrub.
5. **Procedural divergence A6/A7 (§0 finding 4).** Operator applied PROD URL via Vercel dashboard mid-session, out of plan sequence (before A4 close + A5 reviewer call + A11 anchor). State landed correct; procedure broke. Captured for future-discipline: plan templates should instrument a "dashboard-side actions log" to keep operator dashboard work in sync with Claude Code-orchestrated sequence.
6. **A11 procedure infeasible, substituted by scheduled-backup-by-coincidence (§0 finding 6).** Web-Claude plan-review prescribed mandatory on-demand snapshot via Supabase dashboard; infeasible because on-demand snapshots require PITR add-on (deferred to HARDEN). The 2026-05-16 23:30:19 UTC scheduled daily backup (Pro tier default, included; predates A6/A7) substitutes incidentally as the de facto rollback anchor. Plan-review-template miss: future templates should verify dashboard procedures are available on the chosen plan tier before prescribing them.
7. **`.claude/settings.local.json:129` literal credential persistence (caught + scrubbed via A8 HIGH #1).** A8 security-auditor surfaced that the rotated INTERIM `DATABASE_URL` (with burned password `Surprise1-Said0-Deluxe3-Stoppage7-Tablet5` + INTERIM project ref) was baked verbatim into a permission-allowlist entry on line 129. File is gitignored (verified: `git log -p --all -S "Surprise1-Said0"` returns empty), so never propagated to git history. Local-disk persistence only. Scrubbed in-session before plan commit; verified `grep -n "Surprise1-Said0" .claude/settings.local.json` returns empty. Wildcard `Bash(vercel env *)` on line 127 covers future invocations. **Maintenance candidate:** future allowlist entries should match `Bash(<tool> <subcommand> *)` wildcards, NOT literal commands with values; literal-command allowlist entries are a leakage channel.
8. **Live SCAFFOLD.14 vendor credentials in `/tmp/scaffold14-env.txt` — operator-deferred (caught + acknowledged + handed off to SCAFFOLD.13-B, not blocked here).** A8 security-auditor surfaced that `/tmp/scaffold14-env.txt` + `/private/tmp/scaffold14-env.txt` (1035 bytes, world-readable `-rw-r--r--`, 3+ day disk presence) carries **live (un-rotated)** vendor credentials from the 2026-05-14 SCAFFOLD.14 session: `BETTER_AUTH_SECRET`, `ADMIN_PASSWORD`, `GOOGLE_CLIENT_SECRET`, `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`. Out-of-scope for SCAFFOLD.13-A per CLAUDE.md §5.11 (does not block this PR). **Operator (Hrishikesh) acknowledged risk 2026-05-17 ~14:50 IST and deferred both file removal and credential rotation to SCAFFOLD.13-B kickoff as the stratum's first action.** Web Claude flagged but did not block; operator's call as project owner. **Honest record:** the credentials remained on disk at PR-open time. Action chain canonicalized in the named "Handoff to SCAFFOLD.13-B" section below.

## Open questions (§5.9 field 3)

None at close — all 8 surprises resolved or operator-deferred in-session. Tracker-sweep-v9's Q1-Q10 all resolved per plan §10 (Q5 resolved → interim Tokyo; Q8 resolved → cron bridge decoupled from PITR).

## Next session starts at (§5.9 field 4)

- **SCAFFOLD.13-B kickoff** — Doppler integration + Upstash/R2/Resend env audit. **The 6-step credential-rotation handoff is canonicalized in the "Handoff to SCAFFOLD.13-B" section below — that is the load-bearing first action of the 13-B stratum, before any Doppler work.** SCAFFOLD.13-B's stratum description must be amended at its own kickoff to include the credential rotation as its load-bearing first step. SECURITY-1 tracker entry NOT minted per tracker-sweep cadence rule agreed 2026-05-17 — folded into 13-B scope instead.
- **OR ENGINE.6** (events helper at `src/server/events/insert.ts`) per Hrishikesh's queue ordering. If ENGINE.6 runs before SCAFFOLD.13-B, the deferred /tmp credential hygiene per the Handoff section below MUST be the first action of whichever stratum kicks off next (not delayed past two stratum boundaries).
- **New tracker entry:** SCAFFOLD.3-FOLLOWUP-1 (Better Auth Content-Type bug per §0 finding 5) — operator-side tracker amendment post-merge.

## Context to preserve (§5.9 field 5)

- PROD Supabase Pro project (name: `zugzwang-experiment-prod`, ref `zbvprdcyxhlguxbostdj`, ap-south-1 Mumbai, Session pooler URL) — operator-side password manager; **passwords rotated 2026-05-17 post-leak** (new credentials in password manager only). Rotated/burned credentials persist as historical record in operator-side Claude Code transcripts (`~/.claude/projects/.../*.jsonl` + `~/.claude/history.jsonl`); mitigated by rotation.
- Interim project (name: `niihrpqgzxpczyignxnn`, ap-northeast-1 Tokyo, schemaless) — deletion +24h soft window per §10 Q9 amendment; data-drain role reduced since interim is schemaless; 24h matches kickoff and is retained as soft window for unrelated regression discovery.
- 24h grace deletion clock — origin = A10 first-smoke timestamp (2026-05-17); guard = PR-merged-into-main; deletion proceeds only when both conditions satisfied. Operator calendar reminder + tracker v9 MAINT-row.
- Session pooler port 5432 rationale (IPv4 + prepared-statement support) — repeat in SCAFFOLD.13-B for Doppler-wired URL discipline.
- `pg_dump` cron bridge retired at operator's convenience post-merge — bridge was orphaned anyway (interim is schemaless per §0 finding 2; bridge ran against interim and is now orphaned).
- 2026-05-16 23:30:19 UTC scheduled daily backup is the de facto rollback anchor; restore via Supabase dashboard if needed. Pre-launch posture: max 24h data loss between scheduled backups is acceptable (no real user data yet).
- Vercel `DATABASE_URL` is wired with Sensitive flag on, scope Production + Preview single grouped entry. Value unreadable post-creation (Vercel platform-security feature per §0 finding 3 root cause); operational verification via A10 415 is authoritative; audit-trail backstop via `vercel env ls production` (key inventory + scope grouping + added-ago timing) — capture in plan §14 appendix.
- Procedural-discipline maintenance candidates for `docs/maintenance.md`:
  - Instrument "dashboard-side actions log" in plan templates (per §0 finding 4 surprise 5)
  - Verify dashboard procedures available on chosen plan tier before prescribing (per §0 finding 6 surprise 6)
  - Allowlist entries in `.claude/settings.local.json` should match `Bash(<tool> <subcommand> *)` wildcards, NOT literal commands with values (per SURPRISE 7)
  - Hardening guard for `.claude/agents/*.md` briefings: "output format: report only, no preamble" (per A5 reviewer's stray Sentry-wiring preamble observation)
  - Post-session `/tmp` hygiene checklist OR shift to `mktemp` + shell-trap-cleanup pattern (per SURPRISE 8 — `/tmp/scaffold14-env.txt` survived 3+ days)
- A5 + A8 reviewer call outputs captured inline above in "Decisions made"; not committed as separate artifacts (per operator decision — agent transcripts persist in operator-side `~/.claude/projects/*.jsonl` for replay if needed).

## Time (§5.9 field 6 — optional)

Multi-session execution across 2026-05-16 and 2026-05-17; aggregate ~6-8 hours including 6-part plan-review cascade + 2 counter-amendments + 2 reviewer calls + A12 self-audit + close-out log. Three commits on plan (`912c3d3` → `87a2042` → `8c2446f` → upcoming plan §11 amendment + Handoff section commit + .env.example drift commit + log commit + PR# update commit).

## Handoff to SCAFFOLD.13-B (load-bearing first action — credential rotation + Doppler scope) — §5.9 field 7 (project-specific extension)

**Per A8 HIGH finding 2 + LOW finding 4 + operator decision 2026-05-17 ~14:50 IST:** SCAFFOLD.13-B's first action, before any Doppler integration work, is the 6-step credential-hygiene sequence below. This is the canonical action chain — SURPRISE 4 + SURPRISE 7 + SURPRISE 8 are the findings; this section is the action. Future operator (Hrishikesh in a fresh 13-B chat) reads this section as the first thing 13-B does.

1. `rm /tmp/scaffold14-env.txt /private/tmp/scaffold14-env.txt` — closes SURPRISE 8 file-persistence (live SCAFFOLD.14 vendor credentials, world-readable, 3+ day disk presence as of 2026-05-17).
2. `chmod 600 .env.local` — closes A8 LOW finding 4 (operator-deferred to 13-B per amendment).
3. **Rotate** `BETTER_AUTH_SECRET` + `ADMIN_PASSWORD` — highest blast radius. `ADMIN_PASSWORD` is the admin Control Centre password per ADR-0010; `BETTER_AUTH_SECRET` is the session-cookie HMAC key (compromise lets attacker forge any participant session). Generate fresh per `.env.example` comments (`openssl rand -hex 32` for each).
4. **Rotate** `GOOGLE_CLIENT_SECRET` + `RESEND_API_KEY` + `TURNSTILE_SECRET_KEY` — vendor-side primary keys. Operator regenerates from each vendor's dashboard (Google Cloud Console, Resend, Cloudflare Turnstile) per `.env.example` URLs.
5. **Update corresponding Vercel env vars with new values** — dashboard, Sensitive flag on, same Production + Preview single-entry grouping per A6/A7 pattern. Verify post-set via `vercel env ls production` (key inventory + scope grouping; values remain Sensitive-redacted).
6. **THEN** proceed to Doppler scope — the original SCAFFOLD.13-B purpose per tracker-sweep-v9 log §"Path A — SCAFFOLD.13 kickoff first": full env audit across all four vendors (Supabase `DATABASE_URL`, Upstash, R2, Resend) PLUS Doppler-wiring.

**Why this order:** steps 1-2 close immediate file-persistence risk; steps 3-4 burn compromised credentials before Doppler-wire propagates them anywhere new; step 5 syncs Vercel runtime to the new credentials so production still works post-rotation; step 6 then proceeds to the planned Doppler integration with clean credential state.

**Not blocking SCAFFOLD.13-A.** Operator decision 2026-05-17 ~14:50 IST: handed off to 13-B kickoff. Honest record in SURPRISE 8 above.

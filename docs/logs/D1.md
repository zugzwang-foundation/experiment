# D1 — session log (EXECUTE)

**Stratum:** D1 — "stop the drift bleeding" (staging + code-level correctness). Non-critical-path (CLAUDE.md §1), but the per-hash detector is the migration-integrity backbone → carried `@db-migration-reviewer`.
**Branch:** `fix/d1-per-hash-drift` (off `main@68de89a`). **Feature commit:** `dd6d410` (ephemeral — canonical = the squash-merge SHA on `main`). **Plan:** `docs/plans/D1.md`. **Governing ADR:** ADR-0024.
**Date:** 2026-06-25.

---

## What landed (files + PR#)

PR: **pending** (this branch → `main`). 7 files, +271 / −115.

**Source (Changes #2 + #3 + the gate-approved fold-in):**
- `src/server/health/migration-drift.ts` — **#2 detector**: rewritten from a head-`created_at` timestamp check to a PER-HASH multiset compare (`readMigrationFiles("drizzle/migrations")` hashes vs `select hash from drizzle.__drizzle_migrations`). `"ok"` iff multisets equal, else `"drift"`, `"error"` on throw. Pure compare, NO CI/pg_cron conditional (ADR-0024 #6). Old exports `journalHead`/`dbMigrationHeadMillis` deleted; no `server-only`/`@/db` import (param-injected `DriftDbClient`).
- `src/app/api/health/route.ts` — **#2 canary**: `canary` re-sourced to `process.env.VERCEL_GIT_COMMIT_SHA`; no-leak header rewritten to stay truthful (now names `ZUGZWANG_ENV` + `VERCEL_GIT_COMMIT_SHA`); stale "per-env canary" provenance line corrected (code-review LOW).
- `next.config.ts` — **#2 bundling fold-in** (gate-approved): top-level `outputFileTracingIncludes: { "/api/health": ["./drizzle/migrations/**/*"] }`. Forces the journal + `.sql` files into the route's Lambda so `readMigrationFiles` resolves at runtime.
- `src/db/index.ts` — **#3**: `prepare: false` on the postgres-js client (ADR-0024 #8).
- `docs/plans/D1.md` — §2 fold-in addendum (web-authored, committed verbatim).

**Tests (RED-first):**
- `tests/integration/migration-drift.integration.test.ts` — replaced wholesale: per-hash scenarios in-sync / planted-hash-mismatch (RED) / behind, with `drizzle.__drizzle_migrations` snapshot+restore and unstripped `readMigrationFiles` re-seed (CI-safe).
- `tests/server/health/migration-drift.test.ts` — rewritten for the per-hash contract (mocked db + real `readMigrationFiles`); retains the only `catch→"error"` coverage.

**Deliberately UNCHANGED:** `scripts/check-migration-drift.ts` (timestamp+count, CI-safe); prod; schema/migrations (set frozen at `0018`).

## Decisions made
- **Bundling fold-in vs STOP:** the per-hash detector as the plan specified it returned `"error"` in every deployed env (the `.sql`/journal files weren't traced into the `/api/health` Lambda — proven via `route.js.nft.json`: 0 migration files). Surfaced as a blocking SURPRISE + STOPPED for the relay (did not silently expand scope into `next.config.ts`, which is outside plan §2). Web/operator gate: **fold in** with a hard proof gate. The fix is ADR-consistent (keeps `readMigrationFiles`; a build-time hash manifest would have changed the mandated mechanism → rejected). Proven 0→39 traced migration files post-fix.
- **Next 16 config key verified, not assumed:** confirmed `outputFileTracingIncludes` is a **top-level** `NextConfig` key in 16.2.4 (out of `experimental`); proof = the files appearing in the rebuilt route trace.
- **Orphaned unit test → rewrite (not delete):** kept `tests/server/health/migration-drift.test.ts` because the `catch→"error"` branch is covered only there.
- **RED-first integrity:** scenario (b) (planted hash, `created_at` untouched) is the drizzle-orm #5769 regression — proven RED against the old detector (`ok`), green after.

## Surprises caught + fixed in-session
- **BLOCKING SURPRISE (out of plan scope → relay → folded in):** serverless-bundling gap for `readMigrationFiles`. Empirical proof (NFT trace 0→39), forced ADR-consistent remedy (`outputFileTracingIncludes`), D1.md §2 addendum committed verbatim. Full chain in `claude-progress.md`.
- **SURPRISE (in-scope, resolved):** unit test `tests/server/health/migration-drift.test.ts` imported the deleted `journalHead`/`dbMigrationHeadMillis` → `tsc` break (plan named only the integration test). Rewritten via `@test-writer` for per-hash; kept for the error-branch coverage.
- **`@code-reviewer` LOW (fixed):** stale "per-env canary" provenance comment in `route.ts` — touched up (file already in the diff).

## Open questions (for the operator / web)
- **Change #1 (preview→staging repoint) is operator-only** — Vercel decrypt denied to CC. The §1(b) value-confirm command + the Change #1 env edit (Preview `DATABASE_URL` → staging ref `rwfdoqzsghqhhdapxafg`) were emitted; proceeds on the indirect baseline (previews ⇒ prod). Verify post-deploy: preview `/api/health` → `db:"ok"` + `migrations:"ok"`.
- **Flag for the next tracker sweep (NOT this PR):** `package.json` carries caret pins `"postgres": "^3.4.5"` + `"drizzle-orm": "^0.45.0"`, diverging from the literal-pin vendor-dep discipline. Pre-existing; untouched here.

## Next session starts at (exact next action)
After merge: operator runs Change #1 (preview `DATABASE_URL` → staging) + the §1(b) value-confirm, then verifies a preview deploy's `/api/health` reports `migrations:"ok"` (proves the per-hash detector + the trace-include work in a real deployed env). That deployed-verify is the true confirmation of exit-criterion #2. Then D2 (CI required-check + `drizzle-kit check` + env audit) / D3 (staging branch + matcher) per ADR-0024.

## Context to preserve
- The two drift surfaces diverge BY DESIGN (ADR-0024 #6): per-hash on `/api/health` (deployed-only, unstripped), timestamp+count on `check-migration-drift.ts` (runs in CI against the pg_cron-stripped DB). Do not unify them.
- The trace-include is load-bearing: if `next.config.ts` `outputFileTracingIncludes` is ever removed, the deployed detector silently reverts to `"error"`. Any future change to the migrations folder layout must keep the glob valid.
- `@code-reviewer`'s sandbox couldn't see local PG `:54322`; the integration suite (3/3) + full suite (1028 passed) were run green in the execute session.

## Time
~Execute session, 2026-06-25 (recon → RED-first test → impl → blocking-SURPRISE relay → fold-in + proof → cascade → PR).

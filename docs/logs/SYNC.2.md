# SYNC.2 — CC recon executed

> **Stage:** SYNC.2 — read-only recon. **Status:** CLOSED.
> **Mode:** Claude Code (read-only) + web review.
> **Repo snapshot at recon time:** `92b7c47` (`2026-05-29T09:50:16Z`).
> Promoted to `docs/logs/` at SYNC.9 from `docs/sync/RECON.md`; body below is that report, preserved.

---

# RECON.md — SYNC.2 repository reconnaissance

> Ground-truth snapshot of the `zugzwang-foundation/experiment` repo for the
> web-Claude orchestrator to reconcile its context against. Generated by a
> read-only sweep; the only files written this session are this file and its
> sibling `repo-state.json`. **Records what IS, not what should be. Drift is
> flagged, never fixed.**

## At a glance

| Fact | Value |
|---|---|
| Generated at (UTC) | 2026-05-29T09:50:16Z |
| Current commit | `92b7c47` (`92b7c47f265ed36874b1fd38add20fa52262cb71`) |
| Branch | `main` |
| Remote sync | **0 behind, 0 ahead** of `origin/main` — fully synced |
| Working tree | clean (no staged, unstaged, or untracked changes) |
| Total PRs | 58 (#1–#58): **57 MERGED, 1 CLOSED-unmerged, 0 OPEN** |
| Tracked files | **297** |
| Merge commits | 0 (squash-merge workflow — `git log --merges` is empty) |
| Tags | none |
| Latest landed work | SCAFFOLD.8 staging environment (PR #57, merged 2026-05-28) |

---

## STEP 1 — Git + history

- **HEAD:** `92b7c47` — `feat(scaffold-8): staging environment (#57)`
- **No tags.** No annotated or lightweight tags exist in the repo.
- **No merge commits** in the last 40 — confirms a squash/rebase-merge PR workflow; every PR collapses to a single commit on `main`.
- **Branches.** `main` is current. Remote has three branches:
  `origin/main`, `origin/plan/engine-6`, `origin/plan/scaffold-16`. ~22 stale
  local branches linger (e.g. `feat/scaffold-2-stratum-a..d`, `feat/scaffold-3`,
  `chore/claude-md-*`, `plan/engine-6`, `scaffold/observability-bundle`) — all
  belong to already-merged PRs and were never pruned.

Most recent 10 commits:

```
92b7c47 feat(scaffold-8): staging environment (#57)
9a42b8a chore(logs): SCAFFOLD.18 execute review session log (#56)
e26d198 docs(scaffold-8): land brief + plan + plan-mode review log (#55)
e080dab feat(ci): expand CI with Postgres service + migration apply (SCAFFOLD.18) (#54)
e6de02a feat(scaffold-finish-bundle-1): observability stack (SCAFFOLD.5 + .6 + .7) (#53)
45b35e1 feat(scaffold-16): LD-3 text/image Track A carve-out + F-γ-thin §15 ... (#52)
6a6b04b chore: SCAFFOLD.17 post-merge log + tracker entries (#51)
d5be518 feat(scaffold-17): identity-pool seed + pg_cron low-watermark + verification (#50)
42baa8b feat(engine-6): events helper + per-event-type Zod schemas + 6-site emission migration (#49)
8723fa5 docs(scaffold-15): operator-substrate clearance + execute review close-out (#48)
```

---

## STEP 2 — PR history (all states)

58 PRs total, numbered 1–58, no gaps. **57 MERGED**, **1 CLOSED-unmerged**
(#58, `chore: throwaway preview for SCAFFOLD.8 J3 smoke — DO NOT MERGE`,
intentional throwaway), **0 OPEN**.

Phase arc visible in the PR stream:

| PR range | Phase / theme |
|---|---|
| #1–#16 | FOUND.* — repo bootstrap, license, CoC, security policy, ADR-0001, contract docs |
| #17–#20 | CLAUDE.md trims + SPEC.1 drafts + SPEC.1.pdf deletion |
| #21–#31 | SCAFFOLD.2 — drizzle schemas (21 tables / 10 domains), trigger tests, INV-4 |
| #32–#34 | PRECURSOR.5 doc sweep + SCAFFOLD.1 (Tailwind/shadcn/Turbopack) |
| #35–#39 | SCAFFOLD.14 (auth env), SCAFFOLD.4 (Upstash), SCAFFOLD.3 (auth wiring), **tracker v9 sweep** |
| #40–#46 | SCAFFOLD.13-A/B (Supabase DATABASE_URL), SCAFFOLD.12 (domain), SCAFFOLD.3-followup-1 |
| #47–#52 | SCAFFOLD.15 (R2), ENGINE.6 (events helper), SCAFFOLD.17 (identity-pool), SCAFFOLD.16 |
| #53–#57 | finish-bundle-1 (observability SCAFFOLD.5/.6/.7), SCAFFOLD.18 (CI+Postgres), SCAFFOLD.8 (staging) |

The full ordered list with `headRefName` + `mergedAt` is in `repo-state.json`.

**Note on PR #52:** merged from branch `plan/scaffold-16` (not a `feat/*`
branch) — the plan branch itself carried the implementation. Its remote branch
`origin/plan/scaffold-16` is still un-deleted. Likewise `origin/plan/engine-6`
survives though ENGINE.6 merged via `feat/engine-6-events-helper-zod-schemas`
(#49).

---

## STEP 3 — File inventory

**297 tracked files.** By top-level entry (largest first):

| Entry | Files | | Entry | Files |
|---|---|---|---|---|
| `docs/` | 109 | | `.vscode/` | 2 |
| `src/` | 62 | | `.claude/` | 4 |
| `tests/` | 57 | | `.github/` | 1 |
| `drizzle/` | 17 | | `public/` | 8 |
| `scripts/` | 8 | | root config files | ~30 |

Root config/files tracked: `package.json`, `pnpm-lock.yaml`,
`pnpm-workspace.yaml`, `tsconfig.json`, `next.config.ts`, `drizzle.config.ts`,
`vitest.config.ts`, `biome.json`, `lefthook.yml`, `mise.toml`, `justfile`,
`postcss.config.mjs`, `components.json`, `vercel.json`, `proxy.ts`,
`instrumentation.ts`, `instrumentation-client.ts`, `sentry.server.config.ts`,
`sentry.edge.config.ts`, `.nvmrc`, `.gitignore`, `.editorconfig`,
`.env.example`, `CLAUDE.md`, `AGENTS.md`, `README.md`, `LICENSE`,
`SECURITY.md`, `CODE_OF_CONDUCT.md`.

`git status --porcelain` is **empty** — clean tree.

`.claude/` tracked contents are **only the 4 reviewer role briefings**:
`agents/code-reviewer.md`, `agents/db-migration-reviewer.md`,
`agents/security-auditor.md`, `agents/test-writer.md`. (Untracked, local-only:
`settings.local.json`, `scheduled_tasks.lock`.) No `skills/`, `hooks/`, or MCP
config is tracked or present — see drift signal D6.

---

## STEP 4 — Canonical doc inventory

| Doc | Lines | Version / Status | Last-touching commit |
|---|---|---|---|
| `docs/specs/SPEC.1.md` | 1335 | **1.8.0-draft** · Status Draft (→Approved on v1.0.0 lock) · body "Last updated: 2026-05-08" | `e080dab` 2026-05-27 |
| `docs/specs/SPEC.2.md` | 2702 | **v0.3.4-draft** · last absorption 2026-05-27 · lock gate PRECURSOR.4 | `e080dab` 2026-05-27 |
| `CLAUDE.md` | 267 | no header; footer "Last revised PRECURSOR.5 (May 14, 2026) … SPEC.1 v1.8.0 + SPEC.2 **v0.3-draft** + ADRs 0003–0016" | `e0ef868` 2026-05-14 |
| `AGENTS.md` | 424 | no header; footer "Last revised PRECURSOR.5 (May 2026) … SPEC.1 v1.8.0 + SPEC.2 **v0.3-draft** + ADRs 0003–0016" | `e9e1378` 2026-05-14 |
| `README.md` | 2 | stub, no header | `4f4d746` 2026-04-23 (Initial commit) |
| `LICENSE` | 679 | GNU AGPL v3 (AGPL-3.0-or-later) | `00bcef4` 2026-04-27 |

**Broader `docs/` shape (109 files, all `.md`):**

| Subtree | Files | Notes |
|---|---|---|
| `docs/logs/` | 37 | per-session close-out logs (STEP 6) |
| `docs/specs/` | 43 | `SPEC.1.md`, `SPEC.2.md` + `flows/` (41: 40 `F-*` flow specs + a `README.md`) |
| `docs/plans/` | 21 | `_template.md` + 20 task plans |
| `docs/runbooks/` | 2 | `BREAK_GLASS.md`, `dataset-release.md` |
| `docs/adr/` | 1 | `0001-license-choice.md` only (STEP 5) |
| `docs/briefs/` | 1 | `SCAFFOLD.16-technical-research-brief.md` |
| `docs/references/` | 1 | `manifold.md` |
| `docs/workflows/` | 1 | present |
| `docs/` root | 2 | `maintenance.md`, `parked.md` |

---

## STEP 5 — ADR inventory

**Exactly one ADR file exists on disk:**

| File | ID | Title | Status |
|---|---|---|---|
| `docs/adr/0001-license-choice.md` | 0001 | Use AGPL-3.0-or-later for the experiment-phase repo | **Accepted** |

No `Superseded-by` / `Supersedes` links present (only one file to link).

**Missing from sequence: `0002`–`0016` (15 numbers).** CLAUDE.md and AGENTS.md
treat "ADRs 0003–0016" as accepted, load-bearing decisions and cite individual
ones throughout (0003 route-handler carve-outs, 0004/0010 auth, 0012 design,
0013 bet transaction, 0014 no-HTTP-in-txn, 0015 idempotency, 0016 UUIDv7). Per
the known catalogue framing, the *substance* of these decisions lives in SPEC.2
§0.1 change-log entries; the standalone ADR files are queued backfill, not yet
written. **This is recorded as a file-level fact, not re-litigated.** See D2.

---

## STEP 6 — Task-log / close-out inventory

Single log directory: `docs/logs/` (37 files). Each ID maps to an introducing
commit; abbreviated below (full `intro_commit` per file in `repo-state.json`):

- **Foundation:** CHAT-0-PRECURSOR-D (`9d3ce25`), CHAT-1-MAX-MIGRATION
  (`507f9bb`), FOUND.2 (`6a04ec3`), FOUND.3 (`8c87b79`), FOUND.4 (`66400a0`),
  FOUND.5 (`dc5f6f1`), FOUND.6 (`2630855`), PRECURSOR.5 (`e0ef868`).
- **SCAFFOLD.2 sub-strata:** SCAFFOLD.2 (`1ced8c2`), .2-3A (`04f5324`),
  .2-3B (`6079452`), .2-3C (`e443260`), .2-3D (`b4fc1d7`).
- **SCAFFOLD substrate:** .1 (`61157a9`), .3 (`62cd299`),
  .3-FOLLOWUP-1 + -plan (`f546053` / `3a737c6`), .4 (`825e18b`),
  .12 + -security-auditor (`7362e46`), .13-A (`7167397`), .13-B (`2fb9091`),
  .14 (`c7936e1`), .15 + -operator-substrate (`660f193` / `8723fa5`),
  .16 + -plan-mode (`45b35e1`), .17 + -post-merge (`d5be518` / `6a6b04b`),
  .18-execute + -plan-review (`9a42b8a` / `e080dab`), .8 (`92b7c47`),
  .8-plan-mode-review (`e26d198`).
- **Bundles / engine / tracker:** SCAFFOLD-finish-bundle-1 (`e6de02a`),
  SCAFFOLD-finish-bundle-2 (`e080dab`), ENGINE.6 (`42baa8b`),
  tracker-sweep-v9 (`82bee48`).

The latest log, `SCAFFOLD.8.md`, was introduced by HEAD itself (`92b7c47`).

---

## STEP 7 — Source tree + config

**`src/` — 62 files** across these directories (full list in `repo-state.json`):

- `src/app/` — App Router: `(admin)/admin/login`, `(auth)/{onboarding,sign-in,sign-in/otp}`, `(dev)/scaffold-1-smoke`, and `api/{_smoke-error,auth/[...all],cron/r2-orphan-sweep,health,uploads/sign}`.
- `src/server/` — substrate domains only: **auth** (10 files, incl. `auth/admin`), **storage** (4), **upstash** (3), **middleware** (3), **moderation** (2), **idempotency** (2), **events** (2), **identity-pool** (1), **config** (1).
- `src/db/schema/`, `src/lib/` (+ `lib/posthog`), `src/components/` (+ `components/ui`).

**Route handlers (`route.ts`):** `api/_smoke-error`, `api/auth/[...all]`,
`api/cron/r2-orphan-sweep`, `api/health`, `api/uploads/sign`.

**`drizzle/migrations/` — 8 SQL migrations + `meta/` journal + 8 snapshots:**

```
0000_uuidv7_function        0004_seed_system_state
0001_initial_schema         0005_auth_schema_corrections
0002_events_partitioning    0006_image_uploads_extension
0003_append_only_triggers   0007_pg_cron_jobs
```

(`meta/_journal.json` entries 0–7 match the eight files exactly.)

**`supabase/` — no migrations.** Only `supabase/.branches/_current_branch` and
`supabase/.temp/cli-latest` are tracked. No `supabase/migrations/` directory and
no RLS policy files exist. See D4.

**`tests/` — 57 files**, layout *wider* than AGENTS.md §9 describes:

| Subtree | Files |
|---|---|
| `tests/server/` | 26 |
| `tests/db/` | 18 |
| `tests/integration/` | 7 |
| `tests/unit/` | 3 |
| `tests/_setup/` | 2 |
| `tests/invariants/` | **1** |
| `tests/e2e/` | **0** (empty) |

Only **one** invariant test exists: `I-APPEND-ONLY-001.resolutions-append-only.spec.ts`.
The other three canonical invariant tests named in AGENTS.md §9
(`I-ATOMICITY-001`, `I-NO-OVERDRAFT-001`, `I-SIDE-BIND-001`) are absent. See D5.

**`.claude/settings.json`:** does **not** exist. `claude_settings_model` is
therefore **`null`**. A local-only, untracked `.claude/settings.local.json`
exists but carries **no `model` field**.

**Key dependency versions** (from `package.json`):

| Package | Version | | Package | Version |
|---|---|---|---|---|
| next | `16.2.4` | | better-auth | `1.6.11` |
| react / react-dom | `19.2.4` | | drizzle-orm | `^0.45.0` |
| typescript | `^5` | | drizzle-kit | `^0.30.0` (dev) |
| tailwindcss | `^4` | | drizzle-zod | `^0.7.0` |
| @biomejs/biome | `2.4.13` | | zod | `3.25.76` |
| @sentry/nextjs | `10.53.1` | | openai | `6.39.0` |
| @upstash/ratelimit | `2.0.8` | | @upstash/redis | `1.38.0` |
| posthog-js | `1.376.0` | | posthog-node | `5.35.1` |
| @aws-sdk/client-s3 | `3.1045.0` | | resend | `6.12.3` |
| @vercel/functions | `3.6.0` | | vitest | `^3.0.0` (dev) |

Stack matches AGENTS.md §1 (Next 16 / Postgres-Supabase / Drizzle 0.45+ / Biome
v2 / Upstash / R2-via-AWS-SDK / Better Auth / Resend / Sentry + PostHog).

---

## Drift signals I noticed

> Flagged for the orchestrator to reconcile. **Not fixed.** Ordered by
> reconciliation importance. "D2" etc. are local handles for this report.

### D1 — Tracker lag is the headline reconciliation gap (HIGH)
The last tracker-related PR landed in-repo is **#39 `chore(tracker): v8 → v9
sweep` (merged 2026-05-16)**. Since then **18 PRs merged** (#40–#57): SCAFFOLD.13-A/B,
.12, .3-followup-1, .15, ENGINE.6, .17, .16, finish-bundle-1 (SCAFFOLD.5/.6/.7),
.18 (CI+Postgres+migration-apply), and .8 (staging env). Per the operating model
the tracker is **operator-maintained external HTML in web-Claude project
knowledge** — not in this repo — so if the orchestrator's tracker reflects ~v9
state, this entire body of work (R2 storage, identity-pool seed, events helper,
observability stack, staging environment, expanded CI) may be unreflected.
*Cannot be verified from the repo side; flagging the candidate gap with PR
citations.*

### D2 — ADR files 0002–0016 absent on disk (HIGH — but known/queued)
Only `docs/adr/0001-license-choice.md` exists. CLAUDE.md (§1, §2, footer),
AGENTS.md (footer, §6), and SPEC.2 cite "ADRs 0003–0016" as accepted decisions
and reference 0003/0004/0010/0012/0013/0014/0015/0016 as load-bearing. Consistent
with the known framing (substance in SPEC.2 §0.1 change-log; file backfill is
queued maintenance). Recording the file-level absence; not re-litigating.

### D3 — SPEC.2 version drift between the spec and the contract docs (HIGH)
`SPEC.2.md` self-identifies as **v0.3.4-draft** (last absorption 2026-05-27),
but **CLAUDE.md** (footer, line 267) and **AGENTS.md** (footer, line 424) both
cite **"SPEC.2 v0.3-draft"**. Four absorption increments (.1→.4) have landed in
the spec without the contract-doc footers being bumped.

### D4 — `supabase/migrations/` referenced but does not exist (MEDIUM)
AGENTS.md §6 ("Supabase RLS policies live in `supabase/migrations/`") and
CLAUDE.md §1 (critical paths list `supabase/migrations/`) point at a directory
that has no files. Only `.branches/` + `.temp/` CLI scratch is tracked under
`supabase/`. No RLS policy migrations exist yet.

### D5 — AGENTS.md §6 names a migration filename that doesn't match reality (MEDIUM)
AGENTS.md §6 states the userspace `uuidv7()` function ships in
`drizzle/migrations/0001_uuidv7.sql`. On disk the function is in
**`0000_uuidv7_function.sql`**, and `0001` is **`0001_initial_schema.sql`**.
(CLAUDE.md §2 uses a generic `<NNNN>_append_only_triggers.sql`, which is
concretely `0003_append_only_triggers.sql` — generic form, so not wrong.)

### D6 — `.claude/` skills + hooks named in the contract are not in the repo (MEDIUM)
CLAUDE.md §6 names hooks (`block-destructive.sh`, `block-main-commits.sh`,
`format-and-typecheck.sh`, `session-start.sh`) and an auto-discovered
`.claude/skills/<name>/SKILL.md` convention; AGENTS.md describes `.claude/` as
"subagents, skills, hooks, MCP". On disk `.claude/` tracks **only** the 4
`agents/*.md` reviewer briefings — no `skills/`, no `hooks/`, no MCP config.
(They may live in untracked user-level settings; from the repo they are absent.)

### D7 — Invariant + critical-path code dirs not yet built (MEDIUM — expected for phase)
Only `I-APPEND-ONLY-001` of the four canonical invariant tests (AGENTS.md §9)
exists; INV-1/INV-2/INV-3 tests await their business logic. Correspondingly,
the CLAUDE.md §1 critical-path dirs `src/server/{bets,comments,dharma,resolution}`
**do not exist yet** — present `src/server/` domains are substrate only.
Minor naming note: CLAUDE.md §1/§3 and AGENTS.md "ask first" reference
`src/server/identity/`, but the actual directory is **`src/server/identity-pool/`**.
This reads as current build state (mid-SCAFFOLD, pre-ENGINE business logic) more
than contradiction — flagged so the orchestrator doesn't assume those paths are
populated.

### D8 — `tests/` layout is wider than AGENTS.md §9 documents (LOW)
AGENTS.md §9 describes `tests/{unit,integration,e2e,invariants}`. Actual tree
adds `tests/server/` (26) and `tests/db/` (18) and `tests/_setup/` (2), and
`tests/e2e/` is empty (0 files). 44 of 57 test files live in undocumented
subtrees.

### D9 — README.md is a 2-line stub (LOW)
Untouched since the Initial commit (`4f4d746`, 2026-04-23) despite 57 merged PRs.

### D10 — Stale branches (LOW / housekeeping)
Remote `origin/plan/engine-6` and `origin/plan/scaffold-16` persist post-merge;
~22 stale local branches for merged PRs were never pruned.

### D11 — SPEC.1 internal date vs git mtime (LOW)
`SPEC.1.md` body says "Last updated: 2026-05-08" but the file was last modified
by `e080dab` (2026-05-27, the CI+Postgres PR) without a header-date bump.

### D12 — Mixed dependency pinning (LOW / observation)
Newer vendor deps are literal-pinned per the pinning discipline (next, react,
better-auth, @sentry/nextjs, openai, zod, @upstash/*, posthog-*, @aws-sdk/*,
@vercel/functions, resend, @biomejs/biome, lefthook, canonicalize), but several
deps still use caret ranges (`drizzle-orm ^0.45.0`, `drizzle-zod ^0.7.0`,
`drizzle-kit ^0.30.0`, `radix-ui ^1.4.3`, `uuid ^11.0.0`, `postgres ^3.4.5`,
`vitest ^3.0.0`, `tailwindcss ^4`, `shadcn ^4.7.0`, `lucide-react ^1.14.0`,
`tailwind-merge ^3.6.0`, `class-variance-authority ^0.7.1`, `clsx ^2.1.1`).
Observation only — the literal-pin discipline targets *new* deps.

---

*Companion machine-readable snapshot: `docs/sync/repo-state.json`. Both files
written read-only this session; nothing else in the repo was modified, staged,
committed, pushed, or deleted, and the database was not touched.*

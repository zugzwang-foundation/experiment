# D5 / D6 — session log (prod-DB drift remediation + first gated verify-before-serve promote)

**Stratum:** D5a (prod-DB migrate `0006→0018`) + D6 (first gated migrate-before-serve promote) · **Ratified plan:** `docs/plans/D5.md` (PR #175, `41b2190`) · **Banner-finalize PR:** #176 (`«PROMOTE-SHA»` = `61abb04`) · **Log branch:** `chore/d5-d6-log` · **State:** EXECUTED — prod migrated + promoted + verified live · **Date:** 2026-06-28

## What landed (files + PR#)
- **D5a — prod-DB migrate (operational; no code/PR):** `doppler run --config prd -- pnpm db:migrate:prod` applied **13 migrations `0006→0018`** to the live `ba484f0` prod DB. Result `"Applied 13 migration(s); 19 total in journal."`; drift `0005`/6 → **IN SYNC 19/19**; live `/api/health` flipped `migrations: drift → ok`. *(The `42P06` "schema drizzle already exists" + `42P07` "__drizzle_migrations already exists" lines are harmless `IF NOT EXISTS` **NOTICEs** — the bookkeeping objects pre-existed from `0000–0005`, not errors.)*
- **D6 — first gated promote (PR #176, squash-merged — `«PROMOTE-SHA»` = `61abb0485e5ec7b251426932704aabd09f367abf`):**
  - `docs/runbooks/deploy-pipeline.md` §3 banner flip `DRAFT → ACTIVE` (the merge that produced `«PROMOTE-SHA»`). Merged → **staged** build `experiment-hofhix5my-…vercel.app` (auto-assign OFF held: live alias stayed `ba484f0` until promote) → GATE 2 (staged `/api/health`: `migrations:ok`, `canary==«PROMOTE-SHA»`, 200) → **promote** → live flip `ba484f0 → 61abb04`.
- **This log PR (`chore/d5-d6-log`):**
  - `docs/logs/D5.md` — **NEW** (this file).
  - `docs/runbooks/deploy-pipeline.md` §3 finalize (Q2 mechanic (a), post-promote): banner `«PROMOTE-SHA»`→`61abb04` + date `2026-06-27 → 2026-06-28 UTC` (actual promote); §3 step-5 control bracket resolved to the confirmed `vercel promote <staged-url> --scope <team-slug>`; precondition tense-sweep + footer status line.
- **Docs-only footprint** — no `src/`, no schema/migration file, no workflow, no new ADR. (The migrate applied the already-committed `0006–0018`; it authored nothing.)

## Decisions made
- **Both gates held; verify-then-write throughout.** GATE 1 = empty-prod predicate (six destructive-target STOP tables all `0`, exit 0) re-run at apply-time; GATE 2 = staged-build per-hash `/api/health` `migrations:ok` + `canary==«PROMOTE-SHA»`. No doc was written until the live flip was confirmed (STEP 2).
- **Migrate exit code NOT trusted (drizzle-orm #5769).** Success was taken from per-hash `/api/health` (`db:check-drift` IN SYNC as the second signal), per ADR-0024 item 6.
- **Promote control = `vercel promote <staged-url> --scope zugzwang-worlds-projects` (CLI).** The bare command errored `Deployment belongs to a different team`; `--scope` fixed it. Baked into §3 step-5 (was the `[VERIFY THE EXACT CONTROL AT D5 EXECUTION]` bracket).
- **Sequencing per plan Q1/Q3:** A1 migrate-first (D5a on its own recovery boundary), then D6 as separate gated relays (pre-flight → migrate → pre-GATE-2 → promote), each behind an explicit operator go (the single human checkpoint, ADR-0024 driver 3).
- **No subagents / no `db-migration-reviewer`** — operational + doc-only; the migration set was reviewed read-only against this apply sequence in the ratified plan (no `src/` business logic, no new schema). CLAUDE.md §5.11.

## Surprises caught + handled in-session (§5.10)
- **`vercel promote` "Deployment belongs to a different team" (caught, fixed, recorded).** The runbook's draft control `vercel promote <staged-url>` errored on team scope; retried with `--scope zugzwang-worlds-projects` → `Success!`. The live alias was re-confirmed `ba484f0` *before* the retry (no partial swap). Resolved the §3 step-5 bracket to the working invocation incl. the `--scope` requirement.
- **Banner date drift `2026-06-27 → 2026-06-28` (flagged pre-promote, reconciled post-promote).** The ratified banner prose said "First exercised 2026-06-27 UTC"; the actual promote landed **2026-06-28 04:44:46 UTC** (clock rolled between relays). Per relay instruction the literal `2026-06-27` was kept verbatim in the banner-flip commit (#176, web-owned prose) and reconciled to the real date in this post-promote finalize.
- **Cosmetic — `git log --show-signature` printed `cannot run gpg` on the GitHub squash commit.** Local `gpg` binary absent; commits are SSH-signed and branch protection accepted the merge — signing is satisfied. Not a defect.

## Open questions
- None blocking. D5/D6 is exercised-once + documented; the deploy-pipeline drift class is closed for the Experiment.

## Next session starts at (exact next action)
- **Close-out cleanup** (see Close-out items below) — branch deletes, then the next combined SYNC sweep (AGENTS.md migration-head `0016→0018`, the `seed-staging.ts --config staging` string, ADR-0024 out-of-tracker, the `staging-migrate.yml` "inert until D3" line). No code stratum is queued by D5/D6.

## Context to preserve
- **`«PROMOTE-SHA»` = `61abb0485e5ec7b251426932704aabd09f367abf`** (PR #176 squash on `main`) — the **first** production promote via the gated path.
- **Prod DB @ journal head `0018` (19/19, IN SYNC).** Live `/api/health` (`zugzwangworld.com`) = `{status:ok, env:prod, db:ok, migrations:ok, canary:61abb04…}`.
- **Restore point at migrate time:** Supabase prod daily physical backup **2026-06-26 22:44 UTC** + PITR active (Pro). Prod was pre-launch-empty (six STOP tables `0`; only residue `events=1`, `system_state=1`) → re-init is the true fallback; the migrate was fix-forward per-migration-tx.
- **Promote control:** `vercel promote <staged-url> --scope zugzwang-worlds-projects` (CLI) — `--scope` is **required**; dashboard "Promote to Production" is the equivalent.
- **pg_cron jobs (`0007`/`0011`) are now ACTIVE on prod** (applied verbatim; harmless on empty data — watch Sentry over the next cycle).
- **Gate proof:** during D6 the live alias served `ba484f0` continuously while the staged `61abb04` build was Ready and per-hash-`ok` — `autoAssignCustomDomains` OFF works as the migrate-before-serve gate.

## Promotion note (ADR-0024 item 10)
| Field | Value |
|---|---|
| **SHA** | `61abb0485e5ec7b251426932704aabd09f367abf` (PR #176) |
| **Who** | Operator: Hrishikesh (explicit go at each gate); executed via Claude Code |
| **When (UTC)** | 2026-06-28 04:44:46 UTC |
| **Per-hash `/api/health`** | live `zugzwangworld.com`: `canary:61abb04…`, `migrations:ok`, `db:ok`, `status:ok`, HTTP 200 |
| **Control** | `vercel promote <staged-url> --scope zugzwang-worlds-projects` (CLI; `Success! … dpl_5nDYfD2PyK9TE1mYoFLnyCYrdSor`) |

## Close-out items (to action at close-out / next sweep — NOT this PR)
- Delete local branches `chore/d6-promote-finalize` + `chore/d5-d6-log` (verify remote auto-delete; inconsistent).
- DB-password rotation (prod + staging — both pre-launch-empty).
- **Next SYNC sweep:** AGENTS.md migration-head `0016 → 0018`; `scripts/seed-staging.ts` `--config staging → stg`; ADR-0024 out-of-tracker fold; the `staging-migrate.yml` "inert until D3" framing.
- `0007`/`0011` pg_cron — a Sentry glance over the next cycle.

## Time
2026-06-28. Active CC execution across four gated relays (pre-flight → D5a migrate → D6 pre-GATE-2 → D6 promote/log); each relay gated on an explicit operator go.

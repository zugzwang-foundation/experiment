# ENGINE.7 — session log

> **Stratum:** ENGINE.7 — the bet-transaction primitive: the generic **W-1 SERIALIZABLE wrapper** (`src/server/bets/transaction.ts` + `errors.ts`) every bet flow (ENGINE.8) invokes. The most write-contended path in the system; Ultrathink-mandatory.
> **Entry:** plan session (read-only sync-gated recon → SURFACE → founder rulings (a)–(f) + S1–S5 + lock-predicate → draft → web CP review (2 required fixes + 2 optional improvements folded) → WEB GREEN + founder ratification → docs land). Execute session appends below in a fresh CC session + fresh web chat.

---

## Plan session — 2026-06-09

**What landed.**
- `docs/plans/ENGINE.7.md` — the founder-ratified implementation plan (rulings (a)–(f) + S1–S5 dispositions + the two CP-required fixes + two optional improvements folded) — via **PR #93**, squash-merged to `main` at **`7dc22d84f3a39bd0cf69f791fe65675c2d5aff40`** (`plan(engine-7): W-1 bet-transaction wrapper — reviewed plan (#93)`), **docs-only, no reviewer cascade** (ENGINE.3/4/5/11 precedent). **292 lines, md5 `69684b013f1fad62e219e0f6d2f2cde8`**. Pre-squash branch commit: `6bb8cd5` (ephemeral, signed, clean trailer).
- No `src/`, test, or schema/migration changes — plan-only. Plan branch `plan/engine-7` merged; this log ships on `chore/engine-7-log`.

**Decisions made — founder rulings (a)–(f) (ratified).**
- **(a) Market-state gate = WRAPPER-level, COARSE.** After the pool lock, a PLAIN intra-tx `SELECT markets.status` — **no row lock**, must NOT enter the lock-order spine (SERIALIZABLE/SSI catches a concurrent flip as 40001→retry; a `markets` row lock here would risk deadlock vs W-3's `markets→…` order). `Open` → run callback; non-Open → throw `MarketNotOpenError(status)` (a product error, NOT retried). ENGINE.8 maps status→§15 code (`Closed→market_closed_at`, `Resolving→in_flight_timeout` coarse reject-all, others→`market_not_open`). Fine F-BET-6 window DEFERRED (S1).
- **(b) `insertEvent` = CALLBACK calls it; wrapper events-UNAWARE.** The tracker's "appends events via ENGINE.6" is the callback's `insertEvent(tx,…)` inside the wrapper's tx (`events` terminal in the chain).
- **(c) CPMM compute = CALLBACK** (reads locked reserves → `computeBuy`/`computeSell` → writes). Wrapper generic.
- **(d) `I-ATOMICITY-001` lands in ENGINE.7, WRAPPER-level** (SPEC.2 §14.4). Canonical assertion = a representative callback writes `pools`+`positions`+`dharma_ledger` then throws **MID-SPINE** (before `comments`/`bets`/`events`) → assert **zero rows across ALL tables** (full-chain rollback). No `I-ATOMICITY-002`; ENGINE.8 full-flow atomicity is acceptance-level under `tests/server/bets/`.
- **(e) Concurrency-test split = 7/2.** ENGINE.7 owns 7 in `tests/server/bets/concurrency.test.ts` (serializable-isolation-enforced, pool-row-lock-acquired, no-key-update-allows-fk-share, canonical-lock-order [4-table], retry-on-40001, retry-on-40P01, retry-budget-exhausted-emits-alarm-3). The 2 handler-level (idempotency-replay-skips-moderation-and-txn, moderation-outside-transaction) → ENGINE.8.
- **(f) Events-idempotency = PROPERTY TEST, not a named invariant** (per §14.1 — no `I-EVENT-IDEMPOTENT-001`). `bets::concurrency-retry-events-idempotent`: force 40001 → exactly one `events` row + `created_at` stable across the retry. **Retry-purity contract recorded first-class:** the wrapper re-runs the WHOLE callback on retry, so callbacks MUST generate all IDs (`event_id`, `bet_id`, `comment_id`) **and `metadata.idempotency_key`** at handler-entry and close over them — never per-attempt — else `ON CONFLICT (event_id, created_at)` never dedupes.

**Decisions made — surprise dispositions S1–S5 + lock predicate (ratified).**
- **S1** — `markets.resolving_at` absent; coarse gate only; the fine F-BET-6 in-flight window is a carry-forward (needs the `resolving_at` column [schema/migration, ENGINE.9-or-dedicated] + `IN_FLIGHT_BET_TIMEOUT_SEC` [HARDEN.6]). Record the ADR-0013 §7 ↔ schema drift when the column lands.
- **S2** — `error_type` for `error_bet_serialization_exhausted` is **`unavailable`** (§15.2 canonical), not the §9-prose `temporary_unavailable`. ENGINE.7 mints the class + code string; ENGINE.8 wires the envelope.
- **S3** — `comments.stake_at_post_time` is NOT NULL (vestigial, ADR-0009 input superseded by ADR-0017); any comment INSERT (incl. the I-ATOMICITY-001 callback) MUST populate it. `comments.bet_id` nullable — do NOT reconcile (DEBATE.8/9).
- **S4 — RESOLVED as a settled design** (not a deferral): the pool-row `FOR NO KEY UPDATE` lock **is** the position-write serialization backstop; the bet-flow callback does **not** pass `previousQuantity` to `upsertPositionDelta`; chaining is intentionally unwired in v1.
- **S5** — ENGINE.7's error classes are **product** errors mapped to §15 codes (carry the observed-status discriminant), distinct from the `cpmm/dharma/markets` caller-bug sentinels — same module-local pattern, different semantics.
- **Lock predicate** = `pools WHERE market_id = $marketId FOR NO KEY UPDATE` (NOT `pools.id = marketId`; ADR-0013 §1 snippet conflated pool PK with market id).

**Decisions made — web CP review (2 required fixes + 2 optional improvements, folded → WEB GREEN).**
- **Required #1 — F-BET-4 Dharma balance read placed in the callback contract.** ADR-0013 step 7 reads both positions (F-BET-10) AND the user's balance (F-BET-4) in the locked snapshot; the draft omitted the balance read. Added: an in-snapshot balance read **before** the `dharma_ledger` debit (read source = latest `dharma_ledger.balance_after`; canonical shape at `persist.ts:24-35`), friendly `insufficient_dharma` 400 (ENGINE.8) vs authoritative `DharmaOverdraftError` + `CHECK (balance_after >= 0)`. Surfaced into the spine, the dependency table, and the INV-2 "composed, not owned" line. **Accuracy finding:** ENGINE.5 exposes **no public balance-read** today (only the private `readLatestBalance`) → carry-forward seam for ENGINE.8.
- **Required #2 — `I-ATOMICITY-001` made MID-SPINE** (not throw-before-`events`): canonical assertion forces the throw partway (after `dharma_ledger`, before `bets`/`events`) for a full-chain-rollback proof; throw-before-`events` kept as an edge variant. Test plan + Failure modes + Edge cases updated.
- **Optional #3 (folded)** — deadlock-freedom made **global**: the coarse unlocked read is the bet flow's only `markets` touch (no `markets` row lock anywhere) → the 4-table spine never shares a row-lock ordering surface with W-3's `markets→…`.
- **Optional #4 (folded)** — retry-purity **second governed field**: `events.metadata.idempotency_key` (SPEC.2 §3.7) is likewise a handler-entry value closed over under the same purity discipline — the field tying storage-layer dedupe to ENGINE.8's API-boundary idempotency.

**Probes / verifications (recorded).**
- **SERIALIZABLE support — CONFIRMED.** `db.transaction(fn, { isolationLevel: "serializable" })` already used at `auth/admin/login.ts:128` (Drizzle postgres-js).
- **`.for('no key update')` — NO in-repo precedent** (`tos-accept.ts:118` notes its absence). ENGINE.7 is the first call site → **execute must verify the exact Drizzle core-builder API form against postgres-js** before relying on it (carry-forward to execute step 3).
- **Local Postgres :54322 — assumed DOWN** (whole-suite-needs-Postgres convention); DB-backed concurrency suites are CI-gated. The deterministic 40001/40P01 + `FOR KEY SHARE`-non-blocking harness is the schedule long-pole.

**Open questions.** None — (a)–(f) all ruled; S1–S5 dispositioned; the two required fixes + two optional improvements folded and WEB-GREEN-verified.

**Carry-forwards minted (no `#`, to avoid autolink).** carry-forward 1 (fine F-BET-6 in-flight window → needs `markets.resolving_at` column [ENGINE.9-or-dedicated] + `IN_FLIGHT_BET_TIMEOUT_SEC` [HARDEN.6]); carry-forward 2 (F-BET-4 balance-read seam → ENGINE.5 has no public `readBalance(tx,userId)`; ENGINE.8 prereq — export it or thread `previousBalance`); carry-forward 3 (`error_type` envelope wiring `error_bet_serialization_exhausted`→`unavailable` + `MarketNotOpenError`→§15 map → ENGINE.8 / error-envelope work); carry-forward 4 (`.for('no key update')` API verification against postgres-js → execute step 3); carry-forward 5 (`statement_timeout ~1000ms` in-tx + `idle_in_transaction_session_timeout ~30s` pool-level → execute, recommendation not tuned constant).

**Next session starts at.** **ENGINE.7 EXECUTE in a FRESH CC session + fresh web chat** (§5.8 plan/execute split — never this session). **Step 1** = sync + branch **`feat/engine-7-bet-tx`** off `main` (`7dc22d8`+). **Step 2** = `@test-writer` RED authors the suite (`tests/invariants/I-ATOMICITY-001.bet-comment-atomic.spec.ts` mid-spine-abort + the 7 `tests/server/bets/concurrency.test.ts` cases + `bets::concurrency-retry-events-idempotent`; DB-backed → CI-RED locally). **Step 3** = implement `transaction.ts` + `errors.ts` (`server-only`; constants co-located; coarse gate; retry; alarm-3; retry-purity docstring; verify `.for('no key update')`). Pass `@docs/plans/ENGINE.7.md` to the cascade. **FULL ritual, no narrowing:** `@test-writer` RED → implement → `@code-reviewer` → full-scope `@security-auditor` → §5.10 audit; **no `@db-migration-reviewer`** (no schema/migration); **no soak**.

**Context to preserve.**
- **Canonical ratified-plan SHA:** `7dc22d84f3a39bd0cf69f791fe65675c2d5aff40` (squash of PR #93); plan md5 `69684b013f1fad62e219e0f6d2f2cde8` (292 lines).
- **Scope (frozen):** ENGINE.7 = the generic W-1 wrapper (`transaction.ts` + `errors.ts`) + 2 test files only. 4-table lock order `pools → positions → dharma_ledger → events` (friendly-fire struck). Reply-as-bet: W-1 = every bet (F-BET-1/2/3 + F-COMMENT-1/2/3); W-2 retired. `FOR NO KEY UPDATE` on `pools.market_id`. Coarse market-state gate (Open vs not-Open; fine window deferred). insertEvent + CPMM in the callback; wrapper generic + retry-purity contract. `I-ATOMICITY-001` minted wrapper-level (mid-spine); `I-SIDE-BIND-001` → ENGINE.8.
- **Wrapper surface (planned):** `runBetTransaction({ marketId, flow }, callback)` — opens SERIALIZABLE tx; locks `pools WHERE market_id` `FOR NO KEY UPDATE`; coarse `markets.status` gate (plain SELECT); invokes `callback({ tx, pool })`; full-jitter retry `BACKOFF_BASES_MS=[50,100,200]`, 3-retry budget, `RETRYABLE_SQLSTATES={40001,40P01}` (co-located, not tunables); Sentry `addBreadcrumb`/attempt + `captureMessage` alarm-3 on exhaustion; `BetSerializationExhaustedError` (503, `Retry-After:1`). Errors: `BetSerializationExhaustedError`, `MarketNotOpenError(status)`. `server-only`, no barrel.
- **Callback spine (ENGINE.8 builds):** READS in the locked snapshot — positions(single-side, F-BET-10) + dharma balance(F-BET-4) + CPMM compute → WRITES — `positions`(upsertPositionDelta) → `dharma_ledger`(appendLedgerRow, tag `bet_stake`) → (`comments` INSERT, set `stake_at_post_time` + `side_at_post_time`) → (`bets` INSERT, `comment_id`) → `events`(insertEvent, TERMINAL) → `pools` UPDATE reserves. Verified dep signatures: `insertEvent(tx,input)` (`events/insert.ts:95`), `appendLedgerRow(tx,{userId,amount,entryType,betId?,previousBalance?})` (`dharma/persist.ts:54`), `upsertPositionDelta(tx,{userId,marketId,side,shareDelta,previousQuantity?})` (`positions/persist.ts:46`, NO previousQuantity), `computeBuy`/`computeSell` (`cpmm/calculate.ts:57,109`, lowercase side).
- **Boundary:** ENGINE.7 = `transaction.ts` + `errors.ts` + 2 test files. ENGINE.8 = `place.ts`/`sell.ts` + idempotency lookup + two bet floors + friendly single-side/min-bet codes + `I-SIDE-BIND-001` + the 2 handler concurrency tests. No schema/migration → no `@db-migration-reviewer`.
- **Drifts recorded:** S1 (`resolving_at` absent), S2 (`temporary_unavailable`→`unavailable`), S3 (`stake_at_post_time` NOT NULL vestigial), ADR-0013 §1 lock-predicate snippet (`id`→`market_id`), tracker "canonical lock order (ADR-0013)" → SPEC.2 §9 4-table order.
- **Environment quirk:** `/tmp` is clobbered between turns on this machine — md5-gate `/tmp` artifacts at use-time; the durable copy is the merged plan/log under `docs/`.

**Time.** 2026-06-09 (IST). Single-session plan/review loop: sync-gated read-only recon (HEAD `2667140`) → SURFACE → founder rulings (a)–(f) + S1–S5 + lock-predicate → in-chat draft (281 lines) → web CP review (2 required fixes + 2 optional improvements; 292 lines) → **WEB GREEN + founder ratification** → docs-only PR #93 (pre-squash `6bb8cd5`) → founder squash-merge (`7dc22d8`) → post-merge ff-only sync (END ON MAIN) → this log (separate `chore/engine-7-log` PR).

---

## Execute session — 2026-06-09

**What landed.**
- `src/server/bets/transaction.ts` (`runBetTransaction` — the generic W-1 wrapper) + `src/server/bets/errors.ts` (`BetSerializationExhaustedError`, `MarketNotOpenError`) via **PR #95**, squash-merged to `main` at **`37dae5a`** (`feat(bets): ENGINE.7 — W-1 bet-transaction wrapper (transaction.ts + errors.ts) (#95)`). Pre-squash branch commits (ephemeral, both signed): `84e8970` (RED) → `1858384` (green, incl. the `await callback` fold-in).
- Tests (RED-first via `@test-writer`): `tests/invariants/I-ATOMICITY-001.bet-comment-atomic.spec.ts` (3 — mid-spine abort + happy-path + throw-before-events), `tests/server/bets/concurrency.test.ts` (12 = the 7 concurrency + 1 events-idempotency property + the 4 coarse-market-state-gate added at web request), `tests/unit/bets/errors.test.ts` (4 — additive, the DB-independent error-class slice).
- **No schema, migration, or `src/db/` touch** → no `@db-migration-reviewer` (per plan).
- **CI receipt (the only real-Postgres run; local :54322 down):** run `27206540432` **success**, `postgres:17` service + `drizzle-kit migrate` applied — `concurrency.test.ts` **12/12**, `I-ATOMICITY-001` **3/3**, `errors.test.ts` **4/4**; whole suite **601 passed | 3 skipped | 5 todo, 0 failed**.

**Decisions made (execute).**
- **`.for('no key update')` — VERIFIED** against drizzle-orm 0.45 (carry-forward 4 RESOLVED): `LockStrength` union includes `'no key update'` (`pg-core/query-builders/select.types.d.ts:60`); the dialect renders ` for no key update` (`pg-core/dialect.js:290`). Relational query API has no `.for()` → the **core** builder is the call site. Predicate `eq(pools.marketId, marketId)` (UNIQUE FK → ≤1 row).
- **SQLSTATE detector reads `.cause?.code ?? .code`** — Drizzle 0.45 wraps query-builder driver errors in `DrizzleQueryError` with the SQLSTATE on `.cause`; raw `tx.execute`/COMMIT failures carry `.code` top-level (the `positions/persist.ts:130` `isSingleSideViolation` precedent). A top-level-only read would miss in-callback 40001s.
- **`statement_timeout` (1000ms) + `idle_in_transaction_session_timeout` (30000ms) via `SET LOCAL`** inside the tx (carry-forward 5 RESOLVED): kept in-module — `src/db/index.ts` is out of ENGINE.7's scope; `sql.raw` (SET rejects bind params; constant, non-user values only).
- **Error-class shape (web-approved):** `BetSerializationExhaustedError` — instance `.sqlstate`/`.flow`; class-level statics `httpStatus=503`, `retryAfterSeconds=1`, `code="error_bet_serialization_exhausted"`, `errorType="unavailable"` (S2). `MarketNotOpenError(status)` carries the observed `status` only (status→§15 map stays ENGINE.8's). `BetFlow` lives in `transaction.ts`; `errors.ts` `import type`s it (erased — only runtime edge is `transaction.ts → errors.ts`, no cycle).
- **Coarse-market-state-gate test (4) ADDED at web request** — the wrapper's one responsibility the plan under-specified a behavioral test for: Open→callback invoked; Closed/Resolving→`MarketNotOpenError` carrying the observed status, callback skipped, zero rows; gate rejection not retried (callback `attempts===0` + Sentry breadcrumb/`captureMessage` **not** called). Folded with the implementation in one pass (web-approved, asserts fully specified).
- **`tests/unit/bets/errors.test.ts` additive — RATIFIED** (web): the plan's "`tests/unit/` = DB-independent proxy"; the one ENGINE.7 slice with a real local RED→GREEN receipt. Optional dedup applied: class-static asserts live here; the concurrency exhaustion test asserts only wrapper BEHAVIOR (`instanceof` + carried `sqlstate`/`flow`).
- **`return await callback({tx, pool})`** (web-required fold-in) — correct as a bare return (`db.transaction` awaits the arrow), but `await` preserves the async stack frame for Sentry attribution and is safe against a future `try/finally`.

**Surprises caught + fixed in-session (§5.10 / reviewer cascade).**
- **My own typing regression — caught by the full typecheck, reverted.** In the RED surface I claimed `tx.execute<{…}>` was a TS error and the array form `<{…}[]>` was the fix. **WRONG:** the effective signature is `execute<TRow extends Record<string, unknown>>(): Promise<TRow[]>` (`pg-core/db.d.ts:280`) — TRow is the ROW, the result is the array — so the `@test-writer`'s original non-array form was correct. My array edit was a regression that `just typecheck` flagged (TS2344 + TS2339); reverted to the original. Net change to that line from RED = zero. The "verify-don't-trust" gate (running tsc, not trusting my own reasoning) caught it.
- **`@code-reviewer` LOW (docstring) — folded.** The `idle_in_transaction` docstring overstated coverage (it only takes effect after the first statement SETs it; the first lock-acquire is bounded by `statement_timeout`). Tightened the wording.
- **`@security-auditor` LOW (caller contract) — folded.** Added a 2nd retry-purity obligation to the docstring: a callback must not catch/re-wrap a driver `40001/40P01` without preserving `.code`/`.cause.code`, else a genuine serialization failure is invisible to the retry filter (fail-safe direction — a lost retry, never a silent commit).
- **`@security-auditor` SURPRISE (out of scope) → carry-forward 6** (see below). Recorded in `claude-progress.md` and here; NOT auto-resolved.

**Reviewer cascade + audit.** `@test-writer` RED → implement → `@code-reviewer` (0 CRITICAL/HIGH/MEDIUM, 1 LOW folded) → `@security-auditor` (0 FAIL, no INV-1/2/3/4 gap, all 8 threat areas SAFE — lock-bypass, gate-race, retry-purity double-write, retry-filter abuse, fail-posture inversion, INV-2 seam, error-leak, §3 HTTP-in-tx; 1 LOW folded + 1 SURPRISE) → §5.10 self-audit (clean, every wrapper-contract item PASS, no FAIL). Both docstring LOWs are comment-only (no behaviour change; `just verify` re-run green after).

**Open questions.** None.

**Carry-forwards.**
- **RESOLVED at execute:** carry-forward 4 (`.for('no key update')` API — verified against drizzle 0.45); carry-forward 5 (`statement_timeout` + `idle_in_transaction_session_timeout` — landed as in-tx `SET LOCAL`, `src/db/` untouched).
- **NEW — carry-forward 6 (HARDEN.\*, durable record):** a `statement_timeout` abort raises SQLSTATE **`57014`** (`query_canceled`), which is NOT in `RETRYABLE_SQLSTATES = {40001, 40P01}` — so under sustained lock-starvation a timeout-aborted bet **bubbles hard to ENGINE.8 rather than retrying or firing alarm-3** (`bet_serialization_exhausted`, SPEC.2 §17.2 row 3). Fail-direction is safe (the tx aborts, the pool lock releases; no silent commit), and fail-fast is arguably correct — but alarm-3 under-fires in the exact lock-starvation scenario it is meant to detect (operators see `57014`s, not the named alarm). The **`57014`-under-the-pool-lock → alarm-3 mapping**, tuned together with `STATEMENT_TIMEOUT_MS` and the backoff ladder (the plan flags these as "recommendations, not tuned constants"), is a **HARDEN.\*** observability call. Also in `claude-progress.md`.
- **STILL OPEN (unchanged):** carry-forward 1 (fine F-BET-6 in-flight window → `markets.resolving_at` column [ENGINE.9-or-dedicated] + `IN_FLIGHT_BET_TIMEOUT_SEC` [HARDEN.6]; record the ADR-0013 §7 ↔ schema drift when the column lands); carry-forward 2 (F-BET-4 balance-read seam → ENGINE.5 has no public `readBalance(tx, userId)`; ENGINE.8 prereq — export it or thread `previousBalance`); carry-forward 3 (`error_type` envelope wiring `error_bet_serialization_exhausted`→`unavailable` + `MarketNotOpenError`→§15 code map → ENGINE.8 / error-envelope work).

**Next session starts at.** **ENGINE.8 in a FRESH CC session + fresh web chat** (a new plan/execute cycle — NOT this session). Scope: `src/server/bets/place.ts` + `sell.ts` (the per-flow callbacks the wrapper invokes) + the idempotency-key **lookup** site + the two bet floors (ADR-0018) + the friendly single-side/min-bet/`insufficient_dharma` §15 codes + `I-SIDE-BIND-001` (INV-3, callback-level) + the 2 handler concurrency tests (`idempotency-replay-skips-moderation-and-txn`, `moderation-outside-transaction`). ENGINE.8 inherits the defined seams: carry-forward 2 (balance read) + carry-forward 3 (error envelope). **Do NOT start ENGINE.8 in this chat.**

**Context to preserve.**
- **Canonical execute SHA:** `37dae5a` (squash of PR #95). Plan SHA unchanged: `7dc22d8` (PR #93).
- **Wrapper surface as BUILT:** `runBetTransaction<T>({ marketId, flow: BetFlow }, callback: ({ tx, pool: LockedPool }) => Promise<T>): Promise<T>` — SERIALIZABLE open; `pools WHERE market_id FOR NO KEY UPDATE` (core builder); coarse unlocked `SELECT markets.status` (Open→callback, non-Open→`MarketNotOpenError`, not retried); full-jitter retry `BACKOFF_BASES_MS=[50,100,200]` + `RETRYABLE_SQLSTATES={40001,40P01}` (co-located, budget 1+3=4); `addBreadcrumb`/retry + `captureMessage('bet_serialization_exhausted')`/exhaustion (alarm-3); `SET LOCAL` statement/idle timeouts. Generic: NO CPMM/Dharma/event/idempotency/moderation/min-bet. `server-only`, named exports, no barrel.
- **The wrapper generates ZERO IDs** (the retry-purity contract is a CALLER obligation): ENGINE.8 generates `event_id` (+ `metadata.idempotency_key`) at handler-entry, ONCE, closed over — never per attempt.
- **Tracker (operator-maintained — recorded, NOT edited):** ENGINE.7 EXECUTE done; the ENGINE.6 / ENGINE.7-plan / ENGINE.7-execute rows are pending DONE reconciliation; ENGINE.8 unblocked next.
- **Environment:** local Postgres :54322 down (whole-suite-needs-Postgres convention) → DB-backed suites are CI-gated; `tests/unit/` is the local proxy. `/tmp` clobbered between turns — md5-gate at use-time.

**Time.** 2026-06-09 (IST). Fresh execute session: sync-gate (HEAD `7949fbc`, both ENGINE.7 docs present) → branch `feat/engine-7-bet-tx` → orientation reads → `@test-writer` RED (3 files) → commit RED → **web gate (surface RED set)** → web GREEN + the required gate-test addition → implement `transaction.ts` + `errors.ts` → `@code-reviewer` → `@security-auditor` → §5.10 self-audit → `just verify` green + `tests/unit/` 248/248 → commit green (+ `await` fold-in amended) → PR #95 → CI green (run `27206540432`, real `postgres:17`) → **web GREEN + founder squash-merge (`37dae5a`)** → post-merge ff-only sync (END ON MAIN) → this log (separate `chore/engine-7-log` PR).

---

# S-3 · Google OAuth signup deadlock — session log

**Task:** S-3 — four concurrent Google OAuth signups deadlocked a 4-connection pool,
freezing the whole instance. **Critical path** (`src/server/auth/`), full ritual.
**Governing document:** `docs/plans/S-3.md`, ratified, committed before any test or code
existed.
**Ritual:** plan → RED reproduction → fix + ADR → GREEN proof → reviewer cascade → Gate C.
`ultracode` **FORBIDDEN, not used**. `@db-migration-reviewer` correctly **not** invoked —
no schema change.
**Ground:** `origin/main` @ `83cf6fb` at plan time; it moved to `9d2a920` (CHART-1 #425)
mid-task — `src/server/auth/`, `src/db/` and `src/server/identity-pool/` are **byte-clean**
across that range, so nothing here is affected. **Deliberately not rebased** onto it.
**State:** **Gate C complete. Not pushed. No PR. The merge is the founder's.**

---

## What landed

| Commit | Files | Content |
|---|---|---|
| `75210d7` | `docs/plans/S-3.md` | the ratified plan (+ NOT ESTABLISHED #7 amended, #8 added at step 7) |
| `7e95013` | 2 × `tests/integration/` | **the control** — the deadlock reproduction and the orphan-fallback characterisation |
| `464973f` | `src/server/auth/index.ts`, `docs/adr/0042-…md` | **the fix + ADR-0042**, same commit |
| `26772ff` | `src/server/auth/index.ts`, the deadlock test, `docs/plans/S-3.md` | the three step-7 rulings, recorded where they are read |
| *(this commit)* | `docs/logs/S-3.md` | this log |

**PR:** not yet opened. §5.9 puts the log before the PR, so the number is not knowable here.
**Canonical SHA will be the squash-merge SHA on `main`** — the four above are ephemeral and
were rewritten three times (see *Context to preserve*).

### The change itself

⛔ **The entire non-comment diff under `src/` is two lines**, in one file:

```diff
-		transaction: true,
+		transaction: false,
```

Verified independently by `@code-reviewer` rather than asserted. Everything else in the
branch is tests, comments and the ADR.

### The proof

| | Pre-fix | Post-fix |
|---|---|---|
| 4 concurrent signups resolving | **0 of 4** | **4 of 4**, ~160 ms each |
| Backends `idle in transaction` | **4**, `last_query: "begin "`, ~5.9 s and climbing | churn; none survives the window |
| pid-set intersection across the poll window | **4 (= N)** | **0** |
| What ended the standoff | nothing server-side — the test's own 20 s deadline | n/a |

A control at **N=3** (one below `max`) completes cleanly — 3 of 3 in **233 / 249 / 285 ms**
on the SHIPPED instrument, intersection 0. **The boundary is exactly `max`**, which is what makes the N=4 red a reproduction rather
than a test that always hangs (V-2).

⚠ **Cite the final instrument.** An earlier figure of *163-170 ms* appears in the step-4
record; that run polled `pg_stat_activity` every **250 ms**. The shipped test polls every
**25 ms** (tightened at the step-4 correction to widen the overlap-witness margin from ×1 to
×9), and the extra polling load is itself the difference: **163-170 ms → 233-285 ms**. The
signups did not get slower — the instrument got heavier. Anyone reproducing against the
committed test should expect the 233-285 ms band.

---

## Decisions made

1. **Candidate A** — drop the adapter transaction. One line. ADR-0042 ratifies it.
2. **`transaction: true` is a CAPABILITY, not a STATE.** The ADR's real payload: the flag
   only decides what `adapter.transaction` does *when called*, and leaves every path that
   never calls it untouched. Misreading it put this defect on the wrong door for five days.
3. **ASSERT 2's window starts at the first non-empty idle sample** (founder ruling, option
   1). Its first form intersected across *every* sample, so one empty warm-up sample
   collapsed the result to zero — **the post-fix expectation — and it passed on unfixed
   code.** An assertion that passes in both phases for opposite reasons certifies nothing.
4. **ASSERT 1 and ASSERT 2 are `expect.soft`.** With hard `expect` the settle assertion
   throws first and ASSERT 2 is *never evaluated* — which is how it shipped vacuous. Soft
   makes both report in one run. The two inconclusiveness guards stay hard.
5. **The overlap witness is NOT owner-filtered** (founder ruling). It counts any live query
   on the database, so background traffic satisfies it. Changing it would have altered the
   instrument between the pre- and post-fix runs and broken the before/after pair the exit
   criterion rests on. Recorded in the test docblock and NOT ESTABLISHED #8 instead.
6. **Fence by symbol, never by line (O-8).** The ADR first cited the flag by line; **the
   same commit that carries the fix invalidated that citation** by adding comment lines
   above the flag. `:346` → `:342` → `:343`. Eight citation sites converted to symbol form.
7. **The plan was NOT amended** where the cascade diverged from it. A ratified document does
   not get a silent edit; the divergence is written into ADR-0042 and came to Gate C.
8. **Not rebased onto `9d2a920`.** A squash-merge diffs from the merge base anyway.

---

## Open questions

1. ⛔ **Merge over a known-flaky harness?** The full suite is **exit 1**: 12 pre-existing
   Windows path-portability failures — **the same 9 files fail identically at base; that run
   was scoped to those 9 files, not the full suite**, which is why a tenth file's failure was
   invisible to it — **plus
   one order-dependent flake** — `user.pseudonym_assigned::happy-path-emits-one-event`. It
   asserts a *global* `events` row count and saw a row left by a prior file; **six of the
   eleven invariant specs never truncate `events`**. It passed in an earlier full run on
   functionally identical code and passes 3/3 alone. **Not the fix — but S-3 is the
   trigger**, because adding two test files reshuffles vitest's duration-sorted order. Both
   of our new files *do* truncate `events`. **Founder's call.**
2. **NOT ESTABLISHED #7 narrowed, not closed.** Poll-overlap margin went ×1 → ×9 at N=3
   after dropping the interval to 25 ms. That is one machine on one day; the cross-machine
   question stands.
3. **Supavisor cleanup remains UNMEASURED** (recon-2 H-3). Postgres reaps nothing here —
   both reapers `0`, `statement_timeout` never arms on an idle-in-transaction backend. **Do
   not generalise that into "nothing reaps it."**
4. **The accepted false-RED edge case.** If the only non-empty idle sample is the last, the
   window is one sample and a correct fix reads red. Deliberate: a false red is loud, the
   false green it replaced was silent. Read `intersectionWindowSize` before diagnosing.

---

## Next session starts at

**Push the branch and open the PR**, then Gate C sign-off and squash-merge.

    git push -u origin fix/s-3-signup-deadlock
    gh pr create   # ⚠ `gh` is NOT installed on this clone — open the PR in the web UI

⚠ **Nothing is pushed and no PR exists.** The branch is absent from `origin`.
⚠ **`gh` is not installed here**, so any prior "PRs: 0" reading in a session report was
`command not found` producing empty output, not a measurement (O-13).

---

## Context to preserve

- **The four branch SHAs were rewritten three times** — CS-3 amend, the eighth-O-8 fold, and
  a signing pass. Every SHA cited in the six session reports before Gate C is stale. Final
  map: plan `6150f27`→`75210d7`; control `c5c64ec`→`7e95013`; fix `0b7baf3`→`1dfdb7b`→
  `0713e9c`→`464973f`; rulings `946b109`→`b20e234`→`c08f4c2`→`26772ff`.
- **The four fix-branch commits carry local SSH signatures; this log commit does not.**
  Signing was ruled out at finalization and `commit.gpgsign` was unset rather than
  rewriting history to strip the existing four. **It does not matter at merge:** S-1's
  commits on `main` are signed by `GitHub <noreply@github.com>` — the *squash-merge*
  signature — so local signatures vanish either way. AGENTS.md §10 calls signed commits a
  **convention**, not a requirement.
- **Live reads at Gate C:** migration head **`0026_lots_no_delete`** (idx 26, 27 journal
  entries, 27 rows applied); **`EVENT_TYPES` = 24**. ⚠ A first count returned 25 because the
  measuring regex matched a quoted string inside a *comment*; two independent methods give
  24, matching AGENTS.md. **The documentation was right and the instrument was wrong.**
- **A local Supabase Postgres 17.6 was stood up** for this work. Not part of the deliverable.
- **`.env.local` does not reach vitest** — Vite's `VITE_` prefix filter discards unprefixed
  vars, so `tests/_setup/env.ts`'s `localhost:54322` fallback always wins. Every run in this
  task supplied `DATABASE_URL` inline. `env.ts:5-6`'s comment claims otherwise and is wrong.
- **`just` is not installed on this clone**, so the justfile's `dotenv-load` never applies.
- **`claude-progress.md` is gitignored** and will not travel with the PR — which is why the
  SURPRISE rows are restated below rather than left only there.

---

## Time

Single session, 2026-08-27. Phase B (environment) → step 4 (reproduce) → correction →
steps 5-6 (fix + proof) → step 7 (cascade) → CS-3 (edits) → Gate C. Full suite ~28-31 min
per run; three full runs, one of which was killed near the end and was **not** reported
from its partial log.

---

## Findings affecting other strata

**Not none. Five.**

1. **`S3-SURPRISE-1` -> S-6 (go-live).** `POST /api/auth/email-otp/request-password-reset`
   is unauthenticated and **not covered by our Turnstile gate** — that gate is path-exact
   on `/email-otp/send-verification-otp`. Better Auth's own 3-per-60s default stores its
   counters in **memory**, which on Vercel serverless is per-instance and resets on cold
   start; **`secondaryStorage` is unconfigured** although Upstash is already a dependency.
   Risk: mail-bomb a participant, burn Resend quota, damage sending-domain reputation over
   a 51-day single-domain run. **Cannot yield a session and is not an enumeration oracle.**
   Pre-existing; not introduced by S-3.

2. **`S3-8` -> S-5. Owner named here, because `claude-progress.md` is gitignored.**
   `src/server/identity-pool/consume.ts`'s *Stranded-tuple semantic* block states that
   Better Auth's OAuth flow does not wrap user-create in a transaction. That was **false**,
   and it is the premise under which nesting a second pool checkout looked free — **this
   comment is where the defect came from.** `@security-auditor` found **both** halves
   wrong: it also claims "the user row rolls back", never true in either mode, because
   `createSession` is invoked outside `createOAuthUser`'s transaction. The fence said
   *report, do not fix*; it is warned about in three places a reader reaches first. **S-5
   already owns that file's neighbourhood (`S3-1`, the `max` measurement).**

3. **Implicit OAuth account linking is ON by vendor default, and ADR-0042 is its first
   record.** `@security-auditor` confirmed the gate holds — it enumerated **all seven**
   writers of `users.email_verified` and found none satisfiable by a non-owner — and
   recommends this get **its own ADR** rather than a paragraph inside ADR-0042, since
   ADR-0042's decision body is declared immutable and the record would vanish with any
   supersession. **Do not block this PR on it.** Take the next free ADR number from a live
   `ls docs/adr/` when that task opens.

⚠ Also for S-5, not a register row: the cascade corrects the plan's blast-radius trace.
`sign-up.mjs:141` is **reached-and-throws**, not unreachable — the route is registered
unconditionally and `runWithTransaction` wraps the `emailAndPassword` guard rather than
sitting behind it. **The pre-fix surface was therefore slightly larger than claimed, and
the fix removes a lever it never took credit for.**

4. **`S3-6` — six of eleven invariant specs never truncate `events`, and it bites any
   lane that adds a test file.** Routed as a finding, not only as an open question,
   because the trigger is generic. `events` carries no FK, so the `TRUNCATE … CASCADE`
   the other specs rely on never reaches it. The specs that leak:
   `I-DAILY-ONCE-001`, `I-GRANT-ONCE-001`, `I-IDEM-ONCE-001`, `I-NO-OVERDRAFT-001`,
   `I-NO-OVERSELL-001`, `I-RESOLVE-ONCE-001`. The victim is any test asserting a
   **global** `events` count — today `user.pseudonym_assigned::happy-path-emits-one-event`.
   **Vitest orders files by duration, so ADDING A TEST FILE reshuffles the order and can
   place a leaking spec immediately before a counting one.** That is a tax on every
   future lane, not a property of S-3. The merge decision for *this* PR stays in Open
   Questions; the leak itself belongs to S3-6.

5. **The local development environment is not reproducible on Windows, and the
   documented path is macOS-only. → programme chat.**

   **Verified on this clone:** `just` and `mise` are both absent from `PATH`, so
   **`just setup` was never invoked from this shell** — the repo was bootstrapped with
   `pnpm` directly. Consequences met during S-3: the whole `justfile` was unreachable,
   including `just db-migrate` and `justfile`'s `set dotenv-load := true`, which is the
   one mechanism that would have fed `.env.local` to drizzle-kit. Every command in this
   task supplied `DATABASE_URL` inline instead.

   **The documented way to start the local database is macOS-only.** `open -a Docker` is
   a macOS command and it is the incantation given in `docs/logs/ENGINE.10-plan.md:81`,
   `docs/logs/ENGINE.12.md:110`, `docs/logs/ENGINE.13.md:55`, `docs/plans/DEBATE.9.md:202`
   and `docs/plans/ENGINE.10.md:324`. **No document gives a Windows equivalent.**
   ⚠ *(Correction to how this was first reported: the macOS instruction is NOT at
   `.env.example:15-16`. Those lines say "Run `supabase start` once; copy the printed DB
   URL line" and are platform-neutral — `.env.example` contains no Docker or macOS
   reference at all. The macOS-only text lives in the five logs/plans above.)*

   **Why it matters beyond this task:** **S-5** needs a local database to seed 100,000
   identity-pool tuples; **S-6** and **S-7** need to run the suites. On a Windows clone
   they lose the same days S-3 lost to environment setup — and S-3's Phase B is now a
   worked example of the route that does function (`npx supabase@latest` with no global
   install; `mise.toml` pins `supabase = "latest"`).

   ⛔ **Three further claims were reported with this finding and DO NOT reproduce on the
   clone as it stands. Recorded as reported-not-confirmed rather than as fact, because a
   log is worth less than nothing if it asserts things a reader cannot verify:**
   - *"lefthook was never installed, so the pre-push gate had never run"* —
     `.git/hooks/pre-push` **exists** (dated 2026-08-27 02:51) and **all three jobs ran
     and passed** on this branch's push. It is installed now. Whether it ran on any
     earlier push from this clone is not determinable from here. ⚠ Note
     `package.json` has **no `postinstall`/`prepare` script**, so `pnpm install` does
     *not* install it — something installed it separately, which is consistent with the
     underlying concern even though the absolute claim does not hold.
   - *"1,184 files were CRLF against an LF index"* — `git ls-files --eol` reports
     **1319 `w/lf`** and **zero CRLF** in the working tree today. Either it was
     normalised or the figure was mistaken; it cannot be reproduced now.
   - *"node_modules was three days stale"* — its mtime is **2026-08-27 03:10**, today.

   **The finding stands on the two verified legs** (no `just`/`mise`; macOS-only
   documented setup). The three above need whoever observed them to say when, or they
   should be dropped.

---

## Post-push addenda (2026-08-27, after `3958f2e` reached `origin`)

**`origin/main` moved again, and the byte-clean check has been extended to cover it.**
At Gate C it was `9d2a920`; at push time it is **`f2cdcc7`**. Two commits landed between:

    f2cdcc7  CRIT-1 — the resolution criterion returns to /m/[slug], collapsed (#427)
    d9128e2  RESO-1 — Market Detail resolution block: four blocks, relocated bar (#426)

⛔ **Checked by path, not inferred from the PR titles.** Across the **full** range
`83cf6fb..f2cdcc7`, the file count is **0** for every one of
`src/server/auth/`, `src/db/`, `src/server/identity-pool/`, `drizzle/migrations/` and
`src/server/events/`. The two commits touch only `src/components/debate/*`, their render
tests, and their own logs/plans. `git merge-tree` reports **no conflict** against this
branch. **The branch remains correct against the newer main and was not rebased.**

**66 Dependabot alerts on the default branch — 2 critical, 21 high, 35 moderate, 8 low.**
Surfaced by the remote on this push. **Pre-existing and unrelated to S-3**, and it appears
in **no register, no ADR and no plan** anywhere in this repo. **The finding is not the
alerts; it is that nobody has triaged them, 19 days from go-live.** → **founder + S-6.**

**All three lefthook `pre-push` jobs ran and passed on this push** —
`no-force-push-protected` (0.34 s), `biome-check-all` (3.04 s), `typecheck` (14.16 s).
Recorded because AGENTS.md §11 notes the jobs are **skipped entirely when a push changes
no files**, and a skip is visually indistinguishable from a clean run. This push carried
files, so the gate genuinely executed. ⚠ **On the available evidence this is the first
observed execution of that gate on this clone** — see finding 5 for what can and cannot
be established about earlier pushes.

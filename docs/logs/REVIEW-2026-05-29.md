# REVIEW-2026-05-29 — Cold repo review + comments.betId index

## Summary
Independent cold review of the whole repo (no project docs treated as
authoritative; all stale pending sync). 10 fresh-context Explore finders +
skeptic verification pass. Result: 0 Critical, 0 High, 10 Medium, 11 Low
(+6 dropped). Repo is an early scaffold; bets/comments/dharma/resolution
services not built yet. Most findings anticipatory. Full report in REVIEW.md.

## Acted on
- comments.betId index (lone un-indexed FK). Schema +1 line; new migration
  0008_comments_bet_id_idx.sql (single CREATE INDEX). tsc + Biome pass.
  NOT run against DB.

## Deferred (act at engine-build time; see REVIEW.md for file:line)
Critical-path (writer/reviewer ritual only, never auto-edit):
- buildEventMetadata() helper (7-field block hand-built x6)
- origin-allowlist normalize via new URL() (availability, not CSRF)
- moderation timeout/retry double-budget (confirm before DEBATE.2)
- idempotency release fail-closed (before any bet-handler depends on it)
Non-critical:
- extractIp() helper (x4 dup); shared Sentry options; unwired infra
  decisions (validateAdminSession/logRequest/useFlag); lucide-react unused;
  seed-script dup + ops-script nits.

## Open
- REVIEW.md punch-list to be parked in docs/sync/ for the sync phase.

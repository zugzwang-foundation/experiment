# ENGINE.13 — session log

> **Stratum:** ENGINE.13 — Initial Dharma grant at first ToS acceptance: the ADR-0018 / SPEC.1 §10.1 equal initial grant (~1,000 ranged, HARDEN.5-owned), paid EXACTLY ONCE per user inside the F-AUTH-4 ToS-acceptance transaction's first-acceptance branch. Producer `src/server/dharma/grant.ts` + the `dharma.granted` emit site + `INITIAL_USER_DHARMA` + migration 0013's UNIQUE-partial-index backstop + `I-GRANT-ONCE-001`. Critical-path money + auth code; Ultrathink-mandatory.
> **Entry:** plan session (S1 sync-gate → S2 read-only recon → S3 founder rulings R1a–R4a + pre-resolved P1–P5 → S4 in-chat draft → web line review GREEN + folds F-1/F-2 → S5 plan PR → founder squash-merge). Execute session appends below in a fresh CC session + fresh web chat.

---

## Plan session — 2026-06-11

**What landed.**
- `docs/plans/ENGINE.13.md` — the founder-ratified implementation plan (R1a–R4a + P1–P5 + F-1/F-2 folded) — via **PR #108**, squash-merged to `main` at **`7f3214c`** (`ENGINE.13 plan — initial grant at first ToS acceptance (docs-only)`), **docs-only, no reviewer cascade** (plan-PR precedent). 462 lines; branch commit `171ade0`; one-file diff 462+/0−; CI run `27326963097` green (1m57s + Vercel pass); `plan/engine-13` deleted post-merge (remote auto-deleted at squash).
- Base: `main @ 3720935` (S1 sync-gate 6/6 PASS). No `src/`, test, or schema/migration changes — plan-only. This log ships on `chore/engine-13-log`.

**Decisions made — pre-S1 web-lane flips (folded into the kickoff; recon-confirmed).**
- **OQ-A flipped F-AUTH-3 → F-AUTH-4** before the CC session opened: SPEC.1 §13's stale-30d purge clause ("`users` rows with `tos_accepted_at IS NULL` older than 30 days are purged by a daily admin sweep, releasing the linked tuple back to the pool") collides with an append-only ledger — a grant at F-AUTH-3 pins every abandoned signup via the two-layer block (`dharma_ledger.user_id` FK `ON DELETE restrict`, 0001:249, **plus** the Bucket-A no-delete trigger — the block is permanent, not carve-out-able). S2 P2 confirmed with evidence and added the decisive fact (S2-surprise): **no single F-AUTH-3 transaction exists on disk** — pool consumption, the Better Auth users INSERT, and the (never-emitted) `user.pseudonym_assigned` event are three independent pieces; there is nothing for a grant to atomically join.
- **OQ-C flipped reuse-`dharma.credited` → mint `dharma.granted`**: `creditedForDate` is schema-REQUIRED (`schemas.ts:242-244`, no `.optional()`) — a one-time genesis row has no UTC-day accrual key. S2 P3 confirmed.

**Decisions made — founder rulings R1a–R4a ("best recoms", 2026-06-11).**
- **R1a — placement:** inside `acceptTosAction`'s FIRST-ACCEPTANCE branch (after the `:126` tab-race no-op), same transaction; lock order extends `users → dharma_ledger → events`; "at signup" = at first ToS acceptance (the participant threshold) — wording pin rides rider R-A.
- **R2a — event:** mint `dharma.granted`, payload `{ userId, amount }` (numericString), aggregate `dharma_account`, payload_version default 1; `EVENT_TYPES` entry + payload schema + SPEC.2 §19.4.1 row same-commit per the table's own foot-rule. No `AggregateType` change.
- **R3a — constant:** `INITIAL_USER_DHARMA` — the SPEC.1 §16.1:930 name beats the kickoff's `INITIAL_GRANT_DHARMA` (spec > kickoff precedence; ENGINE.12 `DAILY_CREDIT_DHARMA` name-adoption precedent); zero spec edit, drafted rider R-E dropped. `"1000"` decimal string; HARDEN.5 (2026-09-01) owns the value; equal-for-all per ADR-0018 Driver 3.
- **R4a — isolation drift RECORDED, not conformed:** `tos-accept.ts:111` passes no `isolationLevel` (READ COMMITTED + the `:119` FOR UPDATE row lock) while the file's own comments, `tos-accept-event.test.ts`'s header, and SPEC.2 §3.5:289 all claim SERIALIZABLE (recon SURPRISE S1). Conforming without a retry loop would degrade the handled tab-race into a user-visible 40001. Grant safety is isolation-independent (FOR UPDATE + no-op branch + 0013). Truth-up carry-forward minted (forwards below).
- **P1–P5 stood unvetoed:** `I-GRANT-ONCE-001` (P1) · migration 0013 plain-column UNIQUE partial index via the drizzle GENERATED path, loud-failure policy (P2) · auto-read first-row balance path, no `previousBalance` (P3) · riders close as R-A..R-D; drift set → truth-up sweep, NOT this PR (P4) · no backfill — pre-launch environments reseed (P5).

**Decisions made — web line review (GREEN + 2 folds).**
- **F-1 — R-B rider final wording:** retains "at SERIALIZABLE isolation" exactly as the spec has it today; the drafted inline "(Note: …)" caveat DELETED — riders land as final spec prose, no meta-commentary. The isolation drift stays recorded ONLY via the R4a carry-forward + the planned `tos-accept.ts` comment paragraph. Resolves draft-note D-N7.
- **F-2 — execute micro-pins:** (a) "0013" is the EXPECTED-next migration index — re-verify at execute before generating (ENGINE.12 F3b house pattern); (b) pre-RED fixture sweep `grep -rn "initial_grant" tests/` — S1's zero-call-site grep covered `src/` only; a pre-existing same-user duplicate fixture would 23505 under 0013.
- **D-N1–D-N6 pinned as drafted:** grant call BETWEEN the 5-column UPDATE and the `user.tos_accepted` emit — event-log chronology is INSERT-order-independent (`events.created_at` derives from the handler-entry UUIDv7 ms prefix; `eventId` minted before `grantEventId`) (D-N1) · `tos.test.ts` break-risk CONFIRMED — mocked tx has no `insert`, module mock mandatory + called/not-called assertions; `tos-accept-event.test.ts` NO-edit (all count assertions `event_type`-filtered) (D-N2) · CLAUDE.md §2 + AGENTS.md §9 invariant-list one-liners added to the execute file plan (closing-ritual; ENGINE.12 precedent) (D-N3) · T5 fault vehicle = partial passthrough mock of `@/server/events/insert` throwing TERMINAL on `user.tos_accepted`, the tx's final write (D-N4) · payload carries the raw constant `"1000"`, ledger carries canonical 18-dp (accrual precedent) (D-N5) · names pinned `grantInitialDharma` / `validateGrantAmount` / `tests/server/auth/tos-accept-grant.test.ts` / `I-GRANT-ONCE-001.initial-grant-once-per-user.spec.ts` (D-N6).

**Recon highlights (S2, load-bearing — full report lives in the session record).**
- **F-AUTH-4 as built** (`tos-accept.ts:66-172`): one `db.transaction` — `SELECT … FOR UPDATE` `:119` → no-op branches `:125` (missing row) / `:126` (tab-race) → 5-column UPDATE `:131-139` → `user.tos_accepted` emit `:145-158`; `eventId` minted at handler entry `:100` (ADR-0016 D1). Repo-wide, only `bets/transaction.ts:127` and `auth/admin/login.ts:128` set SERIALIZABLE.
- **SURPRISES S1–S5:** S1 — isolation comment/spec ≠ code (→ R4a). S2 — F-AUTH-3 single-tx + `user.pseudonym_assigned` are spec fiction (SPEC.2 §3.5:287 + §8.3:873). S3 — constant-name conflict (→ R3a). S4 — the spec'd stale-30d sweep's tuple-release contradicts the 0003 Bucket-B one-shot trigger; sweep is HARDEN-era (`tos.test.ts:358` `it.todo`). S5 — SPEC.2 §4:365 names `acceptPseudonymAndTos` at `src/server/auth/tos/accept.ts` — name, path, and combined-flow claim all stale vs the built `acceptTosAction`.
- **Carry-forwards consumed:** `persist.ts:51-56`'s auth/onboarding per-user-serialization assignment (discharged: FOR UPDATE + no-op branch + 0013) · `ledger.ts:38-39`'s "signup" sign-discipline (discharged: `validateGrantAmount`) · ENGINE.12 R6's deferred AUTH-lane grant-producer task (this plan IS that task).
- **Test-surface ground:** the DB-backed double-invoke vehicle exists (`tos-accept-event.test.ts` routes `@/db` → `testDb` and drives the REAL action); ENGINE.12's split reused (end-to-end race test + fixture-bypass invariant spec); `I-NO-OVERDRAFT-001` does NOT cover grant-once → `I-GRANT-ONCE-001` minted (P1).

**Process verifications & anomalies (recorded).**
- **Model:** `claude-fable-5`, ultrathink every turn, NOT ultracode (the session-level ultracode flag was ON at S1 turn-1 injection; recon nonetheless ran solo-sequential per the mode pin + standing feedback). Zero Fable→Opus classifier fallbacks across S1–S5.
- **Hygiene checks all clean:** branch names verified free pre-`checkout -b` (both branches); plan-file tail verified post-Write (462 lines, zero stray delimiter tokens); CI watched to green before the S5 report; END-ON-MAIN used checkout → branch-assert → fetch → reset (the verify-before-reset guard).
- No relay losses, no base refreshes mid-session, no worktree deviations.

**Open questions.** None.

**Forwards & queued notes (minted this session).**
1. **R4a isolation truth-up:** SPEC.2 §3.5:289 says SERIALIZABLE; `tos-accept.ts` runs READ COMMITTED + FOR UPDATE (its comments and `tos-accept-event.test.ts`'s header repeat the claim). Later decision: amend §3.5:289 to bless lock-then-recheck OR conform with a proper retry loop. The R-B rider deliberately leaves the isolation clause untouched (F-1).
2. **Drift set → truth-up sweep (recorded once, NOT the execute PR):** S2 (F-AUTH-3 three-tx reality; `user.pseudonym_assigned` never emitted — §3.5:287 + §8.3:873), S4 (sweep tuple-release vs the Bucket-B one-shot trigger), S5 (§4:365 `acceptPseudonymAndTos` name/path/single-tx claim).
3. `tests/server/auth/tos.test.ts:358` stale-30d sweep `it.todo` stays HARDEN-era — untouched by ENGINE.13.
4. **Tracker (operator-maintained — recorded, NOT edited):** ENGINE.13-plan → DONE joins the reconciliation queue.

**Next session starts at.** **ENGINE.13 EXECUTE in a FRESH CC session + fresh web chat** (§5.8 — never this session). Mode pin: `claude-fable-5` / effort xhigh / ultrathink / NOT ultracode; FULL ritual, no narrowing. Step 1 = sync-gate (HEAD `7f3214c`+; `docs/plans/ENGINE.13.md` present on main) → branch `feat/engine-13-initial-grant`. Step 2 = the F-2 micro-pins (next-migration-index re-verify; pre-RED `grep -rn "initial_grant" tests/` fixture sweep) → `@test-writer` RED (T1–T7 + the `tos.test.ts` additive set, against `@docs/plans/ENGINE.13.md`). Then implement `grant.ts` → the tos-accept seam → `limits.ts` → `schemas.ts` → schema + 0013 → riders R-A..R-D + the CLAUDE.md §2 / AGENTS.md §9 one-liners → local gates (`ZUGZWANG_ENV=preview just verify` + invariants + integration + test-db, `:54322` up) → cascade `@code-reviewer` → `@security-auditor` (FULL scope incl. the auth/signup path) → `@db-migration-reviewer` (0013) → §5.10 self-audit → log append + PR → squash. **Do NOT start execute in this chat.**

**Context to preserve.**
- **Canonical plan SHA:** `7f3214c` (PR #108; branch commit `171ade0`).
- **The grant unit, one line:** first-acceptance branch — 5-column UPDATE → `grantInitialDharma(tx, { userId, grantEventId, metadata })` → `user.tos_accepted` emit; `grantEventId` minted at handler entry beside `:100`'s `eventId`, closed over (retry purity); NO `previousBalance` (auto-read first row ⇒ `balance_after = amount`, ENGINE.5 R-1 shape); the `:125`/`:126` no-op branches NEVER reach the grant.
- **Backstop DDL (0013):** `CREATE UNIQUE INDEX dharma_ledger_initial_grant_user_uq ON dharma_ledger (user_id) WHERE entry_type = 'initial_grant'` — plain column (simpler than 0012's expression); drizzle GENERATED path.
- **Environment:** local Postgres `:54322` (Docker Desktop via `open -a Docker` + `supabase start`), CI PR-gated; `/tmp` clobbered between turns (md5-gate at use-time).
- **Tracker (operator-maintained — recorded, NOT edited):** ENGINE.13-plan DONE; ENGINE.13-execute next.

**Time.** 2026-06-11 (IST). Session arc: kickoff (OQ-A/OQ-C pre-flipped by the web lane) → S1 sync-gate (6/6 PASS @ `3720935`) → S2 read-only recon (P1–P6; SURPRISES S1–S5; founder-OQ candidates F1/F2) → S3 founder "best recoms" (R1a–R4a; P1–P5 unvetoed) → S4 in-chat draft (D-N1–D-N7) → web line review GREEN + F-1/F-2 → S5 branch commit `171ade0` → PR #108, CI `27326963097` green (1m57s) → founder squash-merge `7f3214c`, branch deleted → END-ON-MAIN → this log (`chore/engine-13-log`).

---

## Execute session — 2026-06-11

**What landed.**
- The full ENGINE.13 implementation via **PR #110**, squash-merged to `main` at **`76877e6`** (`feat(dharma): ENGINE.13 — initial grant at first ToS acceptance (F-AUTH-4 tx + I-GRANT-ONCE-001)`). Four branch commits: `0cc50a5` (RED test set, 4 files +757) → `0f630b5` (server + riders: `grant.ts` NEW 111 lines, the tos-accept seam +30, `INITIAL_USER_DHARMA`, `dharma.granted` vocabulary, riders R-A..R-D) → `22f1d48` (schema uniqueIndex + generated 0013 + meta + the gate-ruled D2 drop/restore edit + CLAUDE/AGENTS one-liners) → `583bbaf` (the inventory-floor fix-up, S5 SURPRISE-ruled). Final diff: 18 files, ~3.85k insertions. CI green on the PR head (run `27338416918`, 1m59s + Vercel pass).
- **0013 DDL (applied + backstop-proven):** `CREATE UNIQUE INDEX "dharma_ledger_initial_grant_user_uq" ON "dharma_ledger" USING btree ("user_id") WHERE "dharma_ledger"."entry_type" = 'initial_grant';` — single statement, GENERATED path, byte-matches the plan P2 expectation.
- **Tests:** `tos-accept-grant.test.ts` (T1/T2/T3/T5/T7a/T7b, DB-backed real-action vehicle) · `I-GRANT-ONCE-001.initial-grant-once-per-user.spec.ts` (T4 fixture-bypass 23505 + scope guards; invariants 7 → 8) · `tests/unit/dharma/grant.test.ts` (T6 ×8) · `tos.test.ts` +173/−0 additive (called-once with `(tx, { userId, grantEventId, metadata })` + 6 not-called guards) · the D2 duplicated-genesis drop/restore edit · the EVENT_TYPES inventory floor 21 → 22.

**Surprises caught + fixed in-session (§5.10 surprises-as-wins — both chains in the PR body so they survive the squash).**
1. **S1 F-2b fixture sweep → Option-A ruling.** The plan's own F-2b micro-pin caught `tests/integration/positions.integration.test.ts:529-583` (`drift-D2::duplicated-genesis`) deliberately inserting a SECOND same-user `initial_grant` row — the ENGINE.11 A1/A2 D2-B drift belt, a guaranteed fixture-seed 23505 under 0013. Outside the CLOSED set → surfaced via `claude-progress.md`, STOPPED before branch checkout. **Founder ruling (Option A, the D3 mirror):** the file joins the closed set, edit scoped to that one test — DROP INDEX → seed → drift assertions → TRUNCATE + re-CREATE **byte-faithful to 0013** in `try/finally`, same commit as the migration (`22f1d48`). `@db-migration-reviewer` verified restore fidelity character-for-character; post-suite `pg_indexes` confirmed live restore. *Attribution correction (web-lane re-derivation):* the D2-B duplicated-genesis detector is **ENGINE.11's** A1/A2 work, not ENGINE.12's as the S1 report first stated.
2. **S5 inventory floor → fix-up `583bbaf`.** Post-PR-open CI red: ONE test of 695 — `tests/server/events/insert.test.ts::events::canonical-event-types-inventory-shape`, the deliberate EVENT_TYPES exhaustive floor (sorted equality + length pin 21), tripped by the R2a-added `"dharma.granted"`. The plan's break-risk section analyzed `tos.test.ts` + `tos-accept-event.test.ts` but missed this consumer; outside the CLOSED set → surfaced, STOPPED. **Founder ruling:** scoped edit — append `"dharma.granted"` (sort preserved), count 21 → 22, provenance git-verified before writing (`dharma.credited` = ENGINE.0 `4dc16d7`; `dharma.granted` = ENGINE.13). Re-runs: events suite 57/57; full `pnpm vitest run` 688 passed / 0 failed; CI green.

**Session arc + gate evidence.**
- **S1 sync-gate:** items 1–5 + 7 PASS at `b62ec12` (HEAD = the #109 squash itself); item 6 = the F-2b SURPRISE above; item 8 checkout withheld until the ruling, then `feat/engine-13-initial-grant` created (name verified free both sides). Greenfield re-confirmed: 6 `initial_grant` hits in `src/`, all definitional. Next-free migration index re-verified = 0013 (F-2a, no drift). Local `:54322` already up.
- **S2 RED (@test-writer, plan passed as first-read; never touched `src/` or the D2 file):** RED shapes — greenfield-import **collection failure** for `tos-accept-grant.test.ts` + `grant.test.ts` (the plan's designed mechanism; load-bearing for T5/T7, which would be vacuously green pre-seam — the current action's writes already roll back under the T5 fault); **live assertion failure** for T4 against `:54322` (no 23505 pre-index); **assertion failure** for the `tos.test.ts` called-once (0 calls). Note for future RED designs: `vi.mock` with a factory registers fine against a then-nonexistent module path (lazy resolution) — the whole-suite collection-RED fallback did NOT occur; the suite REDs by assertion, the cleaner shape.
- **S3 implement + gates:** CLOSED file set built exactly (plus the drizzle GENERATED-path meta artifacts, 0012 precedent); riders same-commit as the code they describe. Five local gates PASS: `ZUGZWANG_ENV=preview just verify` · `pnpm test:invariants` (8 files/17) · `pnpm test:integration` (9/92) · `just test-db` (23/80) · targeted auth+dharma (22 files/189+2 todo). RED→GREEN flip verified per-test with the verbose reporter, in-session (not subagent-claimed).
- **S4 cascade (all PASS; zero fixes → no gate re-runs):** `@code-reviewer` PASS — 6/6 gate-mandated checks (retry purity / P3 path / seam placement / R4a fidelity / validateGrantAmount mirror / general), 2 LOW both no-code-change (the log-duty item, discharged by this section + the PR body; the T3 note below). `@security-auditor` PASS — full scope incl. the auth/signup path: reachability single-call-site, HMAC onboarding-ref boundary (stolen ref grants into the VICTIM's account only), no-op integrity (purge-set and grant-set disjoint by construction — the R1a rationale survives attack), cross-lambda FOR UPDATE serialization, 23505 loud posture (zero try/catch in the chain). `@db-migration-reviewer` PASS — schema↔SQL↔plan coherent, meta coherent, D2 restore byte-faithful, blast radius confined (actual numstat +62/−45; the "+107/−48" in the gate text was the `--stat` combined-line figure).
- **S5:** §5.10 audit 10/10 PASS (+ the post-ruling inventory addendum) → PR #110 opened with the mandated body → CI red → SURPRISE 2 chain above → fix-up → CI green → founder merge `76877e6`.

**Web-lane facts (gate).**
- The S4 report relay truncated at the operator paste; the gate held open and a scoped verbatim re-emit (no subagent re-runs) completed the review.
- Both SURPRISE rulings (S1 Option-A; S5 inventory floor) were founder-approved on web-lane recommendation.
- The ENGINE.11-not-ENGINE.12 attribution correction originated from web-lane re-derivation against the ENGINE.11 plan/log in PK.
- The founder merged #110 after CI green and the full cascade, ahead of the formal web-lane diff-review step; sequence recorded as-is.

**Decisions made.**
- Both SURPRISE rulings (above) — closed-set additions, scoped edits only.
- **Process lesson adopted for future strata:** the mandated local gate list did not cover `tests/server/events/` — **full-suite `pnpm vitest run` becomes the final pre-PR local gate** (the documented CI proxy). The CLAUDE.md §5.7 gate-list edit is batched to the next sweep per house pattern (closing ritual answered: yes, change needed; deferred-by-ruling, not forgotten).
- S4 out-of-scope findings recorded in `claude-progress.md` (ENGINE.2/ENGINE.7 precedent), both PRE-EXISTING: (a) LOW — Set-Cookie string concatenation without charset validation (`src/app/api/auth/[...all]/route.ts:48`); owner HARDEN.* auth-hardening. (b) LOW — conclusion-freeze enforcement must include `acceptTosAction` in its blocked set now that F-AUTH-4 is money-issuing (`system_state.frozen_at` is schema-only today); owner: the freeze-enforcement task.

**Open questions.** None for ENGINE.13. The R4a isolation truth-up + S2/S4/S5 drift set remain with the truth-up sweep (plan-session forwards 1–2, unchanged).

**Forwards & queued notes (minted this session).**
1. **Tracker note (operator-maintained — recorded, NOT edited):** ENGINE.13-execute → DONE; plus the T3 single-connection limitation — the concurrency test runs both actions over the fixture's single connection (`max: 1`), so cross-connection FOR UPDATE contention is not exercised locally (disclosed in-file; production safety = Postgres row-lock semantics + the 0013 backstop; ENGINE.12 T3 precedent).
2. **CLAUDE.md §5.7 gate-list amendment** (full-suite vitest as final pre-PR local gate) → next SYNC sweep.
3. The two PRE-EXISTING security LOWs (owners above) → `claude-progress.md` carries them.

**Process verifications & anomalies (recorded).**
- Mode pin held: `claude-fable-5`, gated-xhigh, ultrathink every coding turn, NOT ultracode (session-level ultracode flag ON at injection; overridden by the kickoff pin per CLAUDE.md §6). Zero Fable→Opus classifier fallbacks across S1–S6.
- Hygiene: branch names verified free pre-`checkout -b`; heredoc-appended files tail-verified; CI watched to terminal state both runs; END-ON-MAIN used checkout → branch-assert → fetch → reset (verify-before-reset guard); unique per-task `/tmp` filenames, written and consumed same-turn.
- One relay loss (the S4 report top) — handled by gate-supervised verbatim re-emit, no re-derivation.

**Next session starts at.** **S7 — PK staging + tracker note** (operator-relayed), per the gate sequence. No repo work pending beyond this log's PR; the next code stratum picks up from the tracker queue in a fresh CC session + fresh web chat (§5.8).

**Context to preserve.**
- **Canonical execute SHA:** `76877e6` (PR #110; branch commits `0cc50a5` / `0f630b5` / `22f1d48` / `583bbaf`, ephemeral).
- **The shipped grant unit, one line:** first-acceptance branch — 5-column UPDATE → `grantInitialDharma(tx, { userId, grantEventId, metadata })` → `user.tos_accepted` emit; `grantEventId` handler-entry-minted beside `eventId`; auto-read first-row path ⇒ `balance_after = amount`; no-op branches unreachable; 0013 backstop loud-23505.
- **EVENT_TYPES is now 22** (the inventory floor at `tests/server/events/insert.test.ts` pins it — any future event type must amend the floor same-commit).
- **Invariant specs are now 8** (`I-GRANT-ONCE-001` joined); CLAUDE.md §2 + AGENTS.md §9 already say so.

**Time.** 2026-06-11 (IST). Session arc: S1 sync-gate (7/8 + F-2b SURPRISE → STOP → Option-A ruling → branch) → S2 @test-writer RED (collection/assertion shapes as designed) → S3 implement + 0013 + riders + five gates green → S4 cascade 3× PASS (2 PRE-EXISTING LOWs banked) → S5 §5.10 audit clean → PR #110 → CI red (inventory floor) → SURPRISE → ruling → fix-up `583bbaf` → CI green → founder squash-merge `76877e6` → END-ON-MAIN → this log (`chore/engine-13-execute-log`).

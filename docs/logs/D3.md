# D3 — session log (staging-as-replica un-shadowing)

**Stratum:** D3 — staging branch + `branchMatcher` repoint + Production auto-assign-domains OFF · **Plan PR:** #170 (merged) · **Log branch:** `chore/d3-log` · **State:** EXECUTED — EXIT GATE green; log PR awaiting web review · **Date:** 2026-06-26

## What landed (files + PR#)
- **Plan + runbook (PR #170, squash-merged — «D3-SHA» = `41311bc` / `41311bc9ceac494d01abd9b2458774f856e46cd6`):**
  - `docs/plans/D3.md` — RATIFIED plan (OD-1/OD-2/OD-3; verified Vercel toggle navigation; strict Phase A→D ordering; curl-driven verification).
  - `docs/runbooks/deploy-pipeline.md` — **NEW** (OD-3). CC-authored staging auto-deploy mechanics + `/api/health` curl gate + resettable-sandbox/seed posture + operator toggle navigation; §3 prod-promote is a web-authored **PLACEHOLDER** finalized at D5 (ADR-0024 item 5) — no fabricated prescriptive content.
- **Infra state changed (out-of-tree — git + Vercel):**
  - `staging` branch created @ «D3-SHA» (`git push origin 41311bc9…:refs/heads/staging`) — un-shadows the matcher (ADR-0024 item 2).
  - **Operator (Vercel):** staging `branchMatcher` repointed `main → staging`; Production **"Auto-assign Custom Production Domains" toggled OFF** (ADR-0024 item 5 precondition).
- **Session log (this file, PR `chore/d3-log`).**
- **Docs-only PR footprint** (OD-1) — `smoke-staging.ts` deliberately untouched; no `src`/schema/migration/workflow change; no new ADR.

## Decisions made
- **OD-1/OD-2/OD-3 executed exactly as ratified.** Verification was **curl-driven** (`/api/health`), not `pnpm smoke:staging` — smoke would false-fail a healthy post-D1 staging (SURPRISE-1, below). Staging branch created by CC (`git push origin main:staging`) **after** the operator's matcher repoint (OD-2 ordering).
- **Strict Phase A→D ordering held** — the minimal D3 PR (#170) was the **last `main` push that auto-served Production**; `main` was frozen at A.3 and the auto-assign toggle (Phase D) followed *after* the merge auto-served. The `chore/d3-log` PR merges post-Phase-D (auto-assign now OFF → it creates a staged build, does not auto-serve — correct).
- **`staging-migrate.yml` validated on first real fire** (run `28245428299`, GREEN): Doppler CLI action + `DOPPLER_TOKEN_STG` secret work; target = `aws-1-ap-south-1.pooler.supabase.com:5432` (session pooler); migrate idempotent (staging already @ `0018`).
- **Pre-flight:** `BETTER_AUTH_URL` present in Doppler `stg` (local CLI, name-only) ✓; `DOPPLER_TOKEN_STG` validated by the live GHA run (not testable locally).
- **No subagents / no `db-migration-reviewer`** — infra/doc task, no `src` business logic, no schema/migration (CLAUDE.md §5.11).

## Surprises caught + handled in-session (§5.10)
- **Operator confirmation word mismatch on the Phase D toggle (caught, did not proceed).** The Phase D step-10 confirmation came back as **"repointed"** — the *Phase B* word (staging-matcher repoint), not the requested **"toggle OFF auto-assign in Production → Branch Tracking."** Because this toggle is the one Phase D check that **cannot** be verified functionally (the prod canary stays `41311bc9…` whether auto-assign is on or off, since no new `main` push occurs; and the Vercel setting is UI/write-only — memory `feedback_vercel_env_writeonly`), accepting a mismatched word would have risked declaring D3 done with auto-assign silently still ON — the exact D5 hazard ADR-0024 closes. Pushed back (CLAUDE.md §4), requested explicit confirmation; operator confirmed *"Yes — toggled OFF auto-assign in Production → Branch Tracking."* R2-AFTER then taken as the formal proof.
- **SURPRISE-1 (pre-known, re-confirmed) — `smoke-staging.ts` canary assertions are STALE.** `:185` asserts `canary.startsWith("staging-")` and `:202` `"preview-"`, but the canary is the bare git SHA (ADR-0024 item 7 / D1) → smoke **false-fails a healthy staging**. Siblings: `migrate-staging.ts:8` header (`--config staging` → `stg`); `staging-provisioning.md:157` (`ZUGZWANG_ENV_CANARY=staging-...`, no longer read by the route). **DEFERRED to a focused chore in the immediate next chat** (OD-1) — see *Next session*.
- **AGENTS.md "staging-migrate.yml inert until D3" framing is now stale** (the workflow is **armed** — it fires on push to `staging`). Per OD-1's minimal-footprint ("plan + runbook + log ONLY") + the maintenance doctrine (doc reconciliation is periodic, not per-task — `docs/maintenance.md`), **recorded here for the next combined SYNC sweep, not edited in this PR** (same handling as D2's stale `0016` migration-head). The workflow's own header comment stays self-describing and harmless once armed.

## Open questions
- None blocking. D3's EXIT GATE is fully green.

## Next session starts at (exact next action)
- **IMMEDIATE next chat — the stale-canary chore (SURPRISE-1 + siblings):** fix `scripts/smoke-staging.ts` canary assertions (bare-SHA, drop the `staging-`/`preview-` prefix checks) + the two sibling comment drifts (`scripts/migrate-staging.ts:8` `--config staging → stg`; `docs/runbooks/staging-provisioning.md:157` `ZUGZWANG_ENV_CANARY`). Small focused PR; re-run `smoke:staging` against the now-live staging to prove it goes green.
- **Then D5** (NOT before): the prod staged-promote — `db:migrate:prod` (ADR-0022 path) → verify the staged build's `/api/health` per-hash `migrations:ok` → promote. Finalize `deploy-pipeline.md` §3 (web-authored). Prod DB currently @ `0005` (`migrations:"drift"` on prod is expected until then).

## Context to preserve
- **«D3-SHA» = `41311bc9ceac494d01abd9b2458774f856e46cd6`** (PR #170 squash-merge on `main`) — the canonical D3 landing.
- **Production auto-assign-domains is OFF** as of 2026-06-26 → a `main` push no longer auto-serves prod (it creates a staged build); **`staging` branch now auto-deploys staging** (matcher `equals staging`). This is the migrate-before-serve precondition for D5.
- **R2 proof:** prod `/api/health` canary `41311bc9…` identical at 14:46:44Z (before) and 17:12:23Z (after) the toggle; `HEAD /` → 200 both — toggle stranded nothing.
- Staging is a **resettable sandbox**: seed via `doppler run --config stg -- pnpm db:seed:staging` (idempotent: `0 new / 200 present`); full reset = drop schema → `db:migrate:staging` → `db:seed:staging`. Doppler config is **`stg`** (never `staging`).
- `migrations:"ok"` on staging is **per-hash** (D1) and only valid on deployed envs (pg_cron present, unstripped); CI's `db:check-drift` stays timestamp+count.
- Migration head = **`0018`** (19 entries) — AGENTS.md §6 still reads `0016` (stale since SYNC.8; queued for the next sweep alongside the "inert until D3" line).

## Time
2026-06-26. Active CC execution ~minutes (file authoring + PR + CI watch + staging/prod curls); wall-clock spanned ~2.5 h across two operator handoffs (Phase B matcher repoint; Phase D auto-assign toggle).

# POLISH.4 · PR A — THE RECORD · session log

**Branch** `polish/4-pr-a` · **base** `origin/main` @ `8db535d` (`fix(db): bound the Supavisor pool — idle timeouts + max:4 — ⛔ MITIGATION, DO NOT MERGE (#343)`)
**Worktree** `/Users/hrishikesh/code/zz-p4-pra`, cut at `origin/main`.
⛔ **DO NOT MERGE — Gate C first, and this time the label is real.**

**`docs/**` only. Zero `src/`, zero `tests/`.** No reviewer cascade — no `src/server/`, no schema. `ultrathink` on every prompt; ⛔ no `ultracode`, no dynamic workflow, no parallel fan-out (plan §0.2).

---

## 1 · What landed

Six commits, **970 insertions / 11 deletions across 5 files** — all six inside plan §5's `docs/**` allow-list; **zero outside**.

| # | SHA | Commit | Rows | Stat |
|---|---|---|---|---|
| **A0** | `ad4c18b` | `docs(polish): POLISH.4 plan + the HTML-FINISH fold — ground 35da436, amended at 8db535d` | the plan itself + **the fold** | 1 file, +874 |
| **A1** | `1e90212` | `docs(polish): PD-4 register section — first mint` | `PD-4-01` … `PD-4-11` | 1 file, +12 −2 |
| **A2** | `d4fb9cd` | `docs(polish): RR-3 adoption record; close three open assertions` | `PD-4-01` · `PD-4-02` | 2 files, +5 −5 |
| **A3** | `f9a9b2b` | `docs(polish): POLISH-0 §3 .4 row — components 11→19, path-qualify, re-fence Đ BET` | `PD-4-03` + **Rider 1** | 2 files, +4 −4 |
| **A4** | `e90d694` | `docs(polish): POLISH-0 §2.2 — declare the DESIGN_W2_10 close-out gap` | **Rider 2** | 1 file, +4 |
| **A5** | `0178f61` | `docs(polish): route OQ-1, R11, and the values-log §1 item 4 boundary` | `PD-4-08` · `-09` · `-10` + 3 `parked.md` rows | 3 files, +77 −6 |
| **A6** | *this* | `chore(polish): log session — POLISH.4 PR A` | — | this file |

Files touched: `docs/plans/POLISH-4.md` (new) · `docs/polish/POLISH-register.md` · `docs/polish/POLISH-0.md` · `docs/polish/POLISH-TRACKER.md` · `docs/parked.md` · `docs/logs/POLISH-4.md` (new). **All six are the six paths §5 names. Nothing else was written.**

**Per-row outcome: 3 fixed · 3 routed · 4 ruled-and-scheduled (PR B / PR C) · 1 blocked.**

---

## 2 · The plan was never in the repo, and it is now

`docs/plans/POLISH-4.md` was authored in plan mode and **never committed**. It was copied from `~/Downloads/POLISH-4.md` after verifying both legs the kickoff named: **md5 `13fd3da6f65e6d662749b11282a3cc5a`, 638 lines** — both matched exactly, and the copy in-repo re-hashed identical before any edit. Tail checked for stray delimiter tokens: clean.

It then grew **638 → 874 lines** under the amendment.

---

## 3 · The fold — what the amendment did, and what it deliberately did not

The founder ratified all four open decisions and **folded the HTML-FINISH parity lane into `.4`**. **Additive**: §1, §2, §8 and §13's rulings are untouched. Per **`O-5`** the corrections are written **INTO** §4, §5, §10, §12 and §14 — the operative sections — and the new **§15** *records* the change rather than delivering it.

### 3.1 Ratified (§15.1)

| # | Ruling | Where it now binds |
|---|---|---|
| `OD-1` | **RUN the delta recon** | §12 is live and commissions **both** row sets. `PD-4-11` |
| `OD-2` | **Named preset, defaulted byte-identical.** `.1`'s four `shell/` sites stay OUT and get their own row. ⛔ **Do NOT adopt `.3`'s tier** | PR **C**. `PD-4-04` |
| `OD-3` | **REMOVE `c2Strip`**, with §8 row 5's positive control | PR **B**. `PD-4-05` |
| `OD-4` | **File `OQ-1` as `R20`, state `OPEN`**, naming what it blocks | commit **A5**. `PD-4-08` |

### 3.2 Structure

- **§4 — three PRs, not two.** `A` = the record · `B` = **LAYOUT + BEHAVIOUR** · `C` = **VALUES**. **B precedes C because values tuned on a structure about to change are tuned twice** — and this family has already paid for it twice, which is why the reason is cited rather than asserted: PROFILE **R4→R5** discarded a whole three-viewport band table when `ProfileGraphCard`'s `aspect-[2/1]` came off and the band was re-derived to a single `256`; BOOKMARKS **R1→R3** byte-carried a one-panel arena's values and then replaced the arrangement wholesale.
- **§5 — B and C share A's allow-list. There is not a second one.** Both annexations hold for **both** lanes: annexation is a property of the *file*, not of the kind of change proposed against it.
- **§10 — three new halts.** `H-VALUE` (no colour/radius/px/type size/duration/easing out of a mockup) · `H-SYSTEM` (**the design system is not touched by the parity lane** — no token, no preset, no ramp change) · `H-CASCADE` (any row touching `BetComposer.tsx` or `SellModule.tsx` opens the full sequential cascade; **Discovery's `P-8` carve-out does NOT extend** — that surface had no invariant obligations).
- **§12 — the relay commissions BOTH row sets and says so in its task line**, which spends this plan's own §14 lesson on its own relay.
- **§14 — two records.** `.4` is the first task spanning both strata; `POLISH-TRACKER.md` carries zero HTML-FINISH mentions.

### 3.3 ⛔ What the fold did NOT change

§1's carry-in · §2's measurements · §3's eleven-row item table · §6's deny-list · §7's no-edit symbols · §8's census · §11's two riders · §0.2's ultracode ban. **The fold adds work; it does not add permission.**

---

## 4 · Findings measured at the amendment — `P4-F1` … `P4-F5`

⚠ **Prefix chosen deliberately.** A bare `F-n` is already in simultaneous use — #341's plan carries `F-1`…`F-7`, SPEC.1 carries `F-BET-9`/`F-DEBATE-1`. Per `CLAUDE.md` §8 a task-scoped finding **carries its task**: `P4-F<n>`. ⛔ **No `PD-4` id was minted** — §3's range stays `PD-4-01` … `PD-4-11` and §A's leg stays true.

**`P4-F1` — the one that matters most.** ⛔ **An arrangement row's owner is the file that DECLARES the arrangement, not the file that is arranged** — and **both** of the parity lane's named behaviour instances declare **outside** `.4`'s allow-list:

| Instance | Declared in | Whose |
|---|---|---|
| the **sell slide** | `profile/PositionsTable.tsx` — the `h-[50px]` sell-host band, its `animate-in fade-in … duration-[.26s]` wrapper, the JS toggle. `SellModule.tsx` carries **no transition of its own** | `.5`'s — `H-HOST`, **read-only** |
| the **composer's opposite-slot open** | `debate/DebateView.tsx` — `opposite()`, the open-side guard, `composerColumn`, the two `engaged` props — plus `DebateColumn.tsx`'s glow branch. `BetComposer.tsx` **does not choose its slot** | `.3`'s — `D5` |

⇒ **`PD-4-10` is not a one-off; it is the shape of this surface.** The composer is a *guest* on two hosts, and arrangement is a property of hosts. The relay now requires an **owner** on every row and requires such rows to be **reported with that owner**, never dropped and never re-pointed at a file `.4` can reach.

**`P4-F2` — canon §5 fixes LESS than the fold's phrasing assumed, and the difference decides whether a row halts.** Read at head: ✅ fixed and citable — the sell slide's **50 px** host and **`.26 s`** fade, and the composer slide's **opposite slot** + **`translateX ±36 px` + fade**. ⛔ **The sell slide's DISTANCE is STRUCK, not fixed** — `translateY(110%)` was struck by the founder ruling of 2026-08-15 and written into canon §5 in place (the same `O-5` reason this amendment exists), because there is no replica footer to slide away. ⛔ **Neither instance has an EASING at canon §5 at all** ⇒ a row needing one cannot get it from canon and must not take it from a mockup: it is `H-VALUE`, reported whole and built not at all.

**`P4-F3` — the `Đ BET` cross-owner delta is canon-ANTICIPATED, and `.4` must not inherit it as a work order.** #341's PR body reports *"the colhead reads `Buy` and opens a composer that does not"* with `composer/copy.ts` allow-list-excluded on its side. But **canon §7 item 3 reads *"app-wide, **plus the W2.8 entry relabel**"*** — so after #341 lands, `COMPOSER_COPY.header`/`.submit` staying `Đ BET` is **canon-correct**, not a residue. §7's no-edit row **stands unamended** and `.4` files no row against it. ⚠ **Second instance of §14's `O-10` candidate** (*a routed item inherits its location, not its defect class*), arriving from a different direction than `PD-4-06`'s — there a routing carried a class that did not reproduce; here a neighbouring PR's own finding would have. **Two instances, not three.** ⛔ Still not numbered — O-space mints belong to the founder.

**`P4-F4` — the tier-4 close-out gap is one mockup wide, not the whole set.** `.4`'s tier-4 set is four mockups + the W2.11 state kit, each pinned in §12's SETUP by md5 + line count. `W2_8` and `W2_11` both **have** close-outs; `W2_10` is the sole exception. Worth stating because *"the sell mockup has no close-out"* reads as a hole in `.4`'s tier-4 authority generally, and it is not.

**`P4-F5` — A5's own `R20` row falsified `POLISH-0.md` §0's closing tally, in the same commit.** Handled in §5 below.

---

## 5 · Surprises caught + fixed in-session

**S-1 · `O-9` fired mid-commit, on a citation I was in the middle of editing — and the rider is in that commit.** A3's job was to re-fence the Cross-surface cell's `Đ BET` coordinate. Rider 3 requires re-reading a cited section at then-HEAD before saving, so I read `design-canon.md` §7 item 3 — and found the **Tier 2 cell quotes it as *"Đ BET app-wide"* while canon reads *"app-wide, plus the W2.8 entry relabel."*** The omitted clause is load-bearing: without it, #341's `Buy` relabel reads as a defect against canon that `.4` inherits, when canon anticipates it. **Corrected at the site, in A3, not deferred.** This is `O-9` working exactly as written — *if the prose you are editing contains a `§`, go read that document at HEAD before you save.*

**S-2 · Filing `R20` falsified the very sentence that warns against computing a tally.** `POLISH-0.md` §0 closed *"5 SCHEDULED · 14 RULED · **zero OPEN** … **Nothing in this index stops work**"* — three clauses that go false the moment `R20` lands `OPEN`. The correction was written **INTO** that sentence (`O-5`), in the same commit, with the superseded claim struck rather than deleted. ⚠ **And the tally was RE-MEASURED, not incremented** — 20 rows read, states counted off the amended table: **14 RULED · 5 SCHEDULED · 1 OPEN**. The sentence itself says why: *"A tally is a measurement; computing it is how it drifts"*, and it records that it had **already drifted twice**. Arithmetic would have been the third drift, committed by the row filed to end the first.

**S-3 · The §A admit-check's own section-sequence leg went stale from the edit that added §15.** The leg read *"`§A` then `§0`…`§14` — contiguous."* Adding §15 falsified the plan's own receiving-lane gate. Updated, plus a new amendment leg and a note stating the general case: **a self-describing leg goes stale from the very change it describes** — `O-8`/`O-9`'s shape, one register over.

**S-4 · A fresh worktree had neither `node_modules` nor `mise` trust.** `just verify` first exited **1** on `mise ERROR … not trusted`. Fixed with `CI=true pnpm install --frozen-lockfile` and `mise trust` for this directory. ⛔ **No real `.env*` was read, copied or written** — the build ran under the placeholder env `tests/_setup/env.ts` already ships, with `ZUGZWANG_ENV=preview`. Env facts, not regressions.

---

## 6 · Verification

⚠ **`just verify` green is NOT a receipt about this diff, and that is stated rather than discovered** (plan §8 row 1). Biome 2.4.13 has **no markdown support** and `ignoreUnknown: true`; the run reports *"Checked **705** files"* and **not one of them is a file this PR touched.** A markdown-only diff checks **zero** files. **The receipt is the vitest result.**

| Gate | Result |
|---|---|
| `pgrep -f 'node.*vitest'` before the run | **none** — no concurrent runner, so any RED is real (`H12`) |
| `ZUGZWANG_ENV=preview just verify` (typecheck → biome → next build) | **EXIT=0** · *All checks passed* · `Compiled successfully` |
| `pnpm vitest run` (full suite, backgrounded, run directly — not via `just`, which points at the cloud DB) | ✅ **EXIT=0** — **Test Files 346 passed \| 1 skipped (347)** · **Tests 3190 passed \| 1 skipped \| 4 todo (3195)** |
| `git status --porcelain` at close | clean |

Every gate ran as its **own statement with `echo EXIT=$?` immediately after and nothing trailing** — a trailing command owns the compound exit whether the joiner is a pipe or a `;`.

**No test was added, and none was expected:** PR A is `docs/**` only. §8 row 1 predicts *"none — doc-only"* for `PD-4-01` · `-02` · `-03`, and the routed rows change no behaviour.

---

## 7 · Decisions made

1. **The plan and its fold land as ONE commit (A0).** A fold committed *after* the record acts it re-scopes would be a plan describing work already done. The commit subject says so.
2. **`P4-F<n>`, never a bare `F-n`.** Three registers already carry an `F-n`. `CLAUDE.md` §8 exists because of exactly this, one prefix over.
3. **No `PD-4` id minted by the amendment.** §3's range is closed at eleven and §A pins it; parity rows mint as `P4-D<nn>` / `P4-P<nn>` at the recon. Widening the register range to hold amendment findings would have broken the admit-check leg for no gain.
4. **The `INV-3` label is NOT carried into the `RR-3` adoption record.** `POLISH-3-PR-2.md` struck it — INV-3 is a *storage* invariant and a Tailwind class cannot violate it. The rule actually broken is the design-language axis correction + `AGENTS.md` §8. §4.2 **S2**: a supersession voids a rationale, but the delta held on other grounds, so the new ground is stated and the row is not deleted.
5. **`PD-4-07` lands in no PR.** A blocked row has a register entry only. Giving it a PR slot would imply it is schedulable; it is not, until `OQ-1` lands as a SPEC.1 line.
6. **`CLAUDE.md` §1's stale SPEC.1 version was left alone.** It reads 1.0.29; live is 1.0.30. **#341 has already declared that correction as a rider on its own C15**, and `CLAUDE.md` is not in `.4`'s six-path `docs/**` allow-list. Observed, reported here, **not fixed** — fixing it would collide with a declared rider on an open branch.

---

## 8 · Open questions

1. **`R20` needs its founder ruling** — the Đa staked-basis SPEC.1 line. ⛔ **Web-authored; CC never drafts it.** It blocks `PD-4-07`, the `Đa → Đb` grammar at three components, and `H-OQ1`.
2. **`R11` / `PD-0-12` still has no disposition** though `.5` has run three PRs. Routed, not `.4`'s to rule.
3. **`PD-2-35`-style question for `PD-4-04`:** the `OD-2` preset defaults byte-identical, so PR C ships **zero visual change** until a tier is chosen. **Which tier?** `.1`'s four `shell/` sites are routed out, `.3`'s tier is ⛔ excluded, and three tiers live in `composer/`. The preset makes the choice *addressable*; it does not make it.
4. **Does the `.4` delta recon's row set collide with #341's?** #341's own §3 marks rows **30** and **31** *"⛔ OUT — POLISH.4. Not planned, not scaffolded for"* (composer hosting) and row **32** out to the bet engine. Those are `.4`'s inheritance from the d5 lane and the recon should expect them. **Not resolved here** — flagged so the recon is not surprised by them.

---

## 9 · Next session starts at

**The delta recon (`OD-1`), per plan §12 — commissioning BOTH row sets.** ⚠ A second session is already running it in parallel from the same instruction; §12 is the durable copy and the two are required to **agree**. ⛔ **On any divergence the recon REPORTS it and does not silently reconcile** — a relay that quietly conforms to whichever copy it holds destroys the only evidence that two copies existed.

Then, and only then: **PR B (layout + behaviour)** → **PR C (values)**, in that order.

⛔ **PR A does not merge before Gate C.**

---

## 10 · Context to preserve

- **This worktree needs three things a fresh checkout does not have:** `CI=true pnpm install --frozen-lockfile`, `mise trust`, and the `tests/_setup/env.ts` placeholder env exported with `ZUGZWANG_ENV=preview`. None is a regression.
- **`just verify` green means nothing about a markdown diff** — 705 files checked, zero of them this PR's. Always read the vitest result instead.
- **`SlotHeader.tsx` and `suspended-modal.test.tsx` remain annexed.** Re-measured at `8db535d`: #341 touches 54 files and intersects `.4`'s population at **exactly those two**. Unchanged from the authoring measurement at `35da436`. The allow-list is finalised **after #341 lands**.
- **`P4-F1` is the finding to carry forward.** Before filing any parity row, ask which file *declares* the arrangement. On this surface the answer is very often `.3`'s or `.5`'s.

---

## 11 · Time

One session, 2026-08-16. Six commits, one PR.

---

## SEALED SELF-ASSESSMENT — written last, unamendable

**The most valuable thing this session produced was not asked for.** The kickoff named the sell slide and the composer's opposite-slot open as the two in-scope behaviour instances. Measuring them to write §10 honestly is what turned up `P4-F1` — that **both** of them are declared in files `.4` may not edit, one of which (`PositionsTable.tsx`) the plan already fences read-only for a different reason. Had I taken the kickoff's naming as sufficient and written the halt set from it, §12 would have commissioned two row sets whose most prominent rows `.4` cannot execute, and the recon would have discovered that instead — one lane later, with rows already filed. **The instruction to read all four passes before writing a word is what made that finding reachable**, and I want to be plain that I did not derive it from the reading alone: I derived it from the reading *plus* going back to the source to check what the kickoff asserted.

**I corrected the founder's phrasing twice and both are worth the founder's eye.** The kickoff says canon §5 fixes the sell slide's *"distance, duration, easing and its fixed 50px non-reflowing box."* Canon fixes the box and the duration; it **struck** the distance at the founder's own 2026-08-15 ruling, and it states **no easing for either instance**. And it names Discovery's `P-8` carve-out, which is **not on `main`** — I could cite it only as a thing to exclude, never as a thing to apply. If either correction is wrong, the halt set is where it shows.

**What I am least sure of is the B/C split's boundary, not its order.** `PD-4-05` (`c2Strip`) went to B because it is a source-shape removal and no value moves; `PD-4-06` went to B because its deliverable is a three-file zero-movement proof, which is a behaviour claim about code that does not change. Both are defensible and neither is obvious. If the founder reads B as *arrangement only*, both belong elsewhere and it is a one-line move in §4.

**What I did not do.** I did not run the delta recon, I did not touch a single `src/` or `tests/` file, and I did not verify anything about how this surface *renders* — no browser, no measurement, no compiled CSS. Every prior pass in this family records that resolved geometry is provable only in a browser and that jsdom cannot see it. **PR A contains no claim about geometry**, which is the only reason that absence is acceptable here; PR B and PR C will not have that excuse.

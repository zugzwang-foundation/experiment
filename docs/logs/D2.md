# D2 — session log (CI-gate stratum)

**Stratum:** D2 — "CI becomes the gate" · **Branch:** `feat/d2-ci-gate` · **State:** PR-ready (awaiting operator merge + checklist) · **Date:** 2026-06-26

## What landed (files + PR#)
PR: `feat/d2-ci-gate` → `main` (number assigned at open; canonical SHA = the squash-merge SHA on `main`).
- **Ratified plan (commit 1, `ca7a55e`):** `docs/plans/D2.md` (DRAFT→RATIFIED; OD-1..OD-4 LOCKED; two web additions baked in).
- **Implementation (commit 2):**
  - `scripts/ci-env-parity.ts` — **NEW**. Pure `auditEnvParity(...)` + `findingsTotal(...)` (unit-tested) + thin Vercel/Doppler REST I/O. OD-1 scope: (a) orphans / (b) missingRequired / (c) unhealthySyncs / (d) duplicateSyncs. Fail-closed on API/parse error + missing env; reads env-var KEYS / sync METADATA only — no secret value read or logged. Entrypoint-guarded so importing the pure fns never fires I/O.
  - `tests/unit/ci-env-parity.test.ts` — **NEW** (13 tests, RED-first via `@test-writer`; GREEN).
  - `.github/workflows/ci.yml` — **+`drizzle-kit check`** (offline, before the pg_cron strip) **+`db:check-drift`** (after migrate; timestamp+count, UNCHANGED method).
  - `.github/workflows/env-audit.yml` — **NEW** (scheduled daily + `workflow_dispatch`; fails on any finding; OD-2 — off the merge path).
  - `.github/workflows/staging-migrate.yml` — **NEW** (`on: push:[staging]`; `doppler run --config stg -- pnpm db:migrate:staging`; no pg_cron strip; **INERT until D3**).
  - `package.json` — **+`ci-env-parity`** script.
  - `docs/adr/0024-…md` — in-place **Patch P1** (one patch, two notes: OD-3 file-map line → `ci-env-parity.ts`; OD-2 cadence = scheduled-not-per-PR + rationale). File-map rows for CI gate + secret audit updated, same commit.
  - `AGENTS.md` — closing ritual: §11 "what is enforced" (+ required `ci` check, + the two new CI steps, + the two non-gating workflows); §3 tree ("the ONLY workflow" → three workflows).
- **Session log (commit 3):** `docs/logs/D2.md` (this file).

## Decisions made
- OD-1..OD-4 executed exactly as ratified (see `docs/plans/D2.md` → Ratified decisions). Uploads-bucket allow-list key = **`R2_BUCKET_UPLOADS`** — read from `src/server/storage/r2.ts:45`, not guessed.
- **Orphan/required separation** (TDD-driven): required keys are excluded from (a) orphan detection — they are owned by (b) missingRequired — to prevent a Doppler-missing-required-key double-counting as both orphan + missing. Surfaced by the RED loop (the missingRequired test asserted `findingsTotal === 1`), implemented to the `@test-writer` contract.
- **OD-4 provisioning split:** 3 tokens as GHA **secrets** (`VERCEL_API_TOKEN`, `DOPPLER_AUDIT_TOKEN`, `DOPPLER_TOKEN_STG`); 3 non-secret IDs as GHA **vars** (`VERCEL_PROJECT_ID`, `VERCEL_TEAM_ID`, `DOPPLER_PROJECT`) — least-privilege; OD-4 allows "secrets or vars".
- `@code-reviewer` (MANDATORY, WEB ADDITION 1): **clean on all four priorities** (no value logging / tight token handling / fail-closed / pure-fn correctness). Two LOW notes, both non-blocking — one is a reviewer misread of a lazy ternary; one is the by-design, comment-documented, fail-closed Doppler sync normalizer (operator-validated on first run).
- No `db-migration-reviewer` (no schema/migration) and no `security-auditor` (not critical-path; the credential surface was covered by the mandatory `@code-reviewer`'s token-handling focus).

## Surprises caught + flagged in-session (§5.10)
- **AGENTS.md §6 migration-head is STALE.** The `db:check-drift` self-audit proof shows the real committed journal head = **`0018_drop_friendly_fire_events`** (19 entries), but AGENTS.md §6 still reads "Current head: `0016_mod_actions_reason.sql`" (last synced at SYNC.8 — 0017/0018 from DEBATE.8/9 landed after). **Out of D2 scope** (D2 adds no migration) → NOT fixed here (§5.4); recorded in `claude-progress.md` for the next combined SYNC/doc sweep. My `D2.md` §5 inherited the stale "0016"; the load-bearing claim ("D2 adds none") holds — real head is 0018.

## Open questions
- None blocking. The Doppler `/v3/configs/config/syncs` exact field names (sync `status` / Vercel-target `scope`) are **operator-validated on the first `env-audit` `workflow_dispatch` run** (the (c)/(d) normalizer is fail-closed until then — an unparseable sync resolves to `unknown`/flagged, never `in_sync`). (a)/(b) ride stable Vercel-env + Doppler-secret-name shapes.

## Next session starts at (exact next action)
- **Operator completes the D2 PR checklist** (in the PR description): (1) provision the 3 secrets + 3 vars (OD-4); (2) merge; (3) add required status-check context **`ci`** on `main` (confirm required-reviews stays 0; `enforce_admins` stays on → operator also bound, intended); (4) run `env-audit` once via `workflow_dispatch` → confirm zero findings on the current In-Sync topology.
- Then **D3** un-inerts `staging-migrate.yml`: create the `staging` branch, repoint the Vercel staging `branchMatcher` (equals `staging`), toggle Production auto-assign-domains. Do NOT do any of that in D2.

## Context to preserve
- `staging-migrate.yml` is **INERT until D3** (no `staging` branch exists — it never fires). Do NOT create the branch in D2.
- `check-drift`'s method is UNCHANGED (timestamp+count, CI-safe — the pg_cron strip drops statements, not journal entries); per-hash is `/api/health`-only (shipped D1).
- `env-audit` is **scheduled** (off the merge path), NOT a per-PR gate — only `ci` is the required check.
- Gates green: `ZUGZWANG_ENV=preview just verify` ✓; full `pnpm vitest run` = 1041 passed / 2 skip / 5 todo ✓; 3 workflow YAMLs parse ✓; `drizzle-kit check` + `db:check-drift` proven green locally ✓.

## Time
- Single session, 2026-06-26.

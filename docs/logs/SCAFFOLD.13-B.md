# SCAFFOLD.13-B — Credential rotation + Doppler-canonical secrets store cutover

**Status:** Closed 2026-05-20
**Branch:** `feat/scaffold-13-b-execute-closeout` (from `main` at `c8e8e7e`; 3 commits: `e3c8f33` close-out core + `b814dcf` auditor absorption + PR# update commit)
**PR:** [#43](https://github.com/zugzwang-foundation/experiment/pull/43)
**Predecessor:** SCAFFOLD.13-A (#40, post-merge SHA `7167397`); plan promoted at PR #41; Round 5 amend15 PR #42 merged 2026-05-18 (`main` at `c8e8e7e`)
**Unblocks:** Future credential-rotation strata inheriting the `docs/maintenance.md` §5.11 routing extension; testnet phase secrets handoff; SCAFFOLD.12 (custom domain attachment per SURPRISE 3); SCAFFOLD.3-FOLLOWUP-2 candidate (Better Auth session endpoint route mounting per SURPRISE 7)

## What landed (§5.9 field 1)

- **Phase A (rotations, 2026-05-18):** 5 leaked secrets rotated per plan §3 + §5 — `BETTER_AUTH_SECRET`, `ADMIN_PASSWORD`, `GOOGLE_CLIENT_SECRET`, `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`. A6 smoke PASS (sign-in 200 + admin login). IMMEDIATE old-secret disablement performed per Amendment 6 / Q4 verdict (Google + Resend in-session; Turnstile auto-invalidated at 2h grace). `/tmp/scaffold14-env.txt` + `/private/tmp/scaffold14-env.txt` removed (A1). `.env.local` chmod 600 + content updated with 5 rotated values (A2 + A4.5 per Amendment 11).
- **Phase B (Doppler integration, 2026-05-20):** Doppler `zugzwang-experiment` project minted; `prd` config populated with 12 application keys via `doppler secrets upload .env.local` (one-off transient, per SURPRISE 4). Doppler `dev` config existed briefly with `dev_personal` auto-creation (SURPRISE 1) then deleted entirely by operator (SURPRISE 2) — final Doppler state = 1 project × 1 config (`prd`). B5a + B5b + B5c PASS; B5c clean-slate confirmed via `vercel env ls production` → 0 keys; window-discipline observed (no deploys triggered B5c → B6a-smoke PASS). B6a + B6a-smoke (4-step gate including Sensitive-flag literal-value assertion per Amendment 18 / WI4 AMBIGUOUS resolution) + B6b + B7 (activity-log inspection per Amendment 9) + B8 + B9 all PASS. Q9 (a) abandon NOT triggered — Doppler default-Sensitive claim CONFIRMED in operator environment.
- **Vercel runtime state post-cutover:** 12 application keys + 3 `DOPPLER_*` metadata vars (SURPRISE 6), all `Encrypted`/Sensitive, scoped to Production + Preview via 2 Doppler→Vercel integrations from single `prd` config.
- **Mid-execute remediations absorbed:** DATABASE_URL drift fix (SURPRISE 9) + ADMIN_PASSWORD re-rotation (SURPRISE 10) + BETTER_AUTH_URL surgical fix (SURPRISE 3). Each absorbed per CLAUDE.md §7 cleanup absorption rule (<2h items roll into the stratum that surfaces them).
- **`.env.example` drift fix landed** (line 3 swap per plan §1.3 + §1.1).
- **`docs/maintenance.md` "Routing extensions to CLAUDE.md §5.11" sub-section added** per plan §9 Q5 RESOLVED verdict / Amendment 7; "Last revised" footer updated.
- **`docs/plans/SCAFFOLD.13-B.md`** canonical post-Round-5 (commit `32d151e` via PR #42); not modified at close-out (matches 13-A precedent).
- **`docs/logs/SCAFFOLD.13-B.md`** (this file).

## Decisions made (§5.9 field 2)

- **IMMEDIATE old-secret disablement** (Google + Resend) per Amendment 6 / Q4 verdict — no 24h dual-active window; threat model post-/tmp-exposure does not tolerate residual exposure. Tracker MAINT-row at 2026-05-18 IST DROPPED (Amendment 6 absorbs).
- **Q1 mitigation REPLACED with B5c clean-slate** per Amendment 16 / B0 yield Behavior-(a) CONTRADICTED. Doppler→Vercel name-collision behavior is REJECT (not converge); plan's "byte-identical values → collision-irrelevant" framing was structurally invalid. New B5c clean-slate step + operator-side window discipline (no deploys B5c → B6a-smoke PASS) honored.
- **Q9 PRE-COMMITTED Option (a) abandon NOT triggered.** B6a-smoke step 4 (literal-value assertion `vercel env ls production` showing all 12 keys + 3 `DOPPLER_*` as `Encrypted`) PASS. WI4 AMBIGUOUS Sensitive-flag posture RESOLVED as **CONFIRMED-Sensitive** in operator's environment; Vercel community thread 38766 non-Sensitive report NOT reproduced. Doppler default-Sensitive claim holds.
- **B7 redesigned to activity-log inspection** per Amendment 9 / Finding 4 resolution — no probe-key add/delete; zero verification-triggered production redeploys.
- **Q5 mid-execute reversal absorbed:** operator deleted entire Doppler `dev` environment at B2 (per SURPRISE 2). Amendment 4's "dev = key-names-only placeholder" provision dropped from final state. `docs/maintenance.md` §5.11 routing extension testnet/mainnet inheritance value reduced (anchor on `dev` config existence is now historical-only).
- **Q7 scope one-off:** `doppler secrets upload .env.local` adjudicated as transient one-off write at B4, not standing CLI surface. Narrow allowlist (`Bash(doppler secrets list)` + `Bash(doppler configs list)` only) per Amendment 8 / Q7 verdict preserved.
- **Reviewer-call invocation summary:**
  - Phase 1 plan-review (Round 3 surface) `security-auditor`: PASS-with-conditions → 2 MEDIUM + 2 LOW + 1 meta absorbed via Amendments 9-13.
  - Phase 1 plan-review (Round 5 surface) `security-auditor`: HIGH + 4 MEDIUM + 5 LOW absorbed via amend15 loop-2 (commit `32d151e` via PR #42).
  - Phase 2 execute close-out `security-auditor` (this PR): per plan §6 + Amendment 7 pattern-extension via `docs/maintenance.md`. Output captured inline below.

## Surprises caught + fixed in-session (§5.10 — 12 entries)

1. **Doppler personal-config auto-creation (B2 datapoint refinement).** At Doppler project mint, the `dev` environment auto-created a `dev_personal` child config — a Doppler-default behavior surfacing personal-config-per-developer pattern that doesn't intersect this stratum's Path-A-locked posture (no `doppler run` for local dev; `.env.local` continues as dev secrets file). Removed during the Q5 mid-execute reversal (SURPRISE 2). **Forward-relevance:** future plans re-introducing a Doppler `dev` env should anticipate `dev_personal` auto-creation; §0.4 pre-verification pattern should add a config-tree-defaults row for Doppler.

2. **Q5 mid-execute reversal — operator deleted entire Doppler `dev` env.** After `dev_personal` auto-creation surfaced (SURPRISE 1), operator deleted the entire Doppler `dev` environment at B2 rather than retaining `dev` with placeholder values per Amendment 4. Final Doppler state: 1 project (`zugzwang-experiment`) × 1 config (`prd` only). Amendment 4's "key-name inventory reference" purpose (drift-detection visibility) lost; `docs/maintenance.md` §5.11 routing extension survives, but its Amendment 4 anchoring on `dev` config existence is now historical-only. **Forward-relevance:** future plans should expect Doppler `dev` provisioning to be operator-deletable at execute-time; testnet/mainnet inheritance value of the §5.11 routing extension is reduced (pattern recognized, but `dev` config no longer the carrier).

3. **Pre-existing production-config drift on `BETTER_AUTH_URL` + DNS state confusion.** At B5a non-secret eyeball-compare (4-key subset), surfaced `BETTER_AUTH_URL` drift: both Vercel and Doppler held `http://localhost:3000` (local-dev default from `.env.example` line 26) instead of the production Vercel deploy URL. Vercel-side surgically corrected to `https://experiment-zugzwang-worlds-projects.vercel.app`. Custom-domain attachment (true production URL like `experiment.zugzwang.world`) is SCAFFOLD.12 scope and deferred. **Forward-relevance:** `.env.example` defaults marked `http://localhost:3000` for `BETTER_AUTH_URL` silently propagated into production — future plans should treat any `localhost`-defaulted env var in `.env.example` as drift-sensitive and check production parity at vendor-cutover time, not at first-customer time.

4. **Q7 scope one-off — `doppler secrets upload .env.local` for B4 bulk upload.** Operator used `doppler secrets upload .env.local` to bulk-upload at B4 rather than 12 individual paste operations per Amendment 1's copy-paste discipline. Adjudicated as one-off transient write (not standing CLI surface). Q7 narrow-allowlist intent preserved (`Bash(doppler secrets list)` + `Bash(doppler configs list)` only). **Forward-relevance:** future plans should NOT codify `doppler secrets upload` as a standing pattern; one-off bulk upload from `.env.local` is acceptable for execute-phase bootstrap only — and carries the drift-detection cost shown by SURPRISE 9 (DATABASE_URL slipped through B5a/B5b because bulk upload skipped value-correctness check).

5. **No `.vercel/project.json` in repo root** (CC-surfaced finding at B5c diagnostic). Repo at `~/code/zugzwang/experiment` has no `.vercel/project.json` file. The `vercel` CLI was resolving the project via directory-name + user-context inference, not via explicit local link. Resolution happened to be correct (`vercel whoami` agreement + dashboard URL match), but the structural gap exists. Not remediated in-execute (write operation, out of close-out scope). **Forward-relevance:** future credential-rotation plans should add `.vercel/project.json` presence + content verification as a §0.4 pre-verification pattern row; future SCAFFOLD work running `vercel env ls` for inventory should validate the linked project ref before treating output as authoritative.

6. **3 unexpected `DOPPLER_*` metadata vars synced into Vercel.** Post-B6a integration enable, `vercel env ls production` surfaced 3 env vars beyond the 12 application keys in §0.2 inventory: `DOPPLER_PROJECT`, `DOPPLER_ENVIRONMENT`, `DOPPLER_CONFIG` — auto-injected by the Doppler→Vercel integration as metadata pointers (let the running app reference Doppler at runtime). All 3 `Encrypted`/Sensitive; harmless. Not predicted by plan §0.2 inventory. **Forward-relevance:** future plans wiring Doppler→Vercel should anticipate 3 extra `DOPPLER_*` metadata vars appearing post-integration-enable; `.env.example` need NOT mirror them (integration-managed, not user-set), but inventory verifications should expect N + 3 keys.

7. **Plan §4 B6a-smoke step-3 `/api/auth/session` heuristic unverified against codebase reality.** Plan prescribed `curl -I /api/auth/session` → expect HTTP 200 (per Amendment 3, if admin auth was DECOUPLED from Better Auth). At B6a-smoke execute, the endpoint returned 404 — the actual Better Auth route mounting in this codebase likely uses `/api/auth/get-session` or a different path. Investigation deferred from B6a-smoke (functional smoke covered by step 2 admin login). **Forward-relevance:** smoke-test prescriptions in plan-mode should be pre-verified against actual codebase route mounting before plan-mode prescribes them; Amendment 3's heuristic was derived from Better Auth doc defaults, not from `src/server/auth/` route surface in this codebase. **Tracker candidate:** SCAFFOLD.3-FOLLOWUP-2 — separate from existing SCAFFOLD.3-FOLLOWUP-1 (different specific manifestations of Better Auth route discipline; FOLLOWUP-1 is Content-Type 415 on Email-OTP POST, FOLLOWUP-2 is session-endpoint route mounting).

8. **B6b verification asked operator to confirm Vercel-side scope change but NOT Doppler-side sync count** (symmetric pre-verification gap). When operator's confirmation could plausibly reflect either "new sync added" or "existing sync edited," the gap allowed the Add Sync replacement bug (SURPRISE 12) to slip through. **Forward-relevance:** plan-mode prescriptions for multi-source-multi-target syncs (e.g., 1 Doppler config → 2 Vercel environments) must specify verification on BOTH surfaces — Vercel-side scope confirmation AND Doppler-side count assertion. Symmetric pre-verification rule (per Q8 meta-lesson) applies to integration-config mutations, not just to vendor-doc claims.

9. **`DATABASE_URL` drift undetected through B5a/B5b; surfaced at B6a-smoke runtime.** B6a-smoke step 2 (admin login) failed; Vercel runtime logs surfaced `ECONNREFUSED 127.0.0.1:54322` — post-B6a Production runtime was hitting the local Supabase CLI default port (54322) instead of the PROD pooler. Root cause: `.env.local` carried the local-dev `DATABASE_URL` value (Supabase CLI default port 54322 from operator's local Supabase instance), which was bulk-uploaded to Doppler `prd` at B4 (per SURPRISE 4 transient pattern) without value-correctness check. B5a non-secret eyeball-compare didn't catch it (DATABASE_URL not in 4-key non-secret subset); B5b backup verification (presence check, not value-correctness) didn't catch it either. Surgical fix: operator edited Doppler `prd` `DATABASE_URL` to PROD pooler URL; integration re-synced to Vercel. **Forward-relevance:** at any future B4-class state-mismatch correction surfacing (this session: `BETTER_AUTH_URL` per SURPRISE 3 surfaced before this one), full re-audit of all keys for same dev-uploaded-to-prod drift pattern is **mandatory, not optional**. If even one B5a non-secret eyeball comparison surfaces drift, expand B5b verification to value-correctness on all 12 keys.

10. **`ADMIN_PASSWORD` exposed in plaintext via Doppler dashboard screenshot during B5b/Phase-1 drift detection.** Vendor UI revealed the value when operator clicked the eye icon for the row; screenshot captured the plaintext. Rotated mid-execute via fresh `openssl rand -hex 32` → pwd manager → Doppler `prd` → `.env.local` (Phase 0 of mid-execute remediation). **Cumulative pattern:** this is the 4th/5th secrets-discipline incident across SCAFFOLD.13 phase (preceded by 13-A SURPRISE 4 credential-leak + 13-A SURPRISE 7 `.claude/settings.local.json:129` literal + 13-A SURPRISE 8 `/tmp/scaffold14-env.txt`; citations verified against `docs/logs/SCAFFOLD.13-A.md` §3). **Forward-relevance:** operator-side diagnostic prescriptions touching secrets-management dashboards must include explicit no-screenshot-with-reveal guardrails; vendor dashboards with eye-icon reveal UIs (Doppler, Vercel non-Sensitive vars, Resend at API-key creation, Google OAuth client-secret display) should be treated as "reveal only if you're prepared to rotate" surfaces.

11. **Doppler→Vercel auto-redeploy NEVER observed firing across the session.** All Production redeploys (B6a initial sync + DATABASE_URL surgical fix + ADMIN_PASSWORD re-rotation + BETTER_AUTH_URL surgical fix + B6b initial Preview sync) required manual operator trigger via Vercel dashboard. Plan §0.4 pre-verification row claimed "auto-redeploy is the mechanism, not manual sync" (from Doppler search results 2026-05-17); this claim did NOT hold in operator's environment. Possible explanations: (a) auto-redeploy fires only for non-Sensitive vars; (b) propagation lag exceeded session observation window; (c) Doppler doc claim doesn't match Vercel-side platform behavior; (d) integration-specific setting wasn't enabled. **Forward-relevance:** future testing should explicitly wait ≥10 min after Doppler edits before assuming auto-redeploy is absent, AND/OR consult Doppler + Vercel docs (community channels) for canonical answer on Sensitive-var redeploy triggers. Manual redeploy is the operational fallback regardless.

12. **Doppler "Add Sync" workflow at B6b inadvertently replaced existing B6a Production sync.** When second sync was added (Preview at B6b), the original Production sync (B6a, 12:16 PM) appeared to be replaced. Recovery: operator re-created Production sync via Add Sync again (1:13 PM). Doppler Activity log shows: Production sync 12:16 PM → Preview sync added 1:10 PM → Production sync re-created 1:13 PM. Specific gesture causing replacement was not isolated; UI behavior on multi-sync addition unclear. **Forward-relevance:** future plans adding multiple Doppler→Vercel environments should verify TWO syncs visible in the Integration connection page IMMEDIATELY after creating each one, not at end of sequence (reinforces SURPRISE 8 symmetric-pre-verification gap).

## Open questions (§5.9 field 3)

None at close — all 12 surprises resolved or operator-adjudicated in-session. SURPRISE 7 (Better Auth session endpoint route mounting) is a tracker candidate (SCAFFOLD.3-FOLLOWUP-2, separate from FOLLOWUP-1 per operator adjudication 2026-05-20), not a 13-B open question.

## Next session starts at (§5.9 field 4)

Operator's call per tracker queue. Candidates:

- **SCAFFOLD.3-FOLLOWUP-1** (Better Auth Content-Type 415 bug on Email-OTP POST per 13-A §0 finding 5) — existing tracker entry.
- **SCAFFOLD.3-FOLLOWUP-2** (Better Auth session endpoint route mounting per this stratum's SURPRISE 7) — fresh tracker entry; NOT bundled with FOLLOWUP-1 (different specific manifestations of Better Auth route discipline).
- **SCAFFOLD.12** (custom domain attachment) — required to retire `BETTER_AUTH_URL = experiment-zugzwang-worlds-projects.vercel.app` to a stable production URL (per SURPRISE 3 deferral).
- **ENGINE.6** (events helper at `src/server/events/insert.ts`) per pre-13-A queue ordering.
- **Tracker-sweep v9 → v10:** deferred per cadence rule (≥3 strata since v9; current count = 2 with 13-A + 13-B closed). Likely after ~SCAFFOLD.15.

## Context to preserve (§5.9 field 5)

- **Doppler workspace:** 1 project (`zugzwang-experiment`) × 1 config (`prd`) × 2 syncs (Vercel Production + Preview). Final state post-Q5-reversal per SURPRISE 2.
- **Vercel runtime state:** 12 application keys + 3 `DOPPLER_*` metadata vars, all `Encrypted`/Sensitive; scoped Production + Preview.
- **`BETTER_AUTH_URL`** currently set to Vercel default deploy URL (`https://experiment-zugzwang-worlds-projects.vercel.app`); custom-domain attachment deferred to SCAFFOLD.12 (SURPRISE 3).
- **`DATABASE_URL`** points to Supabase PROD Mumbai pooler (post-13-A rotation + post-13-B drift correction per SURPRISE 9). Session pooler port 5432, IPv4 + prepared-statement support.
- **Doppler→Vercel auto-redeploy** NEVER observed firing this session — assume manual redeploy required until disproven (SURPRISE 11 forward-relevance). Operator must manually trigger Vercel redeploy after Doppler var changes.
- **5 rotated credentials** in operator password manager (rotation set per Phase A); old vendor-side secrets disabled per IMMEDIATE policy (Google + Resend), Turnstile auto-invalidated at 2h grace.
- **Q9 (a) abandon NOT triggered** — Doppler default-Sensitive claim CONFIRMED in operator environment at B6a-smoke step 4. WI4 AMBIGUOUS resolved as CONFIRMED-Sensitive.
- **/tmp hygiene:** `/tmp/scaffold14-env.txt` + `/private/tmp/scaffold14-env.txt` removed (A1). Recheck at any future SCAFFOLD/HARDEN open via `find /tmp /private/tmp -name '*env*' -mtime +0`.
- **`.env.local`:** mode 600, gitignored; contains 5 rotated values + DATABASE_URL PROD pooler + ADMIN_PASSWORD re-rotation + BETTER_AUTH_URL surgical fix.
- **No `.vercel/project.json`** — SURPRISE 5 forward-relevance carries for future Vercel CLI work.

## Time (§5.9 field 6 — optional)

Multi-session: 2026-05-17 (plan rounds 1-5 with security-auditor adjudications) → 2026-05-18 (Phase A rotations + amend15 plan absorption + PR #42 merge) → 2026-05-20 (Phase B execute B5a-B9 + close-out PR work). Aggregate ~10-12 hours across plan + execute combined.

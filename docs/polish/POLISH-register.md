# POLISH — Defect Register

> **Doc:** `POLISH-register.md` · web-authored from operator captures. Committed 2026-08-05. GitHub is canonical; PK is the mirror.
> **Status:** scaffolded 2026-07-30 IST at POLISH.0. **No surface has had a founder VISUAL inspection.** POLISH.2 · Discovery has had a **CC machine pass** (2026-08-09, §9 as amended) — mockup-against-source, which cannot see what only a render shows. The two are different events and the surface is closed by neither alone. *(Through 2026-08-08 this line read "No surface has been inspected", which was already stale: it was committed on 2026-08-05, two days after POLISH-1a shipped ten deltas against the shell.)*
> **Governed by:** `POLISH-0.md` §4 (schema) and §5 (routing). Read those first.

---

## How to use this

One row per defect. Web authors rows from the operator's captures; the operator ratifies dispositions; **only the founder may set `accepted-divergence`** (P12).

**ID:** `PD-<surface>-<nn>` — surface is the POLISH number (`3`, `7a`, `7b`). Stable forever. Never renumbered, never reused.

**The `baseline` field is the gate.** If you cannot name the tier and the document a thing violates, it is **not** a visual defect — it is class **S**. That test is what keeps this register from becoming taste.

**Class:** V visual · F functional · B backend gap · S spec gap · R ruling needed.
**Disposition:** `routed` · `superseded` · `data-blocked` · `duplicate-of-known` · `accepted-divergence`.
**Status:** `open` · `routed` · `fixed` · `verified` · `closed`.

⚠ **Schema is binding — see POLISH-0 §4.1.** `class` takes exactly one letter; `status` takes exactly one of five values; a row is **NEVER** appended below a blank line.

**At each surface close:** emit a batched summary row into the tracker. Never row-by-row.

**Row template — copy this:**

```
| PD-n-01 | <one-line title> | V | tier-2 · canon §10 R-5 | POLISH-n_<surface>_<state>.png | routed | open | — | <PR / tracker row / halt> |
```

---

## POLISH.1 · Shell + branded header/nav

*Machine phase **RUN** at #288 · #289 · #290; the founder pass has not. Gates: **B4 VOID** (withdrawn, not deferred — SPEC.1 v1.0.26 `:1498`) · **B8 STRUCK** from `.1` (SPEC-blocked on the unwritten §21.7 rider) · **B10 CLOSED** (`acc2e03`, #283). See `POLISH-TRACKER.md:19` and `:40–42`.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — | — |

## POLISH.7a · Auth surfaces

*Machine phase **COMPLETE** — PR #320, halted at Gate C; the founder pass has **not** run, so the surface is **OPEN**. No gates: B4 void · B8 struck · B10 closed never applied here (they are `.1`'s), and `POLISH-0.md` §6 · *The gate state* records `.7a` as gate-clear from the outset. **21 deltas classified · 8 shipped · 4 halted · 6 superseded · 3 data-blocked**; rows below allocated from the live high-water mark, first `PD-7a-*` on the file.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-7a-01** | Card content diverged from the W2.1 picker on four presentational axes — title copy, the stacked email row, the missing `.otp-icon` ring, and the back link's position | V | tier-4 · `DESIGN_W2_1_auth-modal_mockup-v0_3.html` `:313`, `:154`+`:318`, `:361`, `:360` | `6032ea3` | routed | fixed | mockup↔build drift, never inspected | **SHIPPED** — D01 · D03 · D07 · D14. Grouped: one disposition, one commit, four axes of the same drift |
| **PD-7a-02** | The phishing-safety line was not verbatim — *"If someone **does**"* where the design source reads *"If someone **asks you for it**"* | V | tier-4 · `auth-modal:371` | `6032ea3` | routed | fixed | transcription, not design | **SHIPPED** — D12. The build's own comment already called it "design-source copy" |
| **PD-7a-03** | The `role="alert"` callout was hand-rolled twice with the same class string | V | tier-2 · W2.11 — one state, one primitive | `6032ea3` · `3158f59` | routed | fixed | **primitive duplication (`PD-0-10`'s root cause)** | **SHIPPED** — D21, as `(auth)/_components/AuthAlert.tsx`. ⚠ Consolidating it dropped a `mt-3` at one of THREE call sites — caught by `@code-reviewer` H-1, fixed, and pinned where the class is emitted |
| **PD-7a-04** | No error boundary on `(auth)` — an uncaught throw escalated to the whole-document `global-error.tsx` | R | tier-2 · W2.11 kit P1–P7 · `POLISH-0.md` §7 criterion 4 | `6635201` | routed | fixed | no state file had ever been added to the group | **SHIPPED** — D20, ruled at R-C. `loading.tsx` deliberately NOT added (POLISH.2's `page.tsx:28-30` precedent) and the omission is pinned by a test |
| **PD-7a-05** | `my-auto` on the sign-in and otp Cards computed to zero — both Cards top-aligned at 93.24px instead of centring | F | tier-3 · `docs/logs/POLISH-1b.md:92` (measured 92px) | `5a11b38` → `1a41b0f` → `0002ace` | routed | fixed | `min-height:100%` resolves against a containing block whose SPECIFIED height is `auto` | **SHIPPED at the third attempt** — D19. ⚠ First landed on `src/app/layout.tsx`, which un-stuck `GlobalHeader` on every page taller than the viewport (`@code-reviewer` C-1, header top −62/−562/−578); reverted; re-landed on `(auth)/layout.tsx` under a line-scoped exception. See `docs/plans/POLISH-7a.md` §12 |
| **PD-7a-06** | The Google button carries no `.gmark` mark, and cannot obviously carry one | V | tier-4 · `auth-modal:314-316`, CSS `:149` | recon; `docs/plans/POLISH-7a.md` §2.1 (1) | routed | open | ratified TRUE-NEUTRAL system vs an external brand guideline | **HALT → `AUTH-GOOGLE-MARK`.** Founder decision, undated. Text-only is a shipping state, not a broken one |
| **PD-7a-07** | Code entry is one field where the ratified design locks a 6-box segmented input | V | tier-2 · `DESIGN_W2_1_CLOSE-OUT.md:60` · tier-4 · `auth-modal:212-216` | recon | routed | open | — | **HALT (H2) → `AUTH-OTP-FIDELITY`.** `UI-A7.md:99` pins the single-field binding byte-for-byte; a 6-box port needs new client submit logic |
| **PD-7a-08** | Resend has no cooldown — the mockup ships the control DISABLED with a timer | F | tier-2 · `DESIGN_W2_1_CLOSE-OUT.md:60` · tier-4 · `auth-modal:366-370` | recon | routed | open | — | **HALT (H2) → `AUTH-OTP-FIDELITY`.** Gates how often `sendVerificationOtp` fires against the server's own limiters. ⚠ Tier-1 `ADR-0033:21` specifies the affordance with NO cooldown clause — quoted, not resolved |
| **PD-7a-09** | `/onboarding`'s Continue is not disabled until the checkbox is ticked | F | **tier-1** · `SPEC.1.md:777`, spec-locked by `:805` | recon | routed | open | tier-1 requires client state on a page tier-3 ratified as a pure RSC | **HALT (H2) → `AUTH-ONBOARDING-GATE`.** ⚠ `SPEC.1.md:817` assigns this conformance check to `.7a` BY NAME. **Not unsafe today** — `required` blocks submission and `acceptTosAction` re-checks server-side |
| **PD-7a-10** | The picker's submit reads `Send code`; the mockup reads `Continue` | — | tier-4 · `auth-modal:321` | recon | **superseded** | closed | the mockup's Continue advanced to a Turnstile pane that does not exist | §4.2 **S2** — the delta's ground returns when the pane does. → `AUTH-TURNSTILE-WIRE` |
| **PD-7a-11** | The `.mfoot` *Terms · Privacy* consent line is absent from the picker | — | tier-4 · `auth-modal:323-325` | recon | **superseded** | closed | the page-level footer was withdrawn 2026-08-02 | Four independent grounds: B4 VOID (`SPEC.1` v1.0.26 `:1498`) · `/terms` and `/privacy` do not exist · `not-found.test.tsx:108-167` would go RED · `SPEC.1.md:807-817` replaced implicit acceptance with the explicit checkbox. **Closes `AUTH-CONSENT-LINE`, struck at R-D** |
| **PD-7a-12** | The OTP headline omits the echoed address the mockup shows | — | tier-4 · `auth-modal:362-363` | recon | **superseded** | closed | tier-3 put an EDITABLE email field on the screen | §4.2 **S3** — `UI-A7.md:98` pins the controlled `email` binding, so the address is already on screen as a control; echoing it duplicates it |
| **PD-7a-13** | The address renders as an editable input where the mockup shows a static echo | — | tier-3 · `UI-A7.md:98` | recon | **superseded** | closed | — | Tier 3 beats tier 4. Recorded so a later reader does not re-file it |
| **PD-7a-14** | *"Secured by Cloudflare Turnstile"* is absent from the OTP card | — | tier-4 · `auth-modal:372` | `docs/logs/UI-A7.md:15` | **superseded** | closed | — | Restoring it before the widget is real would be a **false claim to the user**. Lands with `AUTH-TURNSTILE-WIRE` |
| **PD-7a-15** | The error slot is a bordered callout where the mockup shows a centred inline message | — | tier-3 · `UI-A7.md:136` | recon | **superseded** | closed | — | Tier 3 maps invalid-OTP to the W2.11 callout treatment |
| **PD-7a-16** | The entire Turnstile pane — three ratified states — is unbuilt | — | tier-2 · W2.1 | `sign-in/page.tsx:137` (TODO), `:143-147` (anchor) | **data-blocked** | open | the widget is unbuilt, so the states cannot be exercised on any environment | **duplicate-of-known → `PD-0-14`.** → `AUTH-TURNSTILE-WIRE` |
| **PD-7a-17** | The version footer renders the two hashes without the version label the spec shows | V | **tier-1** · `SPEC.1.md:789` | `onboarding/page.tsx:160-164` | **data-blocked** | open | the hashes ARE the placeholders | Unjudgeable until real versions exist. → `LEGAL.1` |
| **PD-7a-18** | ToS and Privacy bodies are placeholder Lorem ipsum, rendered in full on the acceptance screen | — | **tier-1** · `SPEC.1.md:776` requires the full text in-page | `public/legal/tos.txt:1` | **data-blocked** | open | content, not build | Pre-recorded in `POLISH-0.md` §3 · POLISH.7a · Pre-recorded. ⚠ **`LEGAL.1` is a GO-LIVE GATE** — acceptance evidence for Lorem ipsum is not a dispute-resolution record, and it carries the relocated AGPL §13 source offer |
| **PD-7a-19** | The `.otp-icon` ring is `--hairline` (1px) where the mockup specifies 1.5px | V | tier-4 · `auth-modal` CSS `:208` | `6032ea3`; Gate C ruling **GR-1** | **superseded** | closed | the only ratified 1.5px rung is semantically wrong for this use | **RULED at Gate C.** `--ring-active` is the emphasis ladder's rung 3 and means *"active carousel item"*, not a stroke width — borrowing it here is a **cross-surface primitive reach AND a wrong-semantic-slot error**, which is the very thing `POLISH-0.md` §7 criterion 6 checks for. `--hairline` is the correct separation token; the 0.5px is accepted |
| **PD-7a-20** | The ported `(auth)` margins interact with `CardHeader`'s `gap-1`, so emitted spacing differs from the mockup's stated values | V | tier-4 · `auth-modal` CSS `:170-174`, `:208` | `otp/page.tsx:133,147`; `card.tsx:26`; Gate C ruling **GR-3** | routed | open | a grid parent's row-gap adds to every child's own margin | ⚠ **The BACK LINK lands at EXACTLY the mockup's 16px BECAUSE of the gap** (`mb-3` 12px + `gap-1` 4px). **Only the icon is 4px out** — `2px/14px` renders as `6px/18px`. Owner: **the comprehensive founder visual pass** |
| **PD-7a-21** | Neither shipped copy delta carries a test pin | V | tier-4 · `auth-modal:313`, `:371` | `@code-reviewer` L-6 | routed | open | a tier-4-verbatim string with no assertion is drift-prone | Owner: **the comprehensive founder visual pass**. `sign-in-render` and `otp-render` assert structure and now class strings, but no copy string |

## POLISH.2 · Discovery

***BUILD HALF COMPLETE — SURFACE STAYS OPEN.** CC machine pass 2026-08-09; every routed delta built and merged at **DISCOVERY-COMPLETE (PR #311, squash `71f4d42`, staging advanced 2026-08-10)**. ⚠ **POLISH.2 IS NOT CLOSED.** The §7 exit bar has seven criteria and **criterion 1 — parity by eye at 1440 — is the founder's and has NOT run.** A surface is not closed on its build half. *Original note follows.* **CC machine pass complete, 2026-08-09. Founder visual inspection NOT yet run** — the pass reads the tier-4 mockup against the source, so it cannot see what only a render shows. Gate: B2 (**met** — `a27f2bf` / PR #276 is an ancestor of `main` and `formatPricePercent` is the single percent path). Deltas are numbered `Vn` per the POLISH-1-D convention (`docs/logs/POLISH-1a.md:5`).*

**Row grouping, stated so the count is not misread.** 50 deltas (`V1`–`V50`) → **31 rows**. The **24 shipped class-A geometry/type deltas** are grouped **one row per commit** (PD-2-01…06) with their `Vn` enumerated in the title: the disposition is identical inside each group, and 24 near-identical lines would bury the 25 rows that carry a real decision. **Every B, C and D delta gets its own row.** Two rows carry no `Vn` — PD-2-19 (an SCL-1 supersession the `Vn` list folded into V14) and PD-2-31 (PERF-1, not a parity delta). `V24` appears in two groups because it spans two files.

*Three rows minted before 2026-08-10 (PD-2-02, PD-2-06, CC-4) carry dual class values predating POLISH-0 §4.1. Grandfathered — not split, because splitting a resolved row is churn. §4.1 binds every row minted after 2026-08-10.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-2-01** | **V2** — no page inset; the grid ran to the viewport edge | V | tier-4 · `.content{padding:16px 28px 12px}` `:67-68` | `(public)/page.tsx` | routed | fixed | Full-bleed was read as "no inset"; the mockup is full-bleed **with** a 28px inset | POLISH.2 C1 |
| **PD-2-02** | **V26 V27 V28 V48** — stat line: emphasis dimmer than its label, separator on an opacity hack, one size for two surfaces, `1 posts` | V · F | tier-4 `:133-135` `:157-159` `:212` | `StatLine.tsx` | routed | fixed | One component serving two mockup rules that were never distinguished | POLISH.2 C2 |
| **PD-2-03** | **V5 V6 V8 V9 V12 V14 V19 V21 V22 V24** — hero geometry, padding, type, clamps, thumb, graph border | V | tier-4 `:71-72` `:82-93` `:123-131` | `HeroPanels.tsx` | routed | fixed | — | POLISH.2 C3 |
| **PD-2-04** | **V4 V33 V34 V35** — carousel rhythm, dot shapes, arrow glyph | V | tier-4 `:138-145` | `DiscoveryCarousel.tsx` | routed | fixed | Every dot rendered as the ACTIVE pill, so the rail read as eight identical bars | POLISH.2 C4 |
| **PD-2-05** | **V20 V24 V39 V40 V41** — grid gap, card padding + `space-between`, title, thumb, sparkline border | V | tier-4 `:148-159` | `MarketCard.tsx` · `DiscoveryGrid.tsx` | routed | fixed | — | POLISH.2 C5 |
| **PD-2-06** | **V47** — the Reload button had **no focus ring**, the only keyboard-reachable control on the surface | V · F | tier-2 · values-log v0.3 §3 item 3 (states) | `ErrorState.tsx` | routed | fixed | Hand-rolled `hover:text-ink` instead of the ratified state slots | POLISH.2 C6 |
| **PD-2-07** | **V7 V44** — hero panels and grid cards shared one hairline, so the hero carried no emphasis | V | tier-4 `:78` vs `:150`; BRIDGE retired ink-emphasis (`globals.css:51-53`) | `HeroPanels.tsx` | routed | fixed | Mockup rings both in `--ink`; the dark ramp has no equivalent | **FOUNDER-RULED** — brighter neutral step, `--ink` never mapped by name |
| **PD-2-08** | **V42** — the active carousel ring was the **same value** as every card's resting hairline (both n2) | R | tier-4 · `.mcard.athero` `:152` | `DiscoveryGrid.tsx` | routed | fixed | `--border-strong` aliased to n2 at BRIDGE, collapsing the emphasis it named | **Gate C.** Built to the V7 ladder: n2 grid < n3 hero < **n4 ring** · **Ruling requested.** |
| **PD-2-09** | **V18** — only the title+teaser box was clickable; the mockup's whole `.argbody` is the post target | F | tier-4 `:402-407` `:395` | `HeroPanels.tsx` | routed | fixed | Anchors cannot nest, so the panel was never wrapped | POLISH.2 C3 — stretched link |
| **PD-2-10** | **V37** — **no keyboard handler at all**; ArrowLeft/ArrowRight did nothing | F | **tier-2 · design-canon §5 Discovery** (not a11y-deferred) | `DiscoveryCarousel.tsx` | routed | fixed | Canon's key binding was never built and no suite covered it | POLISH.2 C4 |
| **PD-2-11** | **V3** — mockup locks the viewport (`overflow:hidden`, `100vh`); the build scrolls | V | tier-4 `:53-56` | — | **superseded** | closed | — | **ADR-0023 §Patch 2026-08-03:229** — sticky header on all `(public)` routes; its Context `:222-225` states the mockups "cannot answer" scroll. **DO NOT PORT** |
| **PD-2-12** | **V23 V25** — mockup sizes both graphs `flex:1`; the build uses fixed heights | V | tier-4 `:130` `:155` | — | **superseded** | closed | The flex sizing only resolves inside the viewport lock PD-2-11 supersedes | Rides PD-2-11 |
| **PD-2-13** | **V1** — the mockup's whole `:root` is the light era; the shipped ramp is dark **and inverted** | V | tier-4 `:44-47` | `globals.css:133-148` | **superseded** | closed | — | **values-log v0.3 §2 R-1 + §1 item 8.** Never map mockup greys by name or lightness |
| **PD-2-14** | **V31** — graph strokes hard-coded `#A3A3A3` / `#0A0A0A` | V | tier-4 `:218` `:220` | `PriceSparkline.tsx:58,65` | **superseded** | closed | — | **values-log v0.3 §1 item 1** (O1/WI-3): `--graph-yes` `#737373` / `--graph-no` `#fafafa`. Build already correct |
| **PD-2-15** | **V32** — mockup hard-codes 8 dots; the build renders `markets.length` | V | tier-4 `:262` | `DiscoveryCarousel.tsx:83` | **superseded** | closed | — | **SPEC.1 §22** sparse-market rule. Build correct |
| **PD-2-16** | **V36** — mockup always shows arrows; the build hides them at `n <= 1` | V | tier-4 `:261,263` | `DiscoveryCarousel.tsx:73,93` | **superseded** | closed | — | **SPEC.1 §22 F-DISC-2** single-market static. Build correct |
| **PD-2-17** | **V38** — dot fill is `@keyframes` width in the mockup, `scaleX` transition in the build | V | tier-4 `:144-145` | `DiscoveryCarousel.tsx:118-136` | **superseded** | closed | — | UI.A4 code-review LOW — bound to `ADVANCE_MS` so it cannot desync. Keep |
| **PD-2-18** | **V43** — mockup's system sans stack vs Geist | V | tier-4 `:50` | `globals.css:154-155` | **superseded** | closed | — | **values-log item 2 / WI-13** — Geist ratified FINAL |
| **PD-2-19** | Mockup post panels show **body only**; the build renders title + teaser | V | tier-4 `:192`, and the mockup's OWN header note `:11-13` | `HeroPanels.tsx` | **superseded** | closed | — | **SPEC.1 §2/§8 SCL-1** (`SPEC.1.md:384`). The mockup self-declares this its "Known render gap" |
| **PD-2-20** | **V10 V11** — side chip renders the bare side; mockup carries entry price (`YES @ 27%`) + its own geometry | R | tier-4 `:188` `:241` `:115-116` | `HeroPanels.tsx:137` · `badges.tsx` | routed | fixed | `SideBadge` spans **11 files / 13 render sites** (was 9 — C4/C4b adopted it, see CC-6) | **DISCOVERY-COMPLETE C3 + C3a.** ⚠ C3 first rendered the entry price through the PAIRED formatter, printing `NO @ 45%` for a 55% NO entry; `bets.price_at_bet` is ALREADY the bought side's price (`place.ts:162` → `calculate.ts:73-97`). C3a renders it RAW via `formatPercentUnpaired`; `EXPECTED_ALLOW_MARKERS` 2 → 3, argued in source |
| **PD-2-21** | **V29 V30** — price-bar geometry: labels outside the bar, 22px hero / 16px card | V | tier-4 `:223-227` `:99-113` | `PriceBar.tsx` | routed | fixed | Row named a NON-consumer and omitted the real one — see **CC-1** | **DISCOVERY-COMPLETE C1.** `size` REQUIRED, no default (O-1). Three consumers: `HeroPanels.tsx:79` hero · `MarketCard.tsx:68` card · `MarketHeader.tsx:96` **`detail`, pinned byte-identical (385 bytes) — OD-2**. PCT.ROUND untouched. ⚠ `detail`'s own numbers are **POLISH.3's row** below |
| **PD-2-22** | **V13** — `argstake` is a progression `Đ 1,000 → Đ 1,407`; the build shows one value | B | tier-4 `:190` `:243` | `HeroPanels.tsx` · `hero.ts` | routed | fixed | Sized L on "current value is in no loaded read" — TRUE for the market-scoped figure, and Discovery lists Open markets only, so it collapses to one `computeSell` | **DISCOVERY-COMPLETE C8 + the Gate C V13 fix.** ⚠ C8 shipped MARKET-scoped under a ruling (OD-1) that said POST-scoped: an author with three Đ1,000 posts read `Đ 1,000 → Đ 4,221` on every panel. Now `min(betShares, heldQuantity)` via a `leftJoinLateral` ordered IDENTICALLY to the substrate's earliest-bet LATERAL, so both figures provably come from the same bet. **+0 queries** |
| **PD-2-23** | **V15** — `argimg` image attachment absent from the hero post | B | tier-4 `:193` `:246` | `HeroPanels.tsx` · `hero.ts` | routed | fixed | No image field on `HeroPost` | **DISCOVERY-COMPLETE C7.** `LEFT JOIN image_uploads` as a COLUMN on the existing picked-posts select — **+0 round-trips**; `mintImageUrls` stayed module-private so ADR-0034 clause 1 held. SC-1 both obligations discharged: the join rides the already-removed-filtered `pickedIds`, and the never-echo sweep asserts a removed post's R2 key absent |
| **PD-2-24** | **V16** — `replyhead` (`Replies · 24` │ `Đ 10,000 staked`) absent | B | tier-4 `:194` `:247` | `HeroPanels.tsx` · `hero.ts` | routed | fixed | The data was already loaded and discarded | **DISCOVERY-COMPLETE C5.** `replyCount` + `replyDharma` from the substrate, summed with `CpmmDecimal` (never JS `+`). **+0 queries.** V48 singular/plural honoured. ⚠ Built to the mockup's `text-n4` (`:97-98`), not the plan's `text-n6` — **OQ-2, founder-held for the 1440 pass** |
| **PD-2-25** | **V17** — SUPPORT/COUNTER split bar absent from hero posts | B | tier-4 `:195-199` `:248-252` | `HeroPanels.tsx` | routed | fixed | Same discarded data as PD-2-24 | **DISCOVERY-COMPLETE C6 + the Gate C V17 pole fix.** Display-only: no button/link/handler, asserted. ⚠ C6 shipped a FIXED `bg-yes` fill over a FIXED `bg-no` track, so the **NO panel painted the NO-side share in the YES pole**. Segments are now SIDE-KEYED per canon; `HeroPanels.tsx` is C0's **seventh** inventory entry, added deliberately. This is **route 3/4** — see CC-7 |
| **PD-2-26** | **V45** — no `--elev-*` anywhere in discovery | V | tier-2 · values-log §1 item 10 (O3) names "Sparkline wrapper; Discovery HeroPost card + hero market panel" | `HeroPanels.tsx` · `MarketCard.tsx` | **duplicate-of-known** | open | — | **POLISH.1** — pre-recorded in `POLISH-0.md` §3 · POLISH.1 · Pre-recorded (`--elev-2` has no live consumer). Do **not** open a second row |
| **PD-2-27** | **V46** — Empty/Error states do not conform to the W2.11 P1 locked shape | V | tier-2 · W2.11 P1 | `EmptyState.tsx` · `ErrorState.tsx` | routed | fixed | ≥5 distinct implementations product-wide | **DISCOVERY-COMPLETE C9, founder ruling R9.** Both adopt P1's ONE shape; all three OQ-6 copy consts VERBATIM; `data-testid`s unchanged; V47's state slots preserved on the CTA. R9 also supersedes W2.11's *Killed by ruling* line for Discovery (empty-Discovery + T2), recorded in the canon amendment |
| **PD-2-28** | `LoadingSkeleton` renders 4 hard-coded card blocks, not `DISCOVERY_GRID_SIZE`-shaped | R | tier-2 · W2.11 T1 ratified **no** loading primitive | `LoadingSkeleton.tsx` | routed | fixed | T1 assumed populated HTML; Discovery mounts a `Suspense` boundary, so first paint IS the fallback | **DISCOVERY-COMPLETE C10, founder ruling R8 — T1 SUPERSEDED.** P7 minted at `ui/loading-block.tsx`; canon amended in the SAME commit (§10 + §1/§8/§12 P1–P6 → P1–P7) + values-log geometry. Count now from `DISCOVERY_GRID_SIZE`: it reserved space for **4** cards against an **8**-slot grid |
| **PD-2-29** | **V49** — `POLISH-0` §3's POLISH.2 row omitted `StatLine` (carries six deltas) and listed `scrollers`, which belongs to the **debate** surface | S | tier-2 · the method document is wrong | `POLISH-0.md` §3 · POLISH.2 · Components; `src/components/debate/scrollers.tsx` | routed | fixed | `StatLine`'s omission sends an inspector past the component that matters. `scrollers` was mis-copied from POLISH.3's row, where it is correct and stays | POLISH.2 docs commit. ⚠ **This row first read "`scrollers` does not exist anywhere under `src/`" — false.** It was asserted from a `src/components/discovery/`-scoped grep and stated as a repo-wide negative. Corrected at Gate C. **O-2: verify against the live repo, and never widen a directory-scoped negative into a tree-wide one** |
| **PD-2-30** | **V50 (new)** — hero avatar renders at the primitive's 24px; mockup `.avatar` is **16px** | V | tier-4 `:84` | `HeroPanels.tsx:118` · `ui/avatar.tsx` | routed | fixed | `data-[size=sm]:size-6` out-specifies a consumer override — the twMerge trap | **DISCOVERY-COMPLETE C2.** `xs` added to the primitive (`data-[size=xs]:size-4`). Exactly **4** `<Avatar>` mounts in `src/`; the three `size="sm"` sites (IdentityCluster ×2, ArgProfile) are untouched — zero delta, guarded |
| **PD-2-31** | **PERF-1** — `/` served in ~35 s cold | B | tier-1 · was the only go-live blocker | `list.ts:48-63` · `page.tsx:59-65` | routed | closed | **Functions ran in `iad1` against a Mumbai DB.** ADR-0006 ratified `bom1` on 2026-05-05 and it was never applied — `vercel.json` had no `regions` key, and a SILENT config surface is indistinguishable from a correct one in every diff, CI run and review | **FIXED**, PR #307 + close-out. **361.6 → 5.34 ms/trip**; Discovery **35.07 → 0.584 s p50** (exit bar 2.0 s, met by 3.4×); Profile **6.2 → 0.189 s p50**. ⚠ The recon's *"41 round-trips"* was **97** (calls, not statements) and its *"warm p50 1.1 s"* **was TTFB — no warm regime exists**. **Batching NOT required** and not done. **POLISH.1–.8 unblocked** · **Closed 2026-08-10.** |
| **CC-1** | `PD-2-21` named a NON-consumer and omitted the real one | S | the register is wrong | `chart/MarketPriceChartCard.tsx` · `MarketCard.tsx:68` | routed | fixed | Row said `PriceBar` is consumed by `MarketHeader` + `MarketPriceChartCard` — the latter names `PriceBar` only in a COMMENT (`:25`) and renders its own `sr-only` readout; the row omitted `MarketCard.tsx:68`, which IS V30's 16px consumer | **Corrected in PD-2-21 above.** Same failure mode as PD-2-29's own recorded correction: a row that sends a build session to a file with nothing to change and past the one that mattered |
| **CC-2** | Round-trip figure has a third, reusable form | S | — | `AGENTS.md` Discovery warning | routed | open | The row records `97`; AGENTS.md still says "1 + 3N … a further 2N", which counts CALLS not statements | **AGENTS.md sweep.** The reusable form is **`1 + 12N`** — survives `DISCOVERY_GRID_SIZE` changing. Now MACHINE-PINNED at `tests/server/discovery/round-trip-budget.test.ts` (25 at N=2; hero read exactly 5) |
| **CC-3** | `1b7f37f` was UNPUSHED and did not land in #309 | S | — | local `chore/post-perf-1-docket` | routed | closed | — | **ADR-0006-DISCIPLINE.** DISCOVERY-COMPLETE branched from `origin/main` explicitly; `git merge-base --is-ancestor` confirmed it absent. No action was needed beyond that |
| **CC-4** | `PD-0-10`'s root cause is broader than its row | S · F | — | `BookmarkCard.tsx` · `ArgumentList.tsx` · `ReviewFeed.tsx` | routed | fixed | Row read "three `PositionMarker` implementations … side chips likewise hand-rolled" as cosmetic duplication. **TWO of the three INVERTED the pole on live participant surfaces** | **Re-class the side-chip half V → F.** Both fixed at DISCOVERY-COMPLETE C4/C4b; C0's guard closes the class for routes 1–2. PD-0-10 goes three implementations → **one** (admin, correct — see CC-9) |
| **CC-5** | Spec↔code drift on `price_at_bet` | S | `debate-export.md:177` · `SPEC.2.md:2721` | `place.ts:162` · `calculate.ts:73-97` | routed | open | Both docs say entry price is "the market **YES-probability** … the same basis for YES and NO bets". The engine stores `computeBuy(...).pEff` for the side **BOUGHT**, so a NO bet stores the NO price | ⚠ **SPEC.2 is web-authored — CC must NOT draft it.** The shipped `.md` export is numerically CORRECT; only the prose is wrong. This is the misreading C3 inherited and C3a corrected. **Doc sweep, founder-authored** |
| **CC-6** | `SideBadge`'s counted inventory went stale WITHIN this PR | S | — | `badges.tsx` · `side-badge.test.tsx` | routed | fixed | Docstrings said "9 files / 8 consumers" — true when the plan was written, stale once C4/C4b adopted the primitive four commits later | Now **13 render sites across 10 consumer files + the definition**; 8 pass neither new prop and are the zero-delta set. Counted inventories are load-bearing here (C0's whole premise), so a stale one in the primitive's own docblock is the same failure in miniature |
| **CC-7** | **The FOUR routes to a wrong pole** — one place | S | — | `side-pole-binding.test.ts` docstring · `docs/logs/DISCOVERY-COMPLETE.md` | routed | routed | C0 catches routes **1 and 2 only**, and a green C0 was reading as completeness | ⚠ **POLISH.3/.4/.5/.6 ALL port from these same light-theme mockups.** (1) semantic indirection — caught · (2) name-porting across the inverted ramp — caught · (3) **fixed pole on a per-side element** — UNCAUGHT, no side-keyed expression exists to match · (4) **porting the fill colour WITHOUT the anchor rule** — UNCAUGHT, and it is HOW route 3 gets written. Rule: when a mockup expresses a binding through POSITION or ANCHORING as well as colour, port the **binding**, never the property |
| **RR-3** | `composer/ReplySplitBar.tsx:64,67` — **live pole inversion on `/m/[slug]`** | F | tier-2 · values-log §3 "left = Support share in the SUPPORT SIDE'S POLE COLOUR" | `ReplySplitBar.tsx:64,67` | routed | open | The file carries **BOTH**: a CORRECT side-keyed pole at `:118-122` (why C0 permits it) **and** a separate FIXED `bg-no` track + `bg-yes` fill at `:64,:67` rendering the Support/Counter split with no side keying, while `postSide` is a prop of the same component | ⚠ **INV-3, class F — founder-ruled NOT a styling item.** Same treatment `BookmarkCard`/`ArgumentList` got. On a NO post the Support share renders black where canon says white. **Owner: POLISH.3/.4.** Verified, deliberately NOT fixed at DISCOVERY-COMPLETE (pre-existing; `/m/[slug]` is another task's surface) |
| **RR-4** | `PositionMarker` outline → filled on `/bookmarks` + `/u/[pseudonym]` | V | — | `BookmarkCard.tsx` · `ArgumentList.tsx` | accepted-divergence | closed | C4/C4b replaced a hand-rolled `<Badge variant="outline">` with the shared `PositionMarker` (`variant="secondary"`) | **Founder-ACCEPTED known delta.** It is the adoption the plan mandated (adopt, never patch) and what delivered the missing `aria-label="Author Flipped"` — PD-0-10's actual root cause. Recorded so **POLISH.5/.6 inspect the CONSOLIDATED state instead of re-filing it** |
| **CC-9** | `(admin)/…/ReviewFeed.tsx:102-104` — the surviving third side-chip hand-roll | S | — | `ReviewFeed.tsx:102-104` | routed | open | Mapping is **CORRECT today** (`bg-yes` for YES) — duplication, not inversion. **EXCLUDED from C0's guard BY DIRECTORY** (`src/app/(admin)/**`), deliberately | ⚠ **This is the chip the operator reads sides from WHILE MODERATING.** It is unguarded and can drift with nothing on disk going red, because the only guard that would catch it excludes its directory by design. **Route to an ADMIN PASS — not PRIMITIVES-2.** Consolidating admin chrome onto a participant primitive is an admin-surface decision (CLAUDE.md §3) |
| **PD-2-32** | **Broken market thumbnails on every grid card + the hero market panel** — the broken-image glyph renders and its alt text overflows into the metadata row | F | observed on staging 2026-08-10, post-advance | `MarketCard.tsx:47-56` · `HeroPanels.tsx:58-66` | routed | **fixed** | **A minted URL that later 404s has NO degradation path.** `getDefaultMarketMediaUrl` (`media.ts:56-62`) handles R2 unavailable AT MINT TIME — presign throws → `null` → the `IMG` placeholder. But presigning is a LOCAL HMAC that never checks existence, so a key pointing at a missing object mints a perfectly valid URL and the failure surfaces only at browser LOAD, where nothing catches it | **⚠ NOT staging-only — a production defect.** R2 objects can 404 in prod too (deleted, swept, replication lag). Both sites are inside the `"use client"` carousel (`DiscoveryCarousel` → `DiscoveryGrid` → `MarketCard`; `HeroPanels` likewise), so **`onError` IS available** — confirmed. **Recommended shape: one shared `MarketThumb` owning three states (null · error · loaded), used by BOTH sites.** Size **S** (~30 lines + render tests). Do NOT patch two `<img>`s independently — that is how PD-0-10 happened · **CLOSED at PRIMITIVES-2 PR-A, PR #317, merged `143380b`.** `src/components/discovery/MarketThumb.tsx` owns null · error · loaded for all THREE image sites (both market thumbs + the hero POST image); the three `<img>`s were not patched independently. ⚠ **`onError` ALONE DID NOT DISCHARGE THIS** — plan §11 **D2-P1** added a **mount-time check** (`complete && naturalWidth === 0`), because React binds `error` as a non-delegated listener at hydrate time and `page.tsx` is `force-dynamic`: an error resolving BEFORE hydration is lost permanently and the broken glyph persists forever. PERF-1's 35.07 s → 0.692 s made that the COMMON case, not the edge. A later reader who assumes `onError` was sufficient will mis-scope this defect class |
| **PD-2-33** | Market-thumb `alt` is the market TITLE, redundant with the title rendered beside it — and it is what makes the failure OVERFLOW | V | tier-2 · WCAG 1.1.1 | `MarketCard.tsx:53` · `HeroPanels.tsx:64` | routed | **fixed** | Both thumbs carry `alt={card.title}` while the same title renders in the adjacent `<h3>`. A screen reader hears it twice; a broken image prints it as visible text | **Should match V15.** The hero POST image is deliberately `alt=""` (`HeroPanels.tsx:189`) — decorative, because the argument text carries the meaning and the title is adjacent. **The identical reasoning applies to the market thumb**, and `alt=""` is also what stops the overflow. ⚠ Fixing (c) alone hides the symptom while PD-2-32's real gap remains — land them together · The a11y half (alt duplication, WCAG 1.1.1) routes to **A11Y.0**; the overflow half is POLISH.2's. · **⚠ ONLY THE OVERFLOW HALF CLOSED, at PRIMITIVES-2 PR-A, PR #317, merged `143380b`** (D4 — both thumbs now `alt=""`, landing with PD-2-32's `MarketThumb.tsx` as required). **THE A11Y HALF REMAINS ROUTED AND OPEN at A11Y.0**, and the `alt=""` exception itself is rowed at **`OQ-6-ALT-EXCEPTION`** (`docs/parked.md`) so the comprehensive founder pass does not rediscover it as a defect. `fixed` on this row means the overflow, not WCAG 1.1.1 |
| **PD-2-34** | Staging's `market_media` rows point at R2 objects that **DO NOT EXIST** — 8/8 | S | — | `curl` of all 8 minted URLs, 2026-08-10 | routed | open | Every one returns **HTTP 404 `<Code>NoSuchKey</Code>`**, 127 bytes. Rows present in the DB, objects never uploaded | **FIXTURE GAP — see `STAGING-FIXTURE-DISCOVERY-SHAPE` in `docs/parked.md`.** This is WHY the defect is visible on staging; it is not the defect itself (PD-2-32 is). ⚠ The STAGING-PARITY set is **md5-pinned** — changing it is a deliberate fixture change WITH a re-pin, never an edit |
| **PD-2-35** | Discovery's responsive column behaviour is **DELIBERATE** and diverges from the mockup's fixed 4-wide grid and 3-wide hero | R | tier-4 · `.grid{grid-template-columns:repeat(4,1fr)}` `:148` · `.hero{grid-template-columns:minmax(0,1fr) minmax(0,1.9fr) minmax(0,1fr)}` `:71` | `DiscoveryGrid.tsx` (`grid gap-3 sm:grid-cols-2 lg:grid-cols-4`) · `HeroPanels.tsx` (`md:grid-cols-[1fr_1.9fr_1fr]`) | routed | open | The mockup is a **fixed-viewport prototype** — its own header states *"Full-screen: 100vw × 100vh, no scroll. Desktop-only."* (`:38`). A 4-wide grid and a 3-across hero at every width are artefacts of that frame, not ratified responsive intent. The build collapses to 1 column, then 2 at `sm`, then 4 at `lg`, and stacks the hero below `md` | **REGISTER ONLY — no code, by ruling (HTML-FINISH row 10).** Recorded so a later parity pass does **not** "correct" the breakpoints toward the mockup and ship a 4-wide grid on a phone. ⚠ Disposition is `routed`/`open`, **NOT `accepted-divergence`** — §"How to use this" reserves that value to the founder (P12), and recording a divergence as deliberate is not the same act as ratifying it. **Founder ratification requested.** Raised at HTML-FINISH · DISCOVERY · PARITY |


## POLISH.3 · Market Detail

*Not yet inspected. Gates: B1 · B2 · B3 · C3.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-3-01** | `PriceBar`'s **`detail`** preset is a NAMED TRANSITIONAL value, not the ratified one | V | tier-4 · `surface_d5_v1_0.html:507-508` — **14px bar / 10px labels** | `PriceBar.tsx` · `MarketHeader.tsx:96` | **inherited** | open | DISCOVERY-COMPLETE pinned `detail` byte-identical (385 bytes) to the pre-preset render so `/m/[slug]` had a ZERO pixel delta — founder ruling **OD-2** | **POLISH.3's row, inherited from DISCOVERY-COMPLETE C1.** Applying d5's numbers there would have spent POLISH.3's decision without POLISH.3's inspection. The preset seam exists; only the numbers are open |
| **PD-3-02** | `SideBadge` can carry the entry price on d5's chips with **no further primitive change** | V | tier-4 · `surface_d5_v1_0.html:1701` renders `YES @ <e>%` | `badges.tsx` · `DebatePost.entryPrice` | **inherited** | open | `price` was made OPTIONAL at C3 precisely so d5 could adopt it later | `DebatePost.entryPrice` / `DebateReply.entryPrice` ALREADY exist (`load-debate-view.ts`), so adoption is a call-site change. ⚠ Render it **RAW** — see **CC-5**; it is already the bought side's price |
| **PD-3-03** | d5 adopts `SideBadge`'s **`detail`** preset (10px chip) at its `.sidechip.md` sites | V | tier-4 · `surface_d5_v1_0.html:540` over `:538` + `:742` — **10px / 3px 9px / .1em / 800** | `badges.tsx` `CHIP.detail` · d5 call sites | routed | open | PRIMITIVES-2 D5 landed the SEAM at the primitive and deliberately wired ZERO call sites — wiring here would re-skin `/m/[slug]` before POLISH.3 inspects it (`POLISH-SURFACE-TEMPLATE.md` §4.2 C1) | **POLISH.3's row.** The preset EXISTS and is render-tested at both poles; adoption is a call-site change. ⚠ Do **not** reach for `CHIP.profile` for d5's `.sm` sites — see the warning at that preset: d5 carries contextual `border-radius:4px` overrides (`:882`, `:911`) a flattened preset cannot express |
| **RR-3** | ⚠ **INV-3 — live pole inversion at `composer/ReplySplitBar.tsx:64,67`** | F | tier-2 · values-log §3 | `ReplySplitBar.tsx:64,67` | **inherited** | open | See the RR-3 row under POLISH.2 for the full evidence | **OWNED HERE (POLISH.3/.4).** Class F, INV-3, live on `/m/[slug]`. Not a styling item |
| **PD-3-04** | The expanded chart overlay carries no accessible text summary; SPEC.1 §9 · Accessibility requires one | F | tier 1 — SPEC.1 §9 · Accessibility: *"an accessible text summary naming the opening price, the current price, and the domain endpoints"* | `MarketPriceChartOverlay.tsx` — zero `sr-only`; its only text is `aria-label="Market price history"` + two × `aria-label="Close price chart"` · `MarketPriceChartCard.tsx` — carries `data-testid="market-price-chart-summary"`, the summary the overlay lacks · `MarketPriceChart.tsx` — SVG is `aria-hidden` in **both** modes | — | open | `docs/plans/UI.19.md` scoped the F-DEBATE-5 summary to the **collapsed card by name** — *"a sibling `sr-only` element in the collapsed card states opening price, current price, and the two domain endpoints"* — and the overlay was specified only as *"mirroring `ProfileGraphOverlay`"*, which has no summary either. **Tier 3 never supersedes tier 1.** The same root cause is why `ProfileGraphOverlay` should be checked at POLISH.5 rather than assumed clean | **Minted at the SPEC.CHART / R13 ruling, 2026-08-12**, so POLISH.3 inherits it rather than rediscovering it. ⚠ **This is NOT the A11Y.0 overlay-focus row** — that one is cross-surface, covers both overlays, and concerns focus move/trap/restore plus duplicate accessible names. This row is a **named tier-1 obligation on one surface**, and it is the evidence that the overlay is inspectable at all: a component with no baseline cannot generate a conformance finding. Disposition at POLISH.3 |
| **PD-3-05** | R14 check 1 — slot-header geometry against the CD-final transcription | R | tier-2 · canon §10 R-5 · `POLISH-0.md` §2.1 bucket C, the only repo-side copy of the values (`values-log v0.3` is off-repo, docket N1) | `SlotHeader.tsx:104` `h-auto min-h-[34px] px-3.5 py-[7px] text-[13px]` · `:118` `flex items-center gap-[5px] text-[19px] font-semibold text-ink` — both re-read at head | routed | closed | ⚠ **The check PASSED on every named property and only the COORDINATES drifted** (−7 from `:111`/`:125`), which is why a line-keyed check reads as a failure where a property-keyed one does not — V-8's genus | **R14 · RULED 2026-08-12 — CHECK 1 PASSED.** No delta on the values. Disposition appends to canon under the **`C-`** form at the POLISH.3 close-out, never as `R14`. Discharges the *"R14 has no register row"* riders at `POLISH-0.md` §2.1 bucket C, `POLISH-0.md` §3 · POLISH.1, and `POLISH-TRACKER.md` §2 — three independent assertions, one mint |
| **PD-3-06** | R14 check 2 — card media clip. The in-card `--imgmax` clip holds; *"renders whole in the pop-up"* holds in the **width axis only** | R | tier-2 · CD-A (retires WI-5), via R14 check 2. Token values at head: `globals.css:179` `--imgmax: 160px` · `:180` `--imgr: 6px` | ✅ in-card: `CommentImage.tsx:28` `max-w-[var(--imgmax)] rounded-[var(--imgr)] [border:var(--hairline)]`, consumed at `PostCard.tsx:98` and `PostFocusHeader.tsx:88`. ⚠ pop-up: `dialogs.tsx:46-50` `<img className="w-full …">` inside `max-w-lg` + `max-h-[80vh] overflow-y-auto` — **no height constraint**, so a tall image scrolls rather than rendering whole. Contrast the lightbox at `dialogs.tsx:88`, `max-h-[80vh] w-full object-contain`, which does constrain it | routed | open | CD-A's phrase *"renders whole"* names no axis, so a two-axis obligation was recorded as one claim and the height half was never checkable. `w-full` discharges width and is silent on height | **R14 · RULED 2026-08-12 — CHECK 2 HALF-PASSED.** Open half → POLISH.3 **PR 2**. ⚠ **MUST be dispositioned TOGETHER with `PD-0-03`'s geometry half (R5)** — CD-A's 720px width changes what *"whole"* means here, so fixing the geometry first re-opens this row. Delta lineage `P3-D09`. Canon append under the **`C-`** form at the close-out |
| **PD-3-07** | Đ-glyph spacing — `/m/[slug]` renders the unspaced form where Discovery and the composer render `Đ 100` | V | tier-2 · internal design-language consistency. SPEC.1 §10.8 governs the **value**; glyph spacing is unspecified, so this is an inconsistency, not a spec violation | **Five sites, verified in source:** `MarketHeader.tsx:98` · `ReplyCard.tsx:55` · `ArgProfile.tsx:67` · `AggregateFooter.tsx:14` · `:19` — all `Đ{…}`. Reference spaced form: `StatLine.tsx:59` `Đ {…}` and `composer/ReplySplitBar.tsx:70` | routed | open | Five independent call sites for one design-language element, no shared primitive. ⚠ Sites 4–5 render unspaced because **JSX strips the trailing newline and indentation from a text node before an expression child** — invisible to a `Đ`-followed-by-digit grep, which is how a first measurement found three | **Applied from `POLISH-register-ADDITIONS.md` §C at POLISH.3 commit 0. D2 · RULED 2026-08-12 — SPACED.** ⚠ **Split across both PRs; STAYS OPEN after PR 1**, which takes `MarketHeader.tsx:98` only. Sites 2–5 are PR 2's, and PR 2's plan carries their guards — there is no test in either direction today. The direction is not symmetric: normalising `/m/[slug]` to spaced breaks nothing; normalising Discovery to unspaced breaks seven assertions across `hero-panels.test.tsx` and `dharma-cluster.test.tsx`. ⛔ `(admin)/…/ReviewFeed.tsx:166` and `composer/{SellModule,BetComposer}` carry the unspaced form and are **out of scope by directory**. Delta lineage `P3-D02` |
| **PD-3-08** | `"1 posts"` — the market header's counts are never pluralised | V | tier-2 · design-language copy register | `MarketHeader.tsx:99`, `:100`. Reference implementation `StatLine.tsx`, pinned at `tests/unit/discovery/render/stat-line.test.tsx:37-46` — `1 post` / `1 reply`, with `not.toContain("1 posts")` as the named failure mode | routed | open | Bare interpolation with no plural rule. ⚠ **Structurally invisible to the existing suite** — every fixture on disk uses counts of 3 / 5 / 28 / 68 / 2 / 0, all of which take the plural form | **Applied from `POLISH-register-ADDITIONS.md` §C at POLISH.3 commit 0. POLISH.3 PR 1.** ⚠ **POLISH.2's half is already CLOSED** — fixed and pinned at V48 — so the ADDITIONS row's `POLISH.3 · POLISH.2` routing was half-stale and `/m/[slug]` is the only unfixed surface. Zero is plural (`0 replies`), never `0 reply`. Delta lineage `P3-D03` |
| **PD-3-09** | A dev-facing placeholder box for unbuilt market media and resolver cards renders to every participant | V | **tier 1** · SPEC.1 §9 *Market media — participant display*: the header renders an auto-advancing carousel, and *"Markets **always** have media … there is **no empty-media state**"*. Corroborated tier-2 · canon §2 (*"Media, resolution, resolver"*) and tier-4 · `surface_d5_v1_0.html:447`, `:251-252`, which render media and resolver cards as designed elements and render no placeholder | `MarketHeader.tsx:43-50` — the file-private `DeferredPlaceholders`, a dashed-border box reading *"Resolver cards — arrive with the market-content slice"* and *"Market media — arrive with the market-content slice"* — mounted unconditionally at `:102`; docblock `:36-42`. **Gating measured: NONE** — zero hits for `NODE_ENV`, `process.env`, `ZUGZWANG_ENV`, a flag or a prop in the file | routed | open | A build-time note about unbuilt work was rendered into the participant surface instead of left in a comment. ⚠ **The spec forbids an empty-media state; the build shipped a labelled one** | **POLISH.3 PR 1.** ⚠ **Removal deletes the only on-screen trace that two ratified-but-unbuilt d5 elements are missing.** The record survives elsewhere and deliberately: `POLISH-0.md` §3 · POLISH.3 · MEDIA.2, and `docs/parked.md`'s `MEDIA.2-GOLIVE`. **The missing carousel itself is MEDIA.2's, not `.3`'s** — this row files only the part that renders on this surface. No test references the stub, measured across all of `tests/`, so removal reddens nothing. Delta lineage `P3-D01` |
| **PD-3-10** | *"Show all replies"* expands **both sides concatenated into one flat list**; the spec requires each side's own full stake-sorted list | F | **tier 1** · SPEC.1 §9 F-DEBATE-1: *"a 'show all replies' affordance expands **each side's** full stake-sorted list"* | `ReplyPreview.tsx:29` — `all = [...support, ...counter]` — then `:33` and `:48` render that flat array | routed | open | The two sides are concatenated **at the source**, so every consumer downstream of `:29` is side-blind. ⚠ The read model already carries them apart — `ReplyGroups` exposes `support`, `counter` and `twoSlot` separately — so the fix partitions what is already there and **adds no `DebateViewModel` field (ADR-0034 D-1 does not fire)** | **POLISH.3 PR 2.** ⚠ **A side-blind expansion on the reply-as-bet surface is a thesis-adjacent defect, not a layout one** — Support and Counter are read-time aggregates over side, and a flat list erases the distinction the affordance exists to show. ⚠ **No test pins the expand semantics**: `ReplyPreview` is rendered only at `tests/unit/debate/render/bookmark-toggle.test.tsx:572`, for bookmark pass-through. PR 2's plan must add the guard. Delta lineage `P3-D04` |
| **PD-3-11** | The route carries no `error.tsx` and no `loading.tsx`; an unhandled render error escapes to the segment above | R | tier-2 · W2.11's kit P1–P7 + `POLISH-0.md` §7 criterion 4. Precedent: `PD-7a-04`, ruled at **R-C** — error boundary added, `loading.tsx` deliberately omitted | `src/app/(public)/m/[slug]/` contains only `page.tsx`, `export/route.ts`, `quote/route.ts`. Siblings that have boundaries: `(public)/bookmarks/{error,loading}.tsx` · `(public)/u/[pseudonym]/{error,loading}.tsx` · `(auth)/error.tsx`. `(public)/not-found.tsx` one level up does catch `notFound()` from here | routed | open | The route was built before the boundary family existed, and **no census enumerates which routes carry which boundary** — measured: no such test exists, and the nearest candidate is a hand-maintained array blind to the tree | **D4 · RULED 2026-08-12. POLISH.3 PR 1: add `error.tsx` at `preset="debate"`, deliberately OMIT `loading.tsx`.** The omission is precedent-verified in the file D4 names — `(auth)/error.tsx:53-57` states R-C directly and `auth-error-boundary.test.tsx:114-124` pins it. ⚠ **The boundary renders NOTHING from `error`** — not `message`, `stack`, `digest` or `cause` — per `(auth)/error.tsx:32-47`, whose reviewed argument is that the **client** arm leaks a real `Error` in production even where the server arm is sanitised. The preset ships under §4.2 B3 and is flagged for Gate C. Delta lineage `P3-D05` |
| **PD-3-12** | `PostPopup` omits the position marker, the lane badge and the aggregate footer that the card renders | V | tier-2 · CD-A close-out (`DESIGN_popup-redesign_CLOSE-OUT.md`), 2026-07-31 | `dialogs.tsx:36-53` — the dialog renders title, `{side} · {pseudonym}`, image and body, **and nothing else** | routed | open | The pop-up was built as a **body reader** rather than as the card's expanded form, so every non-body element the card carries was simply not ported. All four omitted values are already on `PresentPost` and the card reads each one, unconditionally, on the present-post branch: **position marker `PostCard.tsx:77`** `marker={post.marker}` · **author's stake `:78`** `authorStake={post.authorStake}` · **lane badge `:82`** `<LaneBadge badge={post.badge} />` · **aggregate footer `:131`** `<AggregateFooter aggregate={post.aggregate} />`. ⚠ **`marker` is REQUIRED, not optional** — `load-debate-view.ts:107` declares `marker: Marker` on the `removed: false` arm and `Marker` is `"Flipped" \| "Exited" \| "none"`, so absence is a **sentinel**, never a missing field. So nothing is missing from the read model and **ADR-0034 D-1 does not fire** | **POLISH.3 PR 2.** Two rows from one delta per `POLISH-0.md` §4.1 rule 1, sharing this root cause: the tier-1 stake obligation is `PD-3-13`, class **F**, and cannot ride a tier-2 V row. ⚠ **ADR-0034 D-1 does not fire** — no field is added. ⚠ **No test renders `PostPopup` at all.** Delta lineage `P3-D08`, tier-2 half |
| **PD-3-13** | `PostPopup` omits the author's stake `a`, which SPEC.1 requires at every post's header | F | **tier 1** · SPEC.1 §9 F-DEBATE-1: *"Each post renders the author's stake `a` at its header"* | `dialogs.tsx:36-53` — the dialog renders title, `{side} · {pseudonym}`, image and body; `authorStake` is on `PresentPost` and is not read here. The card reads it at `PostCard.tsx:82` | routed | open | The pop-up was built as a **body reader** rather than as the card's expanded form, so every non-body element the card carries was simply not ported. All four omitted values are already on `PresentPost` and the card reads each one, unconditionally, on the present-post branch: **position marker `PostCard.tsx:77`** `marker={post.marker}` · **author's stake `:78`** `authorStake={post.authorStake}` · **lane badge `:82`** `<LaneBadge badge={post.badge} />` · **aggregate footer `:131`** `<AggregateFooter aggregate={post.aggregate} />`. ⚠ **`marker` is REQUIRED, not optional** — `load-debate-view.ts:107` declares `marker: Marker` on the `removed: false` arm and `Marker` is `"Flipped" \| "Exited" \| "none"`, so absence is a **sentinel**, never a missing field. So nothing is missing from the read model and **ADR-0034 D-1 does not fire** | **POLISH.3 PR 2.** ⚠ **Class F, not V, and re-classed deliberately from the recon's single row.** *"No stake, no voice"* is the thesis the stake display carries; a post rendered without it is a **tier-1 non-conformance**, not a spacing choice, and `POLISH-0.md` §4.1 rule 1 requires the split rather than letting a tier-1 obligation ride a tier-2 row. Shared `root_cause` with `PD-3-12`, as that rule directs. ⚠ **No test renders `PostPopup` at all.** Delta lineage `P3-D08`, tier-1 half |
| **PD-3-14** | `PostPopup` renders a post with **no frozen `SideBadge`** — the side is bare interpolated text | F | **tier 1** · INV-3 side binding, via `POLISH-0.md` §7 criterion 2 (*"frozen YES/NO side badge on every post/reply, never changing"*) and criterion 5 | `dialogs.tsx:40-42` — `{side} · {pseudonym}` as a text node; no `<SideBadge>` anywhere in the component | routed | open | The pop-up interpolates `sideAtPostTime` as a string instead of rendering the primitive, so the side is displayed without the frozen-badge affordance that INV-3 is expressed through in the UI | **POLISH.3 PR 2. R5's F half, split from `PD-0-03` by ruling 2026-08-12.** ⚠ **An invariant-visual obligation, not a width preference** — which is why R5 split rather than folding it into the geometry row. ⚠ **The field is present**: `sideAtPostTime` is on `PresentPost` and `dialogs.tsx:41` already reads it, so the remedy is to render the primitive, not to fetch anything. ⚠ **No test renders `PostPopup` at all**, and a per-pole render test must assert **both** a YES and a NO instance — a YES-only test passes on an inverted NO panel, which is exactly how the last live inversion survived a full PR. Delta lineage `P3-D14b` |
| **PD-3-15** | A **third** permanently-disabled control renders on every post and every reply — the `Download` trigger — and no row named it | R | tier-2 · canon §10 item 2 (*"CD either wires the icon to a defined action or removes it"*, scoped there to Profile + Bookmarks, **not** this surface) + `POLISH-0.md` §7 criterion 3 (*"every interactive affordance functional end-to-end"*) | **`BookmarkToggle.tsx::CardActions`** — the disabled `<Button>` carrying `aria-label="Download — sign in to use"` and the `<Download />` glyph; reached on this surface via `ArgProfile.tsx::ArgProfile` and `ReplyCard.tsx::ReplyCard`. ⚠ **`D17`, at commit 0: the citation read `:164-168`, which names the PROPS AND THE ICON ONLY — it omits the opening `<Button` at `:161` and the closing `</Button>` at `:169`, so it fences a fragment rather than the element.** Re-anchored **by SYMBOL** per `O-8`; live evidence `:161-169` at `16971cd`, demoted to evidence. ⚠ **DECLARED CROSS-SURFACE** — `BookmarkToggle.tsx` is one shared component reached from `.3`, `.5` and `.6`; the row stays POLISH.3's, and `.5`/`.6` cite it rather than re-filing it | routed | open | Canon's remove-or-wire rule was scoped by **surface** rather than by **component**, so a shared control inherited the disabled state on a surface the rule never named | **D3 · RULED 2026-08-12 — REMOVED**, same disposition as R1's two. Executes in POLISH.3 **PR 2** as a **second named allow-list exception**; `.5` and `.6` record the adoption. Deleting a dead control is not a re-skin, so §5's one-surface rule does not bar it. ⚠ **No test pins the disabled state.** Delta lineage `P3-D10` |

## POLISH.4 · Composers + Sell module

*Not yet inspected. Gate: B1.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — | — |

## POLISH.5 · Profile

*Not yet inspected. Gate: B1.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-5-01** | Profile adopts `SideBadge`'s **`profile`** preset (8.5px chip) at its `.sidechip.sm` sites | V | tier-4 · `surface_profile_v1_0.html:279` over `:278` — **8.5px / 2px 7px / .08em / 800** | `badges.tsx` `CHIP.profile` · `/u/[pseudonym]` call sites | routed | open | PRIMITIVES-2 D5's split: the SEAM landed at the primitive with ZERO call sites wired, so the adoption is not spent before POLISH.5 inspects the surface | **POLISH.5's row — `.5`, NOT `.6`.** ⚠ This row **carries a routing correction**: `badges.tsx:18` and `docs/plans/DISCOVERY-COMPLETE.md:70` both said POLISH.6. That mockup is POLISH.5's surface. `badges.tsx` was corrected in the same PR; `DISCOVERY-COMPLETE.md` is a landed historical plan and is deliberately **not** edited — this row is the correction of record |
| **PD-5-03** | **P5-a** Footer guard reach — a mounted-elsewhere footer evades both arms (M-2 + F4) | F | D8's static position judgment; the guard's own reach claim | `tests/unit/shell/not-found.test.tsx` ancestry loop + `SiteFooter` name belt | routed | open | Following a mount needs import resolution — a different class of guard | **`GUARD-HARDENING` docket** — `docs/parked.md`, **minted in this commit** (same family as F16). ⚠ **Applied from `POLISH-register-ADDITIONS.md` §A, 2026-08-14** |
| **PD-5-04** | **P5-b** Guard-reach claims overstate the mechanism (L-2 / L-3 / F2) | R | `format.ts:35-38` + the `ROUND0_RENDER` constant's own wording | `(?<!=)\{\s*round0Dharma\s*\(` matches the direct-JSX-child shape only | routed | open | Commit 5 scoped the guard file's header; two claims survive elsewhere | **Ruling: widen the regex OR scope both remaining claims.** ⚠ **Applied from §A, 2026-08-14** |
| **PD-5-05** | **P5-c** Two latent guard hazards — `floor` shorthand surface (L-4), `SiteFooter` raw-source belt (L-5) | F | R4c ratification (L-4); mechanism A's `stripComments` vs the belt's raw read (L-5) | `MONEY_IDS`; the belt's file set grew 3 → 17 in this PR | routed | open | Object-shorthand false positives; belt reads raw source while mechanism A strips comments | **`GUARD-HARDENING` docket** — **both latent, neither fires today**. ⚠ **Applied from §A, 2026-08-14** |
| **PD-5-06** | **P5-d** `tile-identity.test.ts` has no four-digit case of its own (F3) | F | SPEC.1 §10.8 (grouping) vs the suite that owns `displayNetProfitLoss` | Proven structurally blind at **P10** — stayed GREEN with the grouping wrapper deleted | routed | open | Six tests, dedicated describe, every assertion sub-thousand | Fold in when a task legitimately opens that file. ⚠ **Applied from §A, 2026-08-14** |
| **PD-5-07** | **P5-e(i)** Two pre-existing: dead export (L-7), geometry `Number()` on a Đ string (L-9) | S | SPEC.1 §10.8 expressly rules geometry off the Đ basis (L-9) | **L-7** `src/components/debate/composer/copy.ts::c2Strip` · **L-9** `src/components/profile/graph/geometry.ts::yPx` + `::niceMax` | routed | open | L-7: re-pointing rather than removing was the correct §5.3 surgical call | ⚠ **`.5`-RAISED / `.4`-OWNED, AND IT SPLITS BY HALF.** **L-7's file is POLISH.4's** — `debate/composer/copy.ts`; measured at commit 0, `c2Strip` exists THERE AND NOWHERE ELSE, so the bare `copy.ts` in §A was ambiguous across two files. **L-9's half is POLISH.5's.** ⚠ **Evidence is SYMBOL-ANCHORED, not line-keyed** — L-9's `:46,68` had already drifted to `:48,68` (`P5-D25`; a `V-8` instance). ⚠ **Applied from §A, 2026-08-14** |
| **PD-5-09** | `PostSubstrate.priceAtBet` and `ReplySubstrate.priceAtBet` are documented as *"the market YES-probability"* — the field is the **BOUGHT SIDE's** price | S | tier-1 · `price_at_bet` is recorded per bet and is side-scoped; the docblock asserts a semantic the column does not carry | **`src/lib/ranking.ts::PostSubstrate`** and **`::ReplySubstrate`** — the `priceAtBet` docblocks (evidence `:44-48`, `:61-65`) | routed | open | A **`V-3` false receipt** — an assertion ABOUT a field, unchecked by anything, on the exact field POLISH.5 item 3 renders | **`OD-8`, routed to commit 0 and RECORDED here, NOT fixed** — `src/**` is deny-listed on this commit. ⇒ **The consequence, stated so it is not re-derived:** a reader who trusts the docblock computes `100 − x` and prints `NO @ 45%` for an author who entered NO at **55%**. ⚠ **Cross-surface** — `ranking.ts` serves every argument-bearing surface. **First mint above the applied block, per `docs/plans/POLISH-5.md` §1.7** |
| **PD-5-08** | **P5-e(ii)** L-8 — two grouping implementations coexist with a divergent malformed branch | S | D1 **RATIFIES** the duplication; golden fixture backstops drift | `format.ts::groupInteger` guards `/^\d+$/`; `serialize.ts::formatDharmaExportGrouped` uses truthiness, no malformed guard | **accepted-divergence** | closed | Deliberate: unifying them would break the export path's zero-change property | **FOUNDER RULING (P12). DO NOT UNIFY.** ⚠ **CLOSED ON ARRIVAL** — correct and intended, not a mint error. ⚠ **Applied from §A, 2026-08-14** |

## POLISH.6 · Bookmarks

*Not yet inspected. Gate: B1.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-6-01** | The bookmark card's side chip renders a bare pole where the ratified chip carries the entry price — `YES` not `YES @ 27%` | V | tier-2 · `design-canon.md` §10 R-12 (the side-chip price form) · recon `P6-D03` | `BookmarkCard.tsx:46` — `<SideBadge side={item.side} />`, no `price` prop; `grep -n "price=" BookmarkCard.tsx` → zero | routed | open | `/bookmarks` shipped at UI-A6 / ADR-0032 **before** `SideBadge` gained its `price` prop; the primitive moved and the consumer was never re-visited | **POLISH.6 item 1, commit C1.** ⚠ **The DTO field is POLISH.5's to deliver** — `arguments.ts`'s passthrough at `.5` PR A commit A5. **Absent at STEP 0.5 ⇒ per-delta halt; the field is NEVER added on this surface** |
| **PD-6-02** | The figures line's second column reads `Value` where canon ratifies `Current` | V | tier-2 · `design-canon.md:49`, restated `:110` · recon `P6-D04a` | `BookmarkCard.tsx:76-82` — `Value Đ {formatDharma(item.current)}` at `:80` | routed | open | A one-word divergence that predates the canon line; nothing pins the word, so no guard could catch it | **POLISH.6 item 2, commit C1.** ⚠ **The quantity is unchanged** — `item.current` is Đb (`figures.ts:41-42`, computed `:109-118`); only the label moves |
| **PD-6-03** | Both `SideBadge` call sites on the surface render at the default preset, not the ratified `profile` geometry | V | tier-4 · `surface_profile_v1_0.html:278-279` (8.5px / 2px 7px / .08em / 800) · `R12` · recon `P6-D05` | `BookmarkCard.tsx:32` *(removed arm)* and `:46` *(live arm)*; `grep -n 'size="profile"' BookmarkCard.tsx` → zero | routed | open | PRIMITIVES-2 `D5` landed the **seam** with zero call sites wired, by design — the adoption was deliberately deferred to each surface's own polish pass | **POLISH.6 item 3, commit C1.** ⚠ **NOT `PD-5-01`** — that row is POLISH.5's identical adoption on its own surface. **Two rows, two surfaces, one preset** |
| **PD-6-04** | `/bookmarks` has **no P1 empty-state block at all** — its empty state is an inline `<p>`, not a degraded panel | V | tier-4 · `DESIGN_W2_11_state-kit_mockup-v0_1.html:195-199`, the *"Empty Bookmarks · id 18"* block · restated `DESIGN_W2_11_CLOSE-OUT.md:72` · recon `P6-D06` | `page.tsx:42-47` — `<p data-testid="bookmarks-empty" className="py-8 text-center text-n5 text-sm">`, inside the `:41-48` ternary; `grep -n "min-h-\[148px\]" page.tsx` → zero | routed | open | The surface shipped before W2.11's state kit locked. ⚠ **This is an ADDITION, not a re-style** — Discovery's `EmptyState.tsx` is a real P1 block and `/bookmarks` is not a degraded one | **POLISH.6 item 4, commit C3.** ⚠ **Adopts by IMPORTING `ui/empty-block.tsx`** (`JR-5`, v1.4) — POLISH.5 PR B mints it. **`.6` is the only consumer of the `sub` tier**, carried verbatim from mockup `:199` (`OD-1`) |
| **PD-6-05** | The loading state renders three raw `<Skeleton>` elements instead of the P7 `LoadingBlock` primitive | V | tier-2 · `design-canon.md` §10 P7 · recon `P6-D07` | `states.tsx:1` *(the `Skeleton` import)*, `:12` *(three literal keys `["a","b","c"]`)*; `grep -n "LoadingBlock" states.tsx` → zero | routed | open | P7 was minted after this surface shipped; `loading-block.tsx` exists on `main` (C10 / R8) and this consumer was never migrated | **POLISH.6 item 5, commit C4.** ⚠ **KNOWING PARTIAL, ruled `OD-3`:** item 5 discharges P7's **primitive** clause and **not** its **count** clause — canon requires the count come from the surface's own constant, and **there is no bookmarks equivalent** (`limits.ts:200` has `DISCOVERY_GRID_SIZE` and nothing for bookmarks; `loadBookmarks` is unpaginated). **Minting one would be new server API for a single consumer.** Canon note batched to close-out |
| **PD-6-06** | The route error boundary nests flow content inside a `<button>`, has no focus treatment, and makes the whole panel a click target | **F** | tier-2 · the route-boundary family — `(auth)/error.tsx` → `(public)/not-found.tsx` → `m/[slug]/error.tsx`, verified byte-consistent · `m/[slug]/error.tsx:49-51` (*"the states must feel like one family"*) · recon `P6-D08` | `error.tsx:16-18` — `<button type="button" onClick={reset} className="block w-full text-left">` wrapping `<BookmarksError />`, which is a bare `<p>` (`states.tsx:21-27`). **Invalid HTML content model**; no `focus-visible` treatment; no accessible name on the control | routed | open | The retry **works** — the defect is that it has no affordance. ⚠ **The original filing said the gesture did nothing; that premise was measurably false** and the real defects are the three above | **POLISH.6 item 6, commit C5.** ⚠ **Adopts by IMPORTING `ui/error-block.tsx` under `JR-5`/B′** — POLISH.5 PR B mints it. ⚠⚠ **`u/[pseudonym]/error.tsx` CARRIES THE IDENTICAL DEFECT, byte-for-byte** — same wrapper, same className, same `<p>`, same `preset`. **That half is POLISH.5's** (`F-7`, §4 below) |

> **`F-7` · the twin defect's routing note.** Landed at commit 0 X7, 2026-08-14 — it was always commit 0's payload, and was unwritable only until `C-STATES-1` existed to be cited (`design-canon.md` §10, minted at X3′ on this branch).
>
> **`C-STATES-1` governs `u/[pseudonym]/error.tsx` identically.** That file carries `PD-6-06`'s defect byte-for-byte — same whole-panel `<button className="block w-full text-left">`, same `<p>`-inside-`<button>` content-model violation, same missing focus treatment, same `preset="reading"` container.
>
> ⇒ **POLISH.5 item 9, PR B commit B4. POLISH.5 CITES `C-STATES-1` and does not re-derive it.**
>
> ⛔ **POLISH.6 measured the twin, handed it over, and writes it nowhere** — `src/app/(public)/u/**` and `src/components/profile/**` are on POLISH.6's deny-belt.
>
> ⚠ **Why it earns a line rather than a shrug:** two surfaces independently re-deriving one shape is how `D2b` and `D3` went wrong on this task. **One ruling, cited twice, is the remedy** — and citing requires the ruling to be findable.

## POLISH.8 · Admin Centre

*Machine phase RUN — PR #323, `f6d9775`, 2026-08-12. 28 deltas: 6 shipped · 1 superseded · 21 routed. Four Gate C reads. Surface OPEN pending the founder pass.*

⚠ **The recon inventory is 28, not 27.** `P8-D01`–`P8-D29` with **no `D21`** is 28 IDs. The gap is recorded here so a future reader does not hunt for it.

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| **PD-8-01** | `text-white` at `audit/page.tsx:75` — the only Tailwind palette colour class in `src/`, and a contrast failure on the BANNED indicator | V | tier-2 · token contract v0.4 · AGENTS.md §8 ramp direction | `audit/page.tsx:75` · pre-fix palette grep = 1 hit, post-fix = 0 | routed | fixed | `--destructive` maps to `--color-n6`, near the bright end of a dark→bright ramp, so `#fff` sat at ≈1.9:1 on the control that tells the operator an author is banned | **CLOSED at `92c401b` (#323).** `text-background`, per the sibling pill at `ReviewFeed.tsx:170`. **R7 / B12 / `PD-0-07` all close.** ⚠ Removes R15's only live instance — see `PD-8-28` |
| **PD-8-02** | `ReviewFeed.tsx:100-108` hand-rolls a side chip — the third surviving implementation, excluded from the pole guard by directory | V | tier-1 · ADR-0021 surface · POLISH-0 §0 R12 | `ReviewFeed.tsx:100-108` · `tests/server/admin/moderation/review-feed-side-chip.component.test.tsx` | routed | fixed | The only guard that would catch drift declares an exclusion for `src/app/(admin)/**`, so this chip could invert with nothing on disk going red — and two of the three participant hand-rolls had already done exactly that | **PINNED, NOT CONSOLIDATED — founder ruling 2026-08-12.** Both poles asserted with opposite-pole absence; RED on two mutation classes (branches swapped 3/4; one pole hard-coded 2/4). `ReviewFeed.tsx` byte-identical at PR head. **CC-9 closes; R12's cosmetic remainder closes** |
| **PD-8-03** | The typed confirm restated neither the winning side nor the action's permanence | F | tier-1 · SPEC.1 §15 F-ADMIN-3 *Confirmation* | `TerminalActions.tsx:153-176` · `tests/server/admin/terminal-actions-permanence.component.test.tsx` | routed | fixed | The operator armed an irreversible append-only settlement without the surface naming which side they were settling to | **`6430f18` (#323).** Copy only, from data already in props. Both poles asserted with opposite-pole absence; RED 2/7 on a hard-code mutation. ⚠ **@code-reviewer H-1 fixed in the same PR**: the note promised an F-RESOLVE-2 clawback on **Void**, where `actionsForStatus("Voided")` is empty and no correction path exists |
| **PD-8-04** | A malformed date filter was silently dropped — the operator received unfiltered results believing a filter applied | F | tier-1 · SPEC.1 §15 F-ADMIN-5 *System* | `audit/page.tsx` `parseFilters` · `search-surface.tsx` `invalidDateFields` / `InvalidDateNote` | routed | fixed | `new Date("junkT00:00:00.000Z")` → `NaN` → predicate omitted, with nothing on screen saying so | **`ed6b82a` + `013cf3b` (#323).** ⚠ **Gate C read 1 found the first fix asserted breadth it did not have** — on the fire path the search does not run at all and the fallback feed structurally excludes removals and bans. The note now branches on the exported `searchRan`, the page's single expression of that decision |
| **PD-8-05** | The resolution-deadline field did not state UTC, while the widget presents local wall-clock | F | tier-1 · SPEC.1 §15 F-ADMIN-1 (*"minute-granular and parsed as UTC"*) | `create-market-form.tsx:139-145` · `tests/server/admin/create-market-form-utc-label.component.test.tsx` | routed | fixed | An IST operator typing 23:59 committed a settlement deadline 5h30m from what they read back. **The parse is spec-ratified and was not touched** — only the label was the delta | **`ed6b82a` (#323).** Asserted on the rendered `<label>` wrapping the input, never a source grep; positive control on the slug field |
| **PD-8-06** | The action-type placeholder advertised `market.resolved`, which neither side of the F-ADMIN-5 union can ever match | F | tier-1 · SPEC.1 §15 F-ADMIN-5 | `audit/page.tsx:302` → `search-surface.tsx` `ACTION_TYPE_PLACEHOLDER` | routed | fixed | `market.resolved` is an `EVENT_TYPES` value living only in `events`; the form advertised a query that provably returns nothing | **`ed6b82a` (#323).** ⚠ **CONDITIONAL ON `PD-8-10`.** If `admin_events` gains a writer or F-ADMIN-5 is repointed at `events`, this string changes again. Shipping the currently-true copy is the defensible option (§4.2 B3) |
| **PD-8-07** | Three routes carry zero Tailwind classes and the Draft Seed block is bare HTML | — | tier-1 · SPEC.1 §15.3 + F-ADMIN-1/2 *Surface* | `login/page.tsx` · `markets/new/page.tsx` · `create-market-form.tsx` · `[marketId]/page.tsx` | **superseded** | closed | Verified NOT broken: `globals.css` `@layer base` sets `body { @apply bg-background text-foreground }`, so all three render on the branded dark ground | **Never becomes work.** SPEC.1 §15.3 ratifies them verbatim as *"functional and unstyled … must not be removed."* Logged so it is never re-derived |
| **PD-8-08** | Close carries no confirmation of any kind — not a typed gate, not an ordinary confirm | F | tier-1 · SPEC.1 §15 F-ADMIN-3 (*"Close requires a single ordinary confirm"*) | `terminal-actions-logic.ts:48-50` · `TerminalActions.tsx:90-119` | routed | open | ⚠ **The conflicting position is pinned by a GREEN TEST**, not only by a document: `terminal-actions.component.test.tsx:128` is named `close-is-one-click`. No implementation of the SPEC behaviour can leave it green — proven, 1/6 fail | **`ADMIN-CLOSE-CONFIRM`**, `docs/parked.md`. ⛔ **HALTED at S-0k** in #323: the fix needs an edit boundary naming that test file. SPEC-over-plan precedence stands; the task carries it |
| **PD-8-09** | Create-market surfaces the raw error code and discards `error.message` and `field_errors` | F | tier-1 · SPEC.1 §15 F-ADMIN-1 *Errors* | `create-market-form.tsx:118,124` · `src/server/admin/wire.ts` · `markets/create.ts:62,85,92` | routed | open | ⚠ **SPEC and code disagree on the code set.** SPEC lists 10; the action returns **13** — the ten plus `admin_session_required`, `validation_error`, `error_internal`. Three sibling entries (§15 F-ADMIN-3, §11 F-RESOLVE-2/3) each append the wire-boundary clause; F-ADMIN-1 does not, on the same envelope | **`ADMIN-ERROR-COPY`**, `docs/parked.md`. ⛔ **HALTED** by the plan's own item halt. **A SPEC.1 rider lands first** — web-authored — then the copy map. Set equality cannot be asserted against two candidate sets |
| **PD-8-10** | `admin_events` has no writer anywhere in `src/`; F-ADMIN-5's union arm is permanently inert | B | tier-1 · SPEC.1 §15 opening + F-ADMIN-5 *System* · **SPEC.2 §19.3** | `audit-feed.ts:189-200` · 20 insert sites across 13 tables, none `adminEvents` · admin lifecycle emits to `events` (`auth/admin/login.ts:117`) | routed | open | ⚠ **BIGGER THAN THE ADMIN UI.** **SPEC.2 §19.3 ships `admin_events` in the 2026-11-06 public dataset** — table 8, Bucket A, YES, *"Admin-action audit trail"* — inside a contracted **16 shipped / 5 not** count and a 9-table Bucket-A enumeration, and named again in §19.4's PII row. A zero-writer table ships as a published artifact asserting a complete record that has none. **Nine admin lifecycle event types already exist in `events`** (2 `admin.*` + 7 `market.*`) | **`ADMIN-EVENTS-WRITER`**, `docs/parked.md`, **dated pre-go-live**. ⚠ **Founder fork, not CC's.** ✅ **Mitigating: no exporter exists yet** — no `src/server/dataset`, no export script — so this is decidable now at SPEC cost, not later at data cost. The page already declares the emptiness in a `role="note"`; that honesty is deliberate and stays until this lands |
| **PD-8-11** | F-ADMIN-4's inline participant Remove/Ban arm is entirely unbuilt | B | tier-1 · SPEC.1 §15 F-ADMIN-4 *Surface — inline* · §15.1 · §15.3 · **§17 (three acceptance rows)** | `moderateComment` has exactly ONE call site in all of `src/`: `ReviewFeed.tsx:75`. Zero admin affordance under `(public)`/`src/components` | **accepted-divergence** | closed | The reactive loop is functional end-to-end from `/admin/moderation`; the inline arm is operator convenience, not moderation capability | ⚠ **FOUNDER-ACCEPTED, 2026-08-12 (P12).** Recorded at `ADMIN-INLINE-MODERATION-ACCEPTED`, `docs/parked.md`, **which names the three SPEC.1 §17 acceptance rows that ship unmet.** An acceptance whose cost is not written down is a gap someone finds in November |
| **PD-8-12** | The three F-ADMIN-4 deferrals were docketed to DEBATE.7, which closed a month BEFORE they were minted | S | tier-3 · `docs/plans/UI-6.md` + `docs/logs/UI-6.md` vs `docs/logs/DEBATE.7.md` | DEBATE.7 closed 2026-06-19 (`02f87ac`, #143); UI.6 landed 2026-07-22 (#262) — a 33-day gap | routed | closed | ⚠ **They were not "awaiting DEBATE.7" — they had no owner at all**, and one was child-safety adjacent. `docs/parked.md`'s *UI-6 Gate C D4* row points at the same void owner | **CLOSES BY RULING.** `PD-8-11` · `PD-8-13` · `PD-8-14` now carry dispositions instead of a phantom owner. **H-P8-7 discharged** — the halt that catches a delta owned by a closed task |
| **PD-8-13** | The LD-3 text-only `sexual/minors` carve-out ban-review surface is unbuilt | B | tier-1 · SPEC.1 §15 F-ADMIN-4 | Reason enum + `ReasonBadge` label exist; the audit page is read-only and carries no action | **accepted-divergence** | closed | ⛔ **H-P8-4 fired at recon and at every subsequent round. Reported as status only — never designed, scoped, sketched or enumerated, at any point in four Gate C reads** | ⚠ **FOUNDER-ACCEPTED, 2026-08-12 (P12).** Basis recorded: detection by the classifier on the text arm; response by reactive admin removal, which is built and live. ⚠ **The recorded acceptance names what it does not cover** — `sexual/minors` scores **0** on image input (`omni-moderation-2024-09-26`), the PhotoDNA hash gate is parked and unwired, and thresholds are untuned until HARDEN.5. Full text at `ADMIN-INLINE-MODERATION-ACCEPTED` |
| **PD-8-14** | Track-A auto-actioned rows never reach the review feed, and §15's required *"links to the audit record"* renders nowhere | B | tier-1 · SPEC.1 §15 F-ADMIN-4 | `review-feed.ts` returns live `comments` rows only; Track-A content persists no comment, so no row can exist | **accepted-divergence** | closed | Track-A rows are reachable only via the audit feed | ⚠ **FOUNDER-ACCEPTED, 2026-08-12 (P12).** Recorded at `ADMIN-INLINE-MODERATION-ACCEPTED` |
| **PD-8-15** | No admin sign-out affordance exists on any `(admin)` route; the admin cookie has no expiry | F | tier-1 · SPEC.2 §8.6 / F-AUTH-5-ADMIN | `auth/admin/logout.ts:30` is fully built (session DELETE + `admin.signed_out`, atomic) with **zero call sites**; `login.ts:218-225` sets no `maxAge` and no `expires` | routed | open | An admin session, once opened, ends only by manually clearing the cookie or deleting the row out of band | **`ADMIN-SIGNOUT`**, `docs/parked.md`, **dated 2026-08-15**. ⚠ **H-P8-1 fires on IMPORT, not only on edit** — the fix imports `src/server/auth/admin/**`, a CLAUDE.md §1 critical path. Own chat, full ritual. **Scoped to cover the cookie lifetime too** — how an admin session ends is one property |
| **PD-8-16** | `loadAdminMarketsOverview` issues no `.limit()` — the Markets tab reads and renders every `markets` row | F | tier-1 · SPEC.1 §15.3 (*"The list itself is deliberately thin"*) | `src/server/admin/markets/overview.ts:30-39` · rendered `markets/page.tsx:80-97` | routed | open | Bounded in practice — market count is admin-controlled and small. Filed as correctness, not as a live performance claim | **`AUDIT-ORDER-TOTAL`**, `docs/parked.md`. **H-P8-1** — `src/server/admin/**` |
| **PD-8-17** | Audit feed ordering is not a total order — three queries order by `createdAt DESC` with no tiebreaker | F | correctness · tier-1 SPEC.1 §15 F-ADMIN-5 | `audit-feed.ts:72,155,199` — contrast `review-feed.ts:168`, which orders `desc(createdAt), desc(id)` | routed | open | ⚠ **The internal contrast is the proof**: the same file family gets this right one module over. Ties are nondeterministic at the 200-row boundary, so rows can appear and vanish across reloads | **`AUDIT-ORDER-TOTAL`**, `docs/parked.md`. **H-P8-1.** ⚠ **`PD-8-18`'s acceptance is CONDITIONAL ON THIS** — a stable cap is acceptable; a nondeterministic boundary is a different thing |
| **PD-8-18** | The audit feed has no pagination at all — rows beyond 200 are unreachable by any means | R | tier-1 · SPEC.1 §15 F-ADMIN-5 · POLISH-0 §3 pre-recorded X4 | `audit-feed.ts:26` `AUDIT_FEED_DEFAULT_LIMIT` · the cap IS disclosed on-page at `audit/page.tsx:498-500` | **accepted-divergence** | closed | One moderator over 51 days; `admin_events` has no writer so real volume is `mod_actions` only, written on Remove/Ban; a 5-predicate search substitutes for paging | ⚠ **FOUNDER-ACCEPTED (P12), CONDITIONAL ON `PD-8-17`.** If `AUDIT-ORDER-TOTAL` does not land, the acceptance lapses. ⚠ **The canon §10 append that §5 attaches to `accepted-divergence` does NOT apply** — that mechanism was written for visual divergences and this is functional, on a surface with no design baseline. Recorded here with the reason, rather than manufacturing a design-canon row for a pagination decision |
| **PD-8-19** | Create-market: an uploaded image cannot be removed or reordered before submit; a reload regenerates `marketId` and orphans every already-PUT R2 object | F | tier-1 · SPEC.1 §15 F-ADMIN-1 | `create-market-form.tsx:39,78-87,156-170` — `useState(() => uuidv7())`; `displayOrder` is append-position-only | routed | open | The only recovery is a page reload, which is what causes the orphaning | **`ADMIN-MEDIA-EDIT`**, `docs/parked.md`, pre-go-live. ⚠ **Eight markets open on 2026-09-15** — this is the form that creates them |
| **PD-8-20** | POLISH-0's invariant PROOF is imprecise, and `ReviewFeed.tsx` makes a false claim about itself | S | POLISH-0.md §3 · CLAUDE.md §3 | `ReviewFeed.tsx:6` imports `@/components/debate/format`; `:22-25` claims *"ZERO product components"* shared with the participant tree | routed | fixed | ⚠ **The invariant HOLDS — the sentence proving it did not survive checking.** True of *components*, false as a *directory-scoped* statement (O-2) | **BLOCK 2 of this pack.** ⚠ **Do NOT "fix" the code by re-hand-rolling `formatDharma`** — `no-raw-dharma-render.test.ts` scans `(admin)` and would fail it. **H-P8-2's ratified exception**: pure formatters already imported are permitted |
| **PD-8-21** | The row said "7 routes"; there are 8 routable entries | S | POLISH-0.md §3 | `POST /admin/markets/media/sign` (`markets/media/sign/route.ts`, ADR-0027) is a live endpoint inside the scope directory, absent from the row | routed | fixed | A component list is not the route, and neither is a page list | **BLOCK 2 of this pack** |
| **PD-8-22** | The Components cell omitted 8 of 15, including `BanIndicator` — the component holding this row's own pre-recorded delta | S | POLISH-0.md §3 · POLISH-SURFACE-TEMPLATE §2 | `BanIndicator` · `ReasonBadge` · `CategoryChips` · `ImageWithheld` · `Field` · `SearchResultRow` · `ActionForm` · `Row` | routed | fixed | ⚠ **Exactly the failure §2 predicts** — *"POLISH.2's list omitted `StatLine`, which carried six deltas"* — and here the omitted component holds R7 | **BLOCK 2 of this pack** |
| **PD-8-23** | Tier 3 cited `docs/logs/UI-6-log.md`, which does not exist | S | POLISH-0.md §3 | Actual path: `docs/logs/UI-6.md`. The corpus convention is a shared stem (`docs/plans/POLISH-7a.md` / `docs/logs/POLISH-7a.md`) | routed | fixed | A citation is not an artifact | **BLOCK 2 of this pack** |
| **PD-8-24** | The row omitted SPEC.1 §15 — the FUNCTIONAL baseline — from Tier 1 | S | POLISH-0.md §3 | The Tier-1 cell listed five ADRs and no SPEC | routed | fixed | ⚠ **The priority axis for this surface is functional completeness, and its baseline was absent from the row.** Every axis-B finding in #323 is measured against §15 | **BLOCK 2 of this pack** |
| **PD-8-25** | The invariant-check control set omitted audit search, media sign and admin sign-out | S | POLISH-0.md §3 | Verified control set is 13, not 6 | routed | fixed | Two of the three were missing from the relay as well as from the row, so nothing would have caught it | **BLOCK 2 of this pack** |
| **PD-8-26** | POLISH-TRACKER §6 states the `.7a` cost as EIGHT and enumerates SEVEN | S | POLISH-TRACKER.md §6 | The figure appears twice as eight; the bulleted spend lists seven items | routed | fixed | ⚠ **The most persistent genus in this corpus — SEVEN instances, four of them inside PR #323 itself**, including once in the section that minted the rule against it | **BLOCK 5 of this pack**, and the structural treatment at `POLISH-SURFACE-TEMPLATE.md` §13. ✅ **The mechanism has now caught one before push** (a 20-vs-19 commit count at read 3), which is the first time an instance died at authoring rather than at a founder |
| **PD-8-27** | `no-raw-hex-view-layer.test.ts` does not reach `src/app/(admin)/**`, and declares no exclusion | S | RULE-1 axis ③ | `SCAN_DIRS = ["src/components", "src/app/(public)"]` + 9 named `SCAN_FILES`, none under `(admin)` | routed | open | ⚠ Its docblock scopes itself to *"the participant view layer"*, so this is arguably by design — but the scope is **undeclared** for admin, unlike `side-pole-binding.test.ts`, which carries a reasoned `ADMIN_EXCLUSION` | **Quality lane, with `NO-RAW-HEX-REACH`.** ⚠ Adding `(admin)` changes **what the guard claims to be** and needs the docblock corrected in the same edit — a decision about a guard, not a reach bug. **H-P8-3** |
| **PD-8-28** | The same guard has no palette-class rule, so it would not catch `text-white` even with reach | R | POLISH-0 §0 R15 | Its only matcher is `HEX_LITERAL = /#(?:[0-9a-fA-F]{8}\|{6}\|{4}\|{3})\b/`; a Tailwind palette class contains no `#` | **duplicate-of-known** | open | ⚠ **Two independent gaps over the same line** — reach and rule — and collapsing them would leave one unfixed | **R15, quality lane — one visit, THREE fixes** with `NO-RAW-HEX-REACH`'s N5 floor and `SCAN_DIRS` residuals. ⚠ **`PD-8-01` REMOVED R15's ONLY LIVE INSTANCE.** It must now be minted RED by planted-offender mutation. See POLISH-0 §0 |

## POLISH.7b · Onboarding deck + coach-marks

*⛔ Blocked on O1.*

| ID | Title | Class | Baseline | Evidence | Disposition | Status | Root cause | Routed to |
|---|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — | — |

---

## Pre-recorded — found at POLISH.0, before any inspection

These were found by recon, not by a polish pass. **If an inspection re-discovers one, it takes disposition `duplicate-of-known` and cites the ID here — it does not become a second row.** IDs are `PD-0-nn` because POLISH.0 found them.

| ID | Title | Class | Baseline | Surfaces | Disposition | Status | Routed to |
|---|---|---|---|---|---|---|---|
| **PD-0-01** | `<Plus /> Full` where CD-A ratified a **"Read more"** text link. `Read more` has zero occurrences repo-wide | V | tier-2 · CD-A close-out, 2026-07-14 | .3 · .5 · .6 | routed | open | R4 · **RULED 2026-08-12 — ADOPT** (POLISH-0 §0). **POLISH.3 PR 2** · `.5` · `.6`. B11 · CC-LIGHT. ✅ CD-A's `#989898`/`#FAFAFA` ARE `--color-n5`/`--color-ink` — ports to `text-n5 hover:text-ink`, no raw hex |
| **PD-0-02** | `PostCard` `Đ BET` and `Support / Counter` are **literal `disabled`** — no auth prop reaches the component. Signed-in participants see *"sign in to bet"* | F | tier-4 · d5 mockup `:1099-1103`, `:1246-1250` (live handlers) | .3 | routed | open | R1 · **RULED 2026-08-12 — BOTH CONTROLS REMOVED** (POLISH-0 §0). Three sites: `PostCard.tsx:111-119` · `:120-128` (**PR 2**) · `DebateColumn.tsx:58-66`, dead (**PR 1**). B6 · CC-HEAVY. ✅ **ADR-0034 D-1 is DISCHARGED** — the remedy is removal, so no `DebateViewModel` field is added |
| **PD-0-03** | `PostPopup` at `max-w-lg` (~512px) vs CD-A's 720px; `max-h-[80vh]` vs 90vh; **no `SideBadge` at all** — bare interpolated text; CD-A header row absent | V | tier-2 · CD-A | .3 | routed | open | R5 · **RULED 2026-08-12 — SPLIT** (POLISH-0 §0). Geometry stays HERE, class V; **the missing `SideBadge` is its own class-F row, `PD-3-14`**. POLISH.3 **PR 2**. B7a · CC-LIGHT. **C3 CLOSED at `54b0b2a` (#278, 2026-07-31).** |
| **PD-0-04** | CD-A's Support/Counter footer + stake bar, and the reply-variant pop-up, are unbuilt. `ReplyPopup` → zero matches | B | tier-2 · CD-A | .3 | routed | closed | R6 · **RULED 2026-08-12 — RETIRED BY RULING** (POLISH-0 §0). `SlotHeader` is already live, so building CD-A's pop-up composer would make a **fourth** entry to one action. ⚠ **NOT `superseded`** — §5 reserves that disposition for a row cited against §2.1 bucket A, and this row was never subject to one; it is retired by a founder ruling, not by a supersession list. The canon append under the **`C-`** form batches to the POLISH.3 close-out, so no canon line can be cited yet |
| **PD-0-05** | No global banner anywhere. The **2026-11-05 freeze** has no site-wide surface — freeze copy is composer-scoped and surfaces only after a failed write | F | tier-2 · W2.11 **P4**, *"the one loud participant read-only event"* | .1 | routed | open | R2 · SCHEDULED — see POLISH-0 §0. B8 · CC-HEAVY |
| **PD-0-06** | `not-found.tsx` exists nowhere; no `global-error.tsx`. `notFound()` fires from six live sites incl. `/m/[slug]` on unknown **or Draft** slugs | F | tier-1 · the routes throw it | .1 | routed | closed | **BUILT at `acc2e03` (#283, 2026-08-02):** `src/app/not-found.tsx`, `src/app/global-error.tsx`, `src/app/(public)/not-found.tsx`. **R3 discharged.** |
| **PD-0-07** | `text-white` at `audit/page.tsx:75` — the only Tailwind palette colour class in `src/`, bypassing the `--color-*` layer | V | tier-2 · token contract v0.4 | .8 | routed | open | R7 · SCHEDULED — see POLISH-0 §0. B12 · CC-LIGHT |
| **PD-0-08** | Five loading skeletons shipped; **W2.11 T1 ratified none**, and the locked kit P1–P6 has no loading primitive | R | tier-2 · W2.11 T1 | .1 · .2 · .5 · .6 | — | closed | **R8 RULED — T1 SUPERSEDED.** P7 minted at `ui/loading-block.tsx`, canon amended in the same commit (DISCOVERY-COMPLETE C10, `e5827dc`). Kit is **P1–P7**. |
| **PD-0-09** | At least five distinct empty-state implementations; **W2.11 P1 locked one shape** | V | tier-2 · W2.11 P1 | .2 · .5 · .6 | routed | open | **R9 · RULED** — reconcile to W2.11 P1's one shape, batched per surface. Executed for Discovery at DISCOVERY-COMPLETE C9. **Open for .5 and .6.** per-surface V batch |
| **PD-0-10** | Three `PositionMarker` implementations — `badges.tsx` (`secondary` + `aria-label`) vs hand-rolled `outline`, no `aria-label`, in `ArgumentList` and `BookmarkCard`. Side chips likewise hand-rolled | F | tier-2 · one component, one treatment | .5 · .6 | routed | open | R12 · SCHEDULED — see POLISH-0 §0. **root cause: primitive duplication.** Check at B1's PR whether it rewrote `BookmarkCard`; do **not** add scope to B1 · **Re-classed V → F per CC-4.** Both INV-3 arms FIXED at DISCOVERY-COMPLETE C4/C4b; one implementation survives, `(admin)` `ReviewFeed` — correct, owned by POLISH.8. |
| **PD-0-11** | Price-bar percentage labels are non-interactive (`role="img"`); the mockup wires them to `pick()` | R | tier-4 · d5 `:1038`/`:1040` | .3 | **accepted-divergence** | open | R10 · **RULED 2026-08-12 — founder-set (P12)** (POLISH-0 §0). ✅ **The `*(proposed)*` qualifier is discharged: this disposition is now founder-ratified of record, dated.** Appends to canon as **`C-PRICEBAR-1`** at the POLISH.3 close-out |
| **PD-0-12** | Sell button is **hidden** when ineligible, not disabled. W2.10 Option A never specified which | R | — (unspecified) | .5 | **accepted-divergence** *(proposed)* | open | R11 · SCHEDULED — see POLISH-0 §0. canon §10 |
| **PD-0-13** | `design-language.md:178`, `:227` still describe the debate mode selector as real — **known-stale**, flagged at `DEBATE.4.md:44`, never corrected | S | tier-2 · a baseline document is wrong | all | routed | closed | **R19 discharged at `54b0b2a` (#278, 2026-07-31).** THREE sites corrected, not two — `:178`, `:227`, and the §6 ranking clause. |
| **PD-0-14** | Turnstile is not wired — placeholder token, staging on always-pass test keys. W2.1's three ratified Turnstile states cannot be exercised | — | tier-2 · W2.1 | .7a | **data-blocked** | open | AUTH-TURNSTILE-WIRE |
| **PD-0-15** | Raw error codes (`otp_invalid`, `otp_rate_limited`) render to users | V | tier-2 · canon §6 copy | .7a | routed | open | AUTH-ERROR-COPY · ⚠ **Code corrected 2026-08-11 at POLISH.7a.** The auth string is **`otp_rate_limited`** (`src/server/auth/index.ts:154,164`). `rate_limited` occurs at six sites in `src/`, **all under `src/components/debate/composer/**` — POLISH.4's surface.** The three codes a real user reaches (`turnstile_required`, `turnstile_failed`, `otp_rate_limited`) are produced **inside `src/server/auth/**`**, which is hard floor **F4** — this is why the row routes out rather than shipping as a copy fix. There is **no `code → copy` mapping anywhere on the auth path**; `docs/logs/UI-A7.md:13` records that branching on the error code was deliberately NOT built and filed as an open Gate-C question that has no recorded outcome on `main`. ⚠ **A THIRD SITE, found at Gate C:** `tests/unit/auth/sign-in-render.test.tsx` fixtured and asserted `rate_limited` too — S-11 named two sites, the underlying flag had three. `PD-0-13`'s lesson verbatim: a remedy scoped from a ROW rather than from the FLAG will under-fix. |
| **PD-0-16** | The chart's **expanded-overlay** variant has no **presentational** baseline at any tier | R | tier 1 present (SPEC.1 §9 F-DEBATE-5 + §16.1/App. B + §17 × 8) — tier 2, 3 and 4 silent on presentation | .3 | — | closed | **R13 · RULED 2026-08-12 → `design-canon.md` §10 `C-CHART-1`.** ⚠ **Reclassed S → R, and the original finding was wrong in a specific way worth keeping.** `SPEC.CHART` is a genuine phantom — absent from `docs/specs/` and from the whole repository, re-verified at `198d1d0`. But the conclusion drawn from its absence — *"no baseline at any tier"* — does not follow: **SPEC.1 §9 carries "Market price history — the market-detail chart" and F-DEBATE-5**, appended at v1.0.22 on 2026-07-23, specifying the collapsed and expanded modes by name, with eight `debate-view::price-chart-*` §17 rows and a pinned `MARKET_SERIES_MAX_POINTS`. It sat inside the section POLISH.3's own Tier-1 cell already cited, and the ⟐ marks on that cell's §16.1 and §17 entries record that neither was read. **Class S is a SPEC-FIRST halt on a build; the build shipped at UI.19 and is test-pinned, so nothing was halted.** What is genuinely absent is a *presentational* baseline — ticks, node form, legend, panel, backdrop, close-control form — which SPEC.1 §9 routes to canon **by name**, and which canon never received. `C-CHART-1` closes it. No spec written; no divergence accepted; `docs/parked.md`'s SPEC.CHART row closes citing the ruling. |
| **PD-0-17** | **Header Balance and the composer's spendable figure legitimately differ by `DAILY_CREDIT_DHARMA` (10 Đ) on any unclaimed day.** A user can correctly render **1,000** in the header and **1,010** in the composer at the same instant | — | tier-1 · the two reads are correct as built | .1 · .3 · .5 | **superseded** | closed | **NOT A DEFECT. Do not open a row.** `accrueDailyCredit` fires *inside* `place()`, so an unspent daily credit is spendable but not yet in the ledger. `header-balance.ts` pins a BALANCE-FIRST / CURSOR-SECOND statement order precisely so the worst case is a one-credit UNDERSTATEMENT rather than an overstatement — a header that promised capacity the composer rejected would be the real bug. Recorded at STAGING-PARITY Slice C/D, citing the **SHELL-COMPLETE close-out** (where a test that pinned the two equal was caught as the near-miss) and Ratification Record **W-D** |
| **PD-0-18** | **The profile Dharma graph renders empty (or near-empty) for ALL generated staging data** | — | tier-1 · the x-domain is correct as specified | .5 | **superseded** | closed | **NOT A REGRESSION, and not fixed by STAGING-PARITY.** The §23 graph x-domain is **hard-pinned 2026-09-15 → 2026-11-05** as module-private consts at `src/server/profile/graph-series.ts:31–34`, with no env or config override. Everything the generator produces is stamped *now* (August), and **backdating `created_at` is forbidden** — it would mean writing timestamps the engine cannot produce (P-10). Manifest §3. Remedy is **GRAPH-WINDOW-OVERRIDE**, a separate task behind a SPEC.1 §23 rider. ⚠ Slice B removed the *other* cause (staging had no event-backed data at all); an inspector seeing an empty graph now is seeing the domain, not missing rows · **data-blocked** — qualifier moved from the status cell. |

---

## Staging fixture coverage — the standing reference

> **Emitted by gate 4**, per rebuild, to `docs/polish/staging-coverage.json`. That file is the machine-readable copy and carries the SQL probe behind every row; this table is the human one. **Regenerate with `pnpm staging:rebuild`** — the list is byte-identical across cold rebuilds (md5 `2b6b0bc4`), and gate 4 **fails RED** if a rebuild emits anything different, so a change here is always a deliberate fixture change rather than run-to-run noise.
>
> **48 entries · 46 reachable · 2 unreachable.** Every reachable entry was confirmed present by a targeted query at emit time; every unreachable one names a manifest §3 reason. Pseudonyms are pool-allocated FIFO and stable across rebuilds, so these URLs do not rot.

**Markets (§2.1)** — all under `https://staging.zugzwangworld.com`

| Row | URL | What to look at |
|---|---|---|
| M1 Draft | `/m/sp-m1-draft` | **must 404** for a participant — Draft is admin-only |
| M2 Open, heavily active | `/m/sp-m2-active` | the primary POLISH.3 subject; carries all of §2.3 |
| M3 Open, lightly active | `/m/sp-m3-light` | **no badge renders** — Top falls back to closest-to-landslide |
| M4 Open, brand new | `/m/sp-m4-new` | both `EmptySideCTA` slots |
| M5 Closed | `/m/sp-m5-closed` | read-only; write affordances gated |
| M6 Resolving | `/m/sp-m6-resolving` | read-only; distinct badge |
| M7 Resolved (YES) | `/m/sp-m7-resolved` | settled positions; the four-digit P/L carrier |
| M8 Voided | `/m/sp-m8-voided` | the void path differs from resolve |
| M10–M16 filler | `/m/sp-m10-fill` … `/m/sp-m16-fill` | Discovery hero + full grid (`DISCOVERY_GRID_SIZE + 1`) |
| ~~M9 Frozen~~ | — | **unreachable, permanent** — manifest §1.8/§3. Never attempt it on staging |

**Participants (§2.2)** — `/u/<pseudonym>`

| Role | Pseudonym | What it proves |
|---|---|---|
| P-owner | `RedFox000` | owner arm of Profile / positions / bookmarks; holds YES on M2 |
| P-visitor-target | `RedWolf001` | a populated profile viewed as a **visitor** — the DTO split |
| P-empty | `RedOtter002` | every empty state at once; balance is **exactly 1000** |
| P-flipped | `RedBadger003` | the `Flipped` marker; its pre-flip YES comment is still YES (INV-3) |
| P-exited | `RedLynx004` | the `Exited` marker; position at zero, never re-entered |
| P-removed | `RedHare005` | one removed comment, one surviving — masking without a ban |
| P-banned | `RedOwl006` | banned, **past content intact** (ADR-0021) |
| P-crowd-1/2/3 | `RedHawk007` · `RedStoat008` · `RedPine009` | lane volume, reply counts, the interleave |

**Content, positions, bookmarks, moderation (§2.3–§2.5)**

| Row | URL | What to look at |
|---|---|---|
| C1 both sides | `/m/sp-m2-active` | both YES and NO columns carry posts |
| C2 interleave | `/m/sp-m2-active` | 12 posts — a newest-post injection after every 10 ranked |
| C3 three badges | `/m/sp-m2-active` | **one per lane** — Most Debated, Highest Stakes, Contested |
| C4 majority unbadged | `/m/sp-m2-active` | **9 of 12 carry no badge** — the criterion, not an omission |
| C5 many replies | `/m/sp-m2-active` | `ReplySplitBar`, expand, stake ordering within side |
| C6 zero replies | `/m/sp-m2-active` | the empty reply state inside a populated market |
| C7 image | `/m/sp-m2-active` | in-card clip, then the whole-render pop-up |
| C8 truncation | `/m/sp-m2-active` | the "Read more" affordance |
| C9 removed post | `/m/sp-m2-active` | body masked, **its two replies still readable** |
| C10 removed reply | `/m/sp-m2-active` | reply-level masked variant, parent untouched |
| C11 chart | `/m/sp-m2-active` | multi-point chart with post nodes |
| Q1 sellable | `/u/RedWolf001` | the Sell affordance on an Open market |
| Q2 terminal | `/u/RedStoat008` | Sell is **hidden**, not disabled |
| Q3 settled | `/u/RedFox000` | net P/L on the winning side |
| Q4 opposite-slot | `/m/sp-m2-active` | as **P-owner**, the NO composer is `oppositeHeld`-disabled |
| Q5 zero positions | `/u/RedOtter002` | empty `PositionsTable`, owner and visitor copy |
| B1 bookmarks | `/bookmarks` | as **P-owner** — one post + one reply, both others' |
| B2 zero bookmarks | `/bookmarks` | as **P-empty** — the empty page |
| X1 · X2 removals | `/m/sp-m2-active` | the masked cards, and their rows in the audit feed |
| X3 ban | `/admin/moderation/audit` | the ban is logged; the author's past content still renders |
| ~~X4 pagination~~ | — | **unreachable** — a product gap needing a POLISH.8 ruling |

---

## Duplicate-of-known — Phase B defects, already owned

Do **not** open register rows for these. They are being fixed before their surfaces are inspected.

| Known defect | Surfaces | Owner |
|---|---|---|
| **BOOKMARK-ADD-WIRE** — the add path is inert across Market Detail, Profile and Bookmarks | .3 · .5 · .6 | ⚠ **STALE CELL, CORRECTED AT COMMIT 0 (2026-08-14): this SHIPPED 2026-07-30, PR #273.** It is no longer "plan phase live" and ADR-0034 is no longer a pending gate. ⛔ **BUT IT IS NOT WHOLLY DISCHARGED — its slice 4, the PROFILE arm, was DEFERRED**, so `.5` still inherits an unwired add path and must not read this row as closed. `.3` and `.6` are shipped |
| **PCT.ROUND** — YES% + NO% can render **101** | .2 · .3 | B2 |
| **F-DEBATE-4** — interval polling on `/m/[slug]` | .3 | **B3 — CLOSED.** Plan and log both on `main`; behaviour pinned at `tests/unit/debate/render/poll.test.tsx` (`:144` cadence · `:196` suspension · `:280` stop rule) |
| **UI.11** — no AGPL source offer exists anywhere in the shell | .1 | **B4 — WITHDRAWN, not open.** SPEC.1 v1.0.26 `:1498`, 2026-08-02: *"B4 (UI.11) is withdrawn, not rescoped."* The AGPL §13 obligation survives, relocated to the ToS body |

---

*Extended at POLISH.2 (2026-08-09) and at DISCOVERY-COMPLETE's post-staging pass (2026-08-10): **thirty-four** `PD-2-nn` rows allocated under POLISH.2 · Discovery — the first inspection-derived rows in this register. **(Recounted at POLISH-TEMPLATE: the figure read "thirty-one" because PD-2-32/33/34 sat below a blank line and outside the table body, so the stale count and the broken parse agreed with each other. The blank line is gone; the count is read off the file.)** The POLISH.2 table body is **44** rows — 34 `PD-2-nn` plus eight `CC-n` and two `RR-n`, which share the table. `PD-0-18` remains the pre-recorded high-water mark; `PD-2` is a fresh per-surface series per the `PD-<surface>-<nn>` scheme above. **Five `POLISH-register-ADDITIONS.md` rows remain unapplied** — three (§B) + one (§C) + one (§D) — and are NOT among these. ⚠ **The remainder is ENUMERATED, not subtracted**, per the recount recorded two sentences above. ⚠ **Two were applied at POLISH.3's commit 0, 2026-08-12** — §C's Đ-glyph and pluralisation rows, allocated `PD-3-07` and `PD-3-08` — **and all six of §A at POLISH.5/6's commit 0, 2026-08-14**, allocated `PD-5-03` … `PD-5-08` under POLISH.5 · Profile. **The `PD-5-nn` clause of `REGISTER-APPLY` is thereby fully discharged**; that file's apply-checklist has now run **twice** — item 4 for POLISH.3, item 2 for POLISH.5.*

*Scaffolded by web Claude, 2026-07-30 IST. **Eighteen** pre-recorded rows, none from an inspection. Ruling state for every `Rn` lives in `POLISH-0.md` §0, the ruling index. There is no standalone ruling register. Extended at STAGING-PARITY Slice C/D (2026-08-06): PD-0-17 and PD-0-18 pre-recorded as `superseded` so POLISH.5 does not spend a founder review-hour re-deriving two known-correct behaviours, plus the standing staging coverage reference above.*

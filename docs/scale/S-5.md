# S-5 — ⛔ SUPERSEDED. Read the five documents below instead.

> **Status:** SUPERSEDED 2026-08-30, same day it was written · untracked, uncommitted
> **Superseded by:** `S-5-MASTER-REVIEW.md` · `S-5-LOAD-PLAN.md` ·
> `S-5-BOTTLENECK-REGISTER.md` · `S-5-OBSERVABILITY-PLAN.md` ·
> `S-5-CACHE-PERFORMANCE-PLAN.md` · `S-5-SCALE-ROADMAP.md`

---

## Why this file is a pointer and not a plan

It was written by reading `origin/main` and nothing else. **The programme that governs S-5 is
not in the repository** — the scale tracker, the ground-truth handover, the operating ritual
and a prior S-5 proposal (`S-5-LOAD-TEST-APPROACH.md`, 2026-08-27) all live only in
`~/Downloads`. A plan built from `main` alone structurally could not see them.

**What that cost, recorded rather than quietly rewritten:**

| # | The draft said | The governing documents say |
|---|---|---|
| 1 | *"⛔ No throughput number anywhere"* | ⛔ **False, and falsifiable from `main` in one grep** — `docs/plans/ENGINE.10.md:32` carries `p95 < 500 ms @ 5k VUs`, deferred to *this* stratum |
| 2 | Read, signup and write measured **separately** | The ratified profile is **mixed** — *"~90% audience / ~10% bettors"*, *"read load on hot markets **while writes land**"* (ENGINE.10 axes 9, 10) |
| 3 | The 50,000 identity pool is *"a hard wall"*; extension out of scope | Ground truth §9: *"≈900k namespace. **ADR-0011's 50k ceiling no longer binds**"*, and the tracker makes `identity_pool → 100k` an **S-5 deliverable**. ⛔ The real blocker is `docs/parked.md` **STAGING-POOL-UNDERSIZED** — 185 unassigned rows, *"empties in 3.7 seconds"* — which is on `main` and the draft walked past it |
| 4 | Phase 6 would *"answer W-11 / `max`"* | **W-11 is routed to S-8** (F-11, Wed 2 Sep). S-5 produces the input; S-8 rules |
| 5 | Phase 5 measures bet **contention** | Ground truth §9 decision 6: *"bet-contention measurement **dropped**"* — reinstating it needs a ruling (D-9) |
| 6 | Two deliverables absent | The tracker's S-5 contents also name **moderation throughput (verify only)** and the **credit-rollover herd** |
| 7 | No DGX, k6 filed as an open *choice* | Mode is **ULTRA + DGX**; *"k6 rig on DGX"* is in the contents row. The selection is ratified — **the open item is the install** |
| 8 | Treated ADR-0043 as *"just fixed"* | *"S-5 · run \| Waits on S-1 **and** S-3 \| Cannot test 30/s signups while signup jams at four"* |

**What survived and was carried forward:** the three Stage-0 preconditions
(`PENDING_TTL_SECONDS`, `connect_timeout`, the unbounded request wall clock — all three
re-verified open on `main`), the four staging-vs-production fidelity gaps, the S-4-not-closed
sequencing question, the vendor-ceiling inventory, the rate-limiter self-interference
analysis, and the post-run invariant reconciliation obligation.

---

## Open before anything proceeds

⛔ **D-0** — the scale tracker assigns S-5 to **Dev C**; this lane is Dev B. Not established
that the lane is open.
⛔ **D-0b** — load run #1 was scheduled **Sat 29 Aug**; S-5 closes **Mon 31 Aug**.
⛔ **D-0c** — Stage 0's preconditions are three critical-path edits, and ULTRA is *"**Never**
for the five critical paths"*. It needs splitting out as a RITUAL stratum.

Full register: `S-5-SCALE-ROADMAP.md` §1.

---

*Left in place rather than deleted, per this project's convention of amending forward rather
than editing history. Nothing in this file should be executed.*
